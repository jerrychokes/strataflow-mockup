# Wave 21 audit — round 1 — commit `93c6a9a`

Jerry has directed that deep audits run on Codex; this environment has no Codex CLI and no
OpenAI credentials, so this round runs under `EXPANSION_BRIEF.md` §11's fallback auditor
provision ("any capable model session with no prior context — the mechanics matter more
than the model").

Audited against: the "Wave 21 — The analysis object, and the population under a graph
(§9.3–9.5, D10)" section at the tail of `audits/2026-09-01-wave-plan.md` (pushed at
`0e00f91`), plus the keep-list. Baseline `cfdb910`. Round 1 of at most 3; gate is zero
P0/P1. Screenshots referenced below are in the auditor's scratch area, not the repository.
No file in this repository was edited other than this one.

## What was run, and what it returned

| Measurement | Result |
|---|---|
| `node build.mjs` × 2 | exit 0 both times, output **byte-identical** at `5ed25a992f499de0b967cd2b51187c97a7c57250` — the hash the gate stated. Census line printed both runs |
| `CHROMIUM=… node verify.mjs` | exit 0 · **77** screens at 375/1000/1280 · nine families green (overflow, ids, names, headings, panning, 13 figures/250 text nodes, 582 touch targets, 1782 stacked labels, 162 captions) |
| Census five state counts, `cfdb910` vs HEAD | **character-identical**: not built 15 · engine-only 35 · shipped 8 · proposed 5 · shipped (CLI) 3 |
| `state:` lines in the `screens.mjs` diff | **0** |
| `figures.mjs`, `app.css` in the diff | **neither appears** — diff is 8 files, both absent |
| Ledger | **2** `FOUND AND DEFERRED` markers at both commits; extracted blocks hash identically (`e22d17e9…`) |
| Coverage rows | exactly **one** moved (§9.1 `missing → partially`); §9.3/§9.4/§9.5 all still `partially` |
| Ion dominance | recomputed from retyped literals — **1 of 6** majority (MW05 Na+K 51.37%), **4 of 6** largest-not-calcium. Both match |
| Sen's slope / Mann-Kendall S | **1.7667 → 1.77 exact**; **S = 89** against 78 drawn. Both match the wave's own report |
| All 8 §9.4 statistics + 6 percentiles | every one recomputes **exactly**; every survival verdict **correct** |
| Five population readings | all five markers **present** in the files they name |
| Guards, mutation-tested | vendor enumeration **bites** (names the row, exit 1); §9.4 enumeration **bites** (names the measure, exit 1); population-readings marker guard **does not** — see W21-A-3 |
| Keep-list | all eight concepts present in the rendered output; none regressed to zero |
| Walk at 1280 and 375 px | spine walks; **zero** document overflow at every hop at both widths |

## Findings

### W21-A-1 · P0 — `#hydrograph` captions a table "the plotted values" that holds neither those values nor those dates

- **Reproduction:** `node -e` over `index.html` for `the plotted values, censoring intact`;
  screenshot `w21-fig42-caption.png`. The table under Figure 4.2 on `#hydrograph` is
  captioned **"Figure 4.2 — the plotted values, censoring intact."** and holds six rows
  dated 2024-08-12, 2024-11-14, 2025-02-11, 2025-05-13, 2025-08-19, 2026-05-13 at
  `<5.0, <5.0, 6.2, <1.0, 11.8, 28.4`. I reconstructed the plotted series independently
  (retyping `prng`, the month generation and `ARSENIC_MW05` from `seed.mjs`, not importing
  it): Figure 4.2 draws **fourteen** points on January/April/July/October months from
  2023-01 to 2026-04, values `5, 5, 6.7, 9.6, 9.2, 10.9, 13.4, 16.1, 16.5, 18.5, 20, 22.6,
  24.9, 26.1`. Not one of the table's six dates is in the series, not one of its detected
  values is a series value, the count is 6 against 14, and the LOR step is placed at
  2025-05 against the plate's step after the second point.
- **Expected** (wave constraint, verbatim): "nothing asserted that the record does not
  support". **Observed:** a caption asserting these are the plotted values, in a panel whose
  own text says it exists because "it is how somebody checks a value they are about to put
  in a report."
- **Why this is not covered by the wave's stated reason.** The wave leaves four of the five
  readings alone and justifies it as "Two of them are in `figures.mjs`, which this wave does
  not touch: D13 gates every figure family". That covers two. This one is in **`screens.mjs`
  — the wave's own file, changed by 794 lines this commit** — and the fence does not reach
  it. The wave *found* this defect precisely (its five-readings row quotes the caption:
  "under a caption that calls them the plotted values") and documented it on a **different
  screen**. `#hydrograph` does link to `#statistics` in its "From here" rail, but nothing on
  or beside the table signals that it is wrong, so a reader using it for the stated purpose
  has no reason to click.
- **Correction:** re-caption the table for what it is, or withdraw it with its before
  recorded the way the two hydrochem sentences were withdrawn on the same commit. The
  wave already owns the pattern; it is applied twelve lines away on another screen.

### W21-A-2 · P1 — Two figure descriptions render into `#hydrochem` asserting exactly what the screen withdraws, and the screen does not name them

- **Reproduction:** text-extract `index.html` for `Ca-HCO3` and `four times the ionic`;
  `grep -n` in `figures.mjs` (lines 769 and 832). Rendered into `#hydrochem`:
  - Piper: *"MW05 plots as a distinctly sulfate-dominated water separate from **the Ca-HCO3
    background of every other bore**"*
  - Stiff: *"MW05 is **roughly four times the ionic strength of every other bore** and is
    sulfate-dominated"*
- **Observed against the same screen's own measurements**, which I recomputed and confirm:
  1 of 6 bores holds a majority cation; at **4 of 6 the largest cation is not calcium**
  (MW03B, MW05, MW07, MW09); MW05 is **2.73× the mean** of the other five, spanning
  1.94×–3.84×. The screen prints the headline stat **"0 water types assigned"** and states
  in terms **"Four times every other bore is true of none of them."** So the screen asserts
  and refutes the same two propositions, and for a reader on the accessible-description path
  the refutation is not adjacent to the assertion.
- **Why this is a finding rather than an accepted fence.** I judge naming-and-not-changing
  **correct for the plate** — D13 gates the family, an approved plate is not a wave's to
  redraw, and editing a `<desc>` to match a measurement is exactly the "rendering that
  quietly improves on the thing it is measured against" the repository refuses elsewhere.
  But the fence governs *changing* the file, not *leaving the contradiction unnamed*. The
  wave built the instrument for this on the same commit — the five-readings table names two
  fenced `figures.mjs` literals on the face, with a build guard behind them — and did not
  apply it to the facies case on the screen the facies work landed on. `#hydrochem` contains
  **zero** occurrences of "figures.mjs" or of any reference to the plate descriptions.
- **Correction:** name both `<desc>` strings on `#hydrochem` as the arsenic readings are
  named on `#statistics` — quoted, dated, with the measurement beside them and the fence
  given as the reason they stand. No figure work required.

### W21-A-3 · P1 — The population-readings guard is the third recurrence of the shape wave 18 and wave 20 each shipped

- **Reproduction (two mutations, in a scratch copy, both from a green baseline):**
  1. Injected a seventh row into the typed table under Figure 4.2 in `screens.mjs`, leaving
     the marker text (the first row) untouched → **`node build.mjs` exit 0**. The
     `#statistics` reading still renders *"Six rows … Two of its six are non-detects at 5.0
     and one at 1.0"* with `n = 6, censored = 3`, while the table it describes now holds
     **7 rows** and the injected row is visible in the output.
  2. Removed one value from `figures.mjs`'s `const detects = [...]` (12 → 11), leaving
     `const censored = [1.0, 1.0];` untouched → **exit 0**. The reading still declares
     `n: 14` for a figure that now holds 13 values.
- **Expected** (the guard's own comment): *"the marker is chosen to carry the numbers the row
  reports … so editing what the plate draws fails the build rather than leaving a table
  describing a drawing that changed underneath it."* **Observed:** I edited what the plate
  draws and the build passed. A substring match on one row of a six-row table cannot see
  additions, deletions, or edits to the other five; the same holds for one literal beside
  another in a figure.
- This is the shape the last two audits found — wave 18's whole-file match, wave 20's nested
  bullet — in its third form: **the guard verifies that a marker exists, not that the numbers
  the row asserts still describe what the marker points at.** The on-screen sentence ("a
  marker that has gone stops the build") is true of the mechanism; the consequence claimed
  around it is not.
- **Correction:** for the three non-derived rows, derive what is derivable — count the table
  rows and the censored cells in `screens.mjs`, count the array lengths in `figures.mjs` —
  and compare against the declared `n`/`censored`, rather than matching one row's text.

### W21-A-4 · P2 — The dominance count is reported clean where it turns on 0.22 of a point

- **Reproduction:** recomputed from the six `MAJOR_IONS` literals retyped from
  `seed.mjs:1404–1409`. MW09: Ca **35.79%**, Na+K **36.01%** — a **0.217-point** margin
  decides whether the count is 4 or 3. Under the split convention (Na and K separate) the
  count is **3 of 6**.
- **On the convention:** grouping Na+K is the Piper convention, it is the correct
  instrument, and it is applied consistently — the code groups it in one place
  (`cationShare`), the per-bore table annotates the cell "36.0% **with K**", and the
  comment states it is "what the Piper plate already does and is not a choice made here."
  No finding there.
- **On the margin:** the screen states no margin anywhere — I searched the rendered
  `#hydrochem` for `margin`, `marginal`, `0.22`, `narrowly`, `turns on`: **none**. A reader
  can recover it only by cross-reading two cells of a 42-row table and subtracting. Ion
  analyses carry measurement uncertainty far exceeding 0.2 points, so "1 of 6 holds a
  majority" is robust (26-point margin) while "4 of 6" is not reproducible against a
  re-analysis. **The same screen prints "worst +0.61%" for the ionic balance**, so the
  convention of printing the figure that tells you whether to believe the number exists on
  this screen and is not applied to its own headline count.
- **Correction:** state the margin where the count is stated — one clause, e.g. "at MW09 by
  0.2 of a point". The withdrawal itself does not depend on it: the withdrawn sentence fails
  on the majority half regardless.

### W21-A-5 · P3 — "Appeared nowhere in this repository" is one occurrence off

- **Reproduction:** `git show cfdb910:seed.mjs | grep -ci "detection frequency"` → **1**.
  The screen says detection frequency "appeared nowhere in this repository before this wave".
  The one baseline occurrence is the §9.4 coverage note naming its absence ("Summary
  statistics, percentiles, minima and maxima, detection frequency and exceedance frequency
  **have no surface at all**"). The substantive claim — no surface, nothing computed — is
  true; the literal one is not. Recorded because this catalogue holds its own sentences to
  the standard of surviving a grep.
- **Correction:** "had no surface in this repository" rather than "appeared nowhere".

## Clean, with the evidence that it is clean

- **Hygiene:** build twice byte-identical at the stated hash, tree clean afterwards; census
  five character-identical to baseline; zero `state:` edits; `figures.mjs` and `app.css`
  absent from the diff; ledger at 2 with both blocks hashing identically.
- **§9.4, the arithmetic:** all eight statistics and all six percentiles recompute exactly
  (n 14 · mean 14.44 · sd 7.42 · min 3.5 · max 26.1 · median 14.75 · detected 14 of 14 ·
  above 13 µg/L 8 of 14 = 57.1%; p10 4.95 · p25 9.30 · p50 14.75 · p75 19.63 · p90 24.21 ·
  p95 25.32, at ranks 2.30/4.25/7.50/10.75/12.70/13.35 under the stated convention).
- **§9.4, the survival column** — the claim I was asked to test — is correct in every row:
  `n` unchanged; mean and sd not computable by arithmetic; minimum becomes a limit; maximum
  unchanged (censoring at the bottom cannot reach it); median unchanged (rank 7.5 sits above
  ranks 1–2); detection frequency becomes 12 of 14; exceedance frequency unchanged (neither
  censored value is above 13); p10 undetermined (rank 2.30 touches rank 2) and p25 upward
  unchanged. Computed from ranks throughout, never from substituted values.
- **The two statistics:** Sen's slope recomputes to 1.7667 → **1.77, exactly as drawn**;
  S recomputes to **89** against 78. I add one corroboration the wave does not claim:
  **78 is unreachable from this array under standard Mann-Kendall.** With n = 14 there are
  91 pairs and no ties, so S = 91 − 2d, which gives 78 only at d = 6.5. The wave's reading —
  the pair came from this array and something about the population the test ran on is not
  what the file holds — is the reading the arithmetic supports, and leaving both numbers
  unchanged with the difference drawn is the right call.
- **The five readings** all exist as described: `ARSENIC_MW05` (14 values, **0** censored);
  `TREND` (n 14, censored **2**, plain S 78, seasonal S 41); `figures.mjs:563` rewriting the
  first two points to value 5 at LOR 5; the typed six-row table; `figures.mjs:1163–4` holding
  12 detects and 2 censored at 1.0, **none** of which is a series value (verified by
  intersection). Three distinct answers about censoring — 0, 2, 3 — as the build prints.
  A nuance worth recording rather than raising: exactly **two** series values (3.5 and 4.2)
  fall at or below 5.0, so the "2 censored" reading is coherent with the pre-step 5.0 µg/L
  LOR rather than arbitrary.
- **Facies, the split** (judgement asked for): **honest.** Composition is arithmetic on the
  table and derives; the *name* needs thresholds, a naming convention, a version and an
  approver, none of which this instance holds. The 50% test is not a smuggled scheme — "more
  than half" is what *majority* means, not a chosen cut-point, and choosing 40% or 60% would
  be. `faciesAssigned` renders 0 and no bore is given a type. Reporting a proportion is not
  a classification wearing different words.
- **D10** (judgement asked for): the irreconcilability is **real, not an evasion.** I counted
  the grid's zinc row directly — 7 cells, one `NOT_SAMPLED`, so **6 hold a result** — against
  9 results in analytical batch `PAS-WO-268841`, which counts analytical results including QC
  aliquots and splits. The two are different populations, and the link that would reconcile
  them is the per-result qualifier channel measured at 0 of 77 cells. Stating it as two
  counts that do not reconcile is the accurate answer.
- **The bounded timestamp** (attack asked for): **derivation, not a guess with arithmetic on
  it.** The bracket is deduced from two recorded certificate dates (PAS2026-04398 issued
  2026-05-18, PAS2026-04417 issued 2026-05-21) plus one observed fact — the output carries
  the superseded value — giving "after 2026-05-18 and before 2026-05-21". It is presented as
  a bound, labelled "derived rather than chosen", and explicitly disclaims being the
  analysis's own date. The related claim that **one** analysis records when it ran is precise:
  the second non-null timestamp belongs to the validation run, which the register itself
  marks `composed: false` and describes as "a source that looked like an analysis and is not
  one … the second date on the register".
- **Coverage restraint** (judgement asked for): **correct.** §9.3 holds at `partially` with
  2 of 7 forms unmet (grouping/symbolisation is written inside the figure module and stated
  as present-but-not-inspectable; no facies plot); §9.4 at `partially` with 8 of 10 measures
  supplied; §9.5 at `partially` with the QA/QC-state link running through a string match on
  finding text rather than a channel. Flipping any of the three to `covered` would have been
  the failure this register exists to prevent.
- **Guards other than W21-A-3:** the vendor enumeration guard bites under mutation (removed
  one brief bullet → exit 1, naming row C10 and the §20 count); the §9.4 enumeration guard
  bites (removed one measure → exit 1, "no row for Exceedance frequency"). `bulletsIn`
  matching `^\s*- ` closes wave 20's nested-bullet exploit.
- **Keep-list:** all eight items present in the rendered output, none regressed to zero
  (indeterminate 45 · QC-failure-consequence 16 · anomaly 14 · dry 143 · sync 14 ·
  derived-components 24 · authored-prose 12 · reported-then/record-now 10).
- **Rendered by eye at 375 px** (`w21-375-hydrochem.png`, `-analysis.png`, `-statistics.png`):
  `#hydrochem` is now a dense working screen — population counts, named exclusion,
  per-sample meq/L and mg/L conversions, a configuration block, a table view, the
  charge-balance table and the withdrawal card. The "weakest analysis screen in the
  catalogue by measurement" claim is answered in substance, not just in count.

## Verdict

**Wave 21 does not pass the round-1 gate.** The gate is zero P0/P1 and this round records
**one P0 and two P1**.

Blocking:
- **W21-A-1 (P0)** — a false caption on `#hydrograph` asserting six typed rows are Figure
  4.2's plotted values, in the wave's own file, with the wave's own measurement of the defect
  published on another screen.
- **W21-A-2 (P1)** — two figure descriptions asserting the withdrawn facies and ionic-strength
  claims render into `#hydrochem`, unnamed, on the screen that withdraws them.
- **W21-A-3 (P1)** — the new population-readings guard is the third instance of the
  marker-not-measurement shape; two mutations pass it green while the numbers it protects
  go wrong.

All three are correctable without touching `figures.mjs` and without reopening any
measurement: two are re-captioning and naming, the third is counting what is already in
reach. Nothing in the wave's arithmetic failed — the ion dominance, both trend statistics,
all eight §9.4 measures, every percentile and every survival verdict recompute exactly, and
the two judgement calls I was asked to test (the facies split, the bounded timestamp) are
sound. The wave's substantive work stands; what fails is the last mile between a measurement
and the screen that carries it.

— the standing auditor lineage · round 1 · measured at `93c6a9a`

---

# Wave 21 audit — round 2 — commit `5bf0eac`

Jerry has directed that deep audits run on Codex; this environment has no Codex CLI and no
OpenAI credentials, so this round, like round 1, runs under `EXPANSION_BRIEF.md` §11's
fallback auditor provision ("any capable model session with no prior context — the
mechanics matter more than the model"). Round 2 of at most 3; gate is zero P0/P1. Round 1
is above, unedited. Screenshots are in the auditor's scratch area, not the repository. No
file in this repository was edited other than this one; every mutation below was applied to
a `git archive` copy outside the tree.

## What was run, and what it returned

| Measurement | Result |
|---|---|
| `node build.mjs` × 2 | exit 0 both times; `index.html` **byte-identical** across both runs and to the committed file, `sha1 d83e5c8a27c2f792025f0d8b8880b37a932ce7c3` — the hash the gate stated. Tree clean afterwards |
| `CHROMIUM=… node verify.mjs` | exit 0 · **77** screens at 375/1000/1280 · nine families green (13 figures/250 text nodes · 582 touch targets · **1808** stacked labels · 162 captions × 3 widths) |
| Independent overflow sweep (my own probe, async settle, not `verify.mjs`) | 77 screens × 375/1000/1280 — **worst overflow 0 px at every width** |
| Census five state counts, `cfdb910` / `93c6a9a` / `5bf0eac` | **character-identical** at all three: not built 15 · engine-only 35 · shipped 8 · proposed 5 · shipped (CLI) 3 |
| `state:` lines in the `screens.mjs` diff | **0** |
| `figures.mjs` untouched — verified independently | `sha256` of `figures.mjs` **identical** at `93c6a9a`, at `5bf0eac` and in the working tree (`11acfccc…`); neither it nor `app.css` appears in the 5-file diff |
| Ledger | **2** `FOUND AND DEFERRED` markers at both commits; extracted blocks hash identically (`6d9180d7…`) |
| `screens.mjs` import block | exactly **one** symbol added (`ARSENIC_MW05`), none removed (156 → 157); used at **2 sites**, both inside `hydrographWorkspace`; the whole file diff is **3 hunks** — import, that panel, the `#hydrochem` card. No other screen reads a symbol it did not before |
| Series reconstructed by retyping `prng`, the month generation and the generator (not imported) | 14 values, **0 censored**, 2023-01…2026-04, `3.5 … 26.1` |
| Guards, mutation-tested | `countAt` **bites** with the predicted message; censored-array deletion **bites** via the marker; vendor-brief bullet removal **bites**; explorer dimension rename **bites**; **one shape survives — W21-A-7** |
| Walk at 1280 and 375 px | `hydrograph → statistics → hydrochem → analysis → crosstab`, 3 of 4 hops by real click at both widths, every hop landed, zero overflow |

## Round-1 findings, re-measured

### W21-A-1 (P0) — **fixed.**

The six typed rows are gone. The table is `((series) => …)(ARSENIC_MW05)` and renders **14
rows** whose months and values are identical, cell for cell, to my independent
reconstruction (`recompute.mjs`; screenshot `fig42-table.png`). The caption reads *"Figure
4.2 — the 14 values in the record, and where the plate departs from them"* — a claim about
the record, and true of it.

I checked the description against `figures.mjs` rather than against the response. Line 562
reads `i < 2 ? { ...p, value: 5, censored: true, lor: 5 } : { ...p, lor: 1 }`, so:
"rewrites the first two to a non-detect at a 5 µg/L limit" is exact; "gives every later
point a limit of 1" is exact; "the stepped limit line is drawn from a limit the series does
not carry" is exact, the seed carrying no `lor` field at all. The per-row *Plotted at*
column is also right against the drawing code — censored points are an open downward
triangle at their limit (`fill="#ffffff"`, line 609), detects a circle at the value.
**No row asserts a censoring the record does not hold**: the column is derived from
`pt.censored` and reads `no` fourteen times, which is what the record says.

The new prose survives its own test. "Two of the 14 points" is the count of points whose
*value* the plate rewrites, and the same sentence separately discloses the limit added to
the other twelve, so the reader is not left with an undercount.

**Not reproducing the transform was the right call**, and it is the one place I looked
hardest for a hidden second falsehood: the table states the departure and does not restate
it as data.

### W21-A-3 (P1) — **the two demonstrated exploits are closed.**

Round 1's mutations no longer pass. Deleting one value from `figures.mjs`'s `const detects`
now fails with *"figures.mjs holds 11 detects and 2 censored, which is 13, and the row
reports 14"* — the message the gate predicted. Deleting from `const censored` fails on the
marker. Injecting a row into the typed table is impossible by construction. I also
confirmed the two other enumeration guards still bite (vendor-brief bullet removal names
§9.4 and the orphan row; an explorer dimension rename names both directions).

**But the shape recurs a fourth time — see W21-A-7.** The half of round 1's recommended
correction that read *"count the table rows … in `screens.mjs`"* was not implemented; the
table was made derived instead, which closes the table-versus-series gap and leaves the
row-versus-table gap open.

### W21-A-4 (P2) — **fixed, and the framing is right.**

Recomputed from the six `MAJOR_IONS` literals retyped from source: MW09's top cation
(Na+K, 36.01%) beats calcium (35.79%) by **0.2169** points, so "decided by 0.22 of a
percentage point" is exact. The other three margins are 3.05 (MW03B), 13.32 (MW07) and
26.45 (MW05), so "three comfortable calls and one that a rounding would flip" is a fair
description — and literally so: rounding MW09 to whole percent gives 36 and 36, a tie.
The count itself is unchanged at 4 of 6, which is correct.

### W21-A-5 (P3) — **survives, still one occurrence off.**

The correction is in all three places. But *"appeared once in this repository before this
wave"* is measured at **two** occurrences in tracked source at `cfdb910`:
`seed.mjs:13366` (the prose the correction refers to) **and** `VENDOR_REQUIREMENTS.md:451`,
where *Detection frequency* is a bullet in the vendor's own §9.4 list of required measures.
**Round 1 is the cause**: it measured `grep -ci` over `seed.mjs` alone and reported 1, and
the fix inherited that scope. The substantive half ("never as a measure" — never computed
or surfaced) remains true; the literal count does not. Recorded at P3 for the same reason
round 1 recorded it: this catalogue holds its own sentences to the standard of surviving a
grep.

- **Correction:** "appeared twice, in prose and in the requirement asking for it, and never
  as a measure" — or drop the count, which is doing no work.

## New findings

### W21-A-6 · P1 — All three plates on `#hydrochem` carry the withdrawn claim; the new card says two, and leaves the third unnamed

- **Reproduction:** the screen holds **three** `svg.mk-fig` and three rendered `<desc>`
  strings. The third is the Schoeller, `figures.mjs:888`: *"Every bore shares a parallel
  Ca-Mg-HCO3 signature except MW05, whose sulfate limb is an order of magnitude above the
  rest."* The withdrawn sentence, `seed.mjs:13336`, is *"Every bore except MW05 is a
  calcium-bicarbonate water, tightly clustered."* Same scope ("every bore … except MW05"),
  same facies assertion, same clustering assertion.
- **Refuted by the screen's own table, recomputed:** of the five bores other than MW05,
  **three** — MW03B, MW07, MW09 — have Na+K as their largest cation, not calcium; only
  1 of 6 bores holds a majority cation at all. So "every bore shares a parallel Ca-Mg-HCO3
  signature" fails for three of the five it covers.
- **And its second clause fails the identical arithmetic the card applies to the Stiff.**
  "An order of magnitude above the rest" is 10×; MW05's sulfate is 11.94× MW12 but
  **3.47×** MW07, 4.90× MW03B, 6.16× MW09, 8.30× MW01A. True of one of five. The card
  refutes "roughly four times" by pointing at 1.94× and 2.73×; the same test applied one
  plate along refutes "an order of magnitude" and was not run.
- **So the card's own count is false.** It opens *"Two of the three plates on this screen
  carry the withdrawn claim in their own accessible descriptions"* (screenshot
  `plates-card.png`). Measured, it is three of three — and a reader is left to conclude the
  third plate is clean.
- **In fairness:** round 1 named only two descriptions, and the wave named exactly those
  two, correctly and well — the quoted text, the measurement beside it, and the D13 fence
  given as the reason it stands. **The under-scoping is round 1's.** What makes this P1
  rather than an inherited defect is that the fix introduced an affirmative count of its
  own, on a drawn screen, in the card whose whole purpose is to stop this screen asserting
  and refuting the same proposition — and the measurement that falsifies the count is
  already computed on that screen.
- **Correction:** quote the Schoeller `<desc>` beside the other two and make the count
  three; refute its magnitude clause with the sulfate ratios exactly as the Stiff's is
  refuted. No figure work, no new measurement.

### W21-A-7 · P1 — The fourth instance: the reading that describes the derived table hardcodes what the row beside it derives

- **Reproduction (one mutation, scratch copy, green baseline):** truncate `ARSENIC_MW05` to
  13 values (`.slice(0, 13)` after the existing filter) → **`node build.mjs` exit 0**.
- **Observed.** The table and its prose follow the series correctly (caption *"the 13
  values in the record"*, *"disagree about two of the 13 points"*). The population reading
  that describes that table does not: it still renders **n 14**, span **2023-01 to
  2026-04**, range **3.5 – 26.1 µg/L** and the sentence *"The fourteen values the record
  holds"*, when the record holds thirteen ending 2026-01 at 24.9. Meanwhile the row
  twelve lines above it — *The exported series* — self-corrects to **13**, **2023-01 to
  2026-01**, **3.5 – 24.9**, because its four fields are getters over the same array.
  **Two rows describing the same series, on the same screen, disagreeing, with the build
  green.**
- **Expected.** The screen's own sentence: *"The build checks each row … a marker that has
  gone stops the build rather than leaving a table describing a plate that changed
  underneath it."* The table changed underneath the row and the build did not stop. This is
  the same criticism round 1 made, surviving in a narrower form on the one row the wave
  rewrote.
- **Why it is the fourth instance and not a nit.** The guard now measures where it can
  (`countAt`), the marker carries the number on the `i < 2` row, and two rows are getters
  throughout — four of five readings are protected. The fifth hardcodes four fields that
  are trivially derivable from the array the table is now built from, in the same commit
  whose prose says a table reproducing the transform "would put the truth in two places".
  The truth about this series is currently in two places, and only one of them is wired to
  the series.
- **The numbers are correct today.** The exposure is drift, not a live falsehood — which is
  why it is P1 rather than P0, and it is P1 rather than P2 because it is the unimplemented
  half of round 1's stated correction, on the row the wave touched.
- **Correction:** make `n`, `span`, `range` getters over `ARSENIC_MW05` exactly as *The
  exported series* does, and derive the count word in `says` or drop it. Three lines.

### W21-A-8 · P2 — The new table marks a live value with the class this catalogue reserves for a superseded one

- **Reproduction:** screenshot `fig42-table.png`. In the column headed **"As the plate
  draws it"**, the first two rows render `<span class="mk-was">&lt;5.0 · non-detect · LOR
  5.0 µg/L</span>` — **struck through and muted**.
- `.mk-was` is defined at `chrome.mjs:1200` with a comment stating it is the superseded
  former value ("a photocopy of this table still tells the two apart"), and all eleven other
  use sites in `screens.mjs` are genuine former values (`r.was.code`, `r.was.by`,
  `schemeGap.was.guess`, …).
- **Observed:** the struck value is what the plate draws **now**, in a present-tense column,
  on the screen whose point is that the plate still draws it. The encoding says withdrawn
  where the prose says current, and it is the one row a reader most needs to read as live.
- **Correction:** a neutral emphasis (the muted data face without `line-through`), or a
  class whose documented meaning is "differs from the record".

### W21-A-9 · P3 — The Piper is quoted with a character it does not contain

- **Reproduction:** the card quotes *"separate from the Ca-HCO₃ background of every other
  bore"*; `figures.mjs:769` reads `Ca-HCO3`. Inside quotation marks, presented as what the
  plate "reads", and it does not survive a grep for the quoted string.
- **Correction:** quote `Ca-HCO3` verbatim, or drop the quotation marks and paraphrase.

### W21-A-10 · P3 — The guard's comment describes an opt-out that does not exist

- **Reproduction:** `build.mjs:245` states *"a row that names no array says so with
  `counts: false` and keeps the presence check alone, because a guard that pretends to
  measure what it cannot is worse than one that says what it does."* `grep -rn "counts:
  false" *.mjs` returns **one hit — that comment**. No row carries the field and the code
  never reads it; a row is silently unmeasured by omitting `countAt`, so "forgot to add it"
  and "cannot be counted" are indistinguishable. That is precisely the distinction the
  comment claims the design makes, and W21-A-7 is what it costs.
- **Correction:** implement the field and require one of `countAt` / `counts: false` on
  every row, or delete the sentence.

### W21-A-11 · P3 — The withdrawal record on the reading row renders nowhere

- **Reproduction:** the row gained `was: 'Six typed rows … Withdrawn 3 September 2026.'`;
  `grep` for that string in `index.html` returns **false**. The readings table has no
  column for it and no renderer reads it.
- The primary correction was validly taken by the other branch round 1 offered
  (re-caption for what it is), so nothing on screen is wrong. But the field is dead data
  shaped like the `was` fields that do render elsewhere on this screen.
- **Correction:** render it as the other withdrawals on this screen are rendered, or move
  it into the code comment that already records the same thing.

## Clean, with the evidence that it is clean

- **Hygiene, all of it:** build twice byte-identical at the stated `sha1`; tree clean;
  census five character-identical to both `cfdb910` and `93c6a9a`; zero `state:` edits;
  ledger at 2 with byte-identical blocks; `figures.mjs` confirmed untouched by hash at both
  commits and in the tree — which matters here because two of the five fixes are about that
  file's contents and neither changed it.
- **The import the gate flagged for checking:** exactly one symbol added, nothing removed,
  two use sites, three hunks, no other screen affected.
- **The population-reading arithmetic:** the row now reports `n: 14, censored: 0`, which is
  what the array holds by my own reconstruction, and the build line moved from three answers
  about the censoring to **two** — the reading agrees with the exported series because it is
  the exported series, as claimed.
- **The ionic-strength numbers on the new card:** recomputed from retyped literals —
  narrowest **1.94×** (MW07), widest **3.84×** (MW12), mean ratio **2.73×**, and **no bore
  reaches 4×**, so "four is true of no bore on the plate" is correct. The `lowest`/`highest`
  getters are named for the other bore's strength rather than the ratio, which reads oddly in
  source but produces the right sentence.
- **Guards other than W21-A-7:** four mutations, four clean named failures.
- **Overflow and the walk:** zero document overflow on 77 screens at three widths by my own
  probe as well as `verify.mjs`; the spine walks by real clicks at 1280 and 375 px with no
  overflow at any hop.
- **The audit-ID citation on `#hydrochem`** (`W21-A-4`) is consistent with established
  practice, not a new leak: `#exchange`, `#saved-views` and `#explorer` already cite
  `W10-A-1`, `W19-A-1` and `W20-A-5` on their faces.

## Round 2 verdict

**Wave 21 does not pass the round-2 gate.** The gate is zero P0/P1 and this round records
**two P1** (and no P0).

Blocking:
- **W21-A-6** — the third plate carries the withdrawn claim unnamed, and the card that names
  the other two asserts a count of "two of the three" that the screen's own table refutes.
- **W21-A-7** — the fourth instance of the marker-not-measurement shape: one mutation leaves
  two rows describing the same series disagreeing on screen, with the build green.

What the round actually found, stated plainly: **round 1's P0 is properly dead.** The table
is derived, its fourteen rows match an independent reconstruction cell for cell, its account
of the transform is exact against `figures.mjs`, and the reading that describes it agrees
with the series. `countAt` closes both mutations round 1 demonstrated, and the margin
finding is answered with the right number and the right framing. Three of the five round-1
findings are fully and correctly closed, and `figures.mjs` was not touched to do any of it.

Both surviving P1s are the same defect the wave was already fixing, stopped one instance
short — one plate of three, one row of five. Both corrections are small, need no new
measurement, and touch no fenced file: quote a third `<desc>` and correct a count; turn four
hardcoded fields into the getters the row above them already uses. Round 3 of 3 remains.

— the standing auditor lineage · round 2 · measured at `5bf0eac`

---

# Wave 21 audit — round 3 — commit `4f6e0ad`

Jerry has directed that deep audits run on Codex; this environment has no Codex CLI and no
OpenAI credentials, so this round, like rounds 1 and 2, runs under `EXPANSION_BRIEF.md`
§11's fallback auditor provision ("any capable model session with no prior context — the
mechanics matter more than the model"). Round 3 of at most 3; gate is zero P0/P1. Rounds 1
and 2 are above, unedited. Screenshots are in the auditor's scratch area, not the
repository. No file in this repository was edited other than this one; every mutation below
was applied to a `git archive` copy of `4f6e0ad` outside the tree.

## What was run, and what it returned

| Measurement | Result |
|---|---|
| `node build.mjs` × 2 | exit 0 both times; `index.html` **byte-identical** across both runs and to the committed file, `sha1 a04fe2bf3e4ae016043a6bc368a0d44d8c43a52f` — the hash the gate stated. Tree clean afterwards |
| `CHROMIUM=… node verify.mjs` | exit 0 · **77** screens at 375/1000/1280 · nine families green (13 figures / 250 text nodes · 582 touch targets · **1814** stacked labels · 162 captions × 3 widths) |
| Independent overflow sweep (my own probe, async settle, not `verify.mjs`) | 77 screens × 375/1000/1280 — **worst overflow 0 px at every width** |
| Census five state counts, `cfdb910` / `93c6a9a` / `5bf0eac` / `4f6e0ad` | **character-identical** at all four: not built 15 · engine-only 35 · shipped 8 · proposed 5 · shipped (CLI) 3 |
| `state:` lines in the `screens.mjs` diff | **0** |
| `figures.mjs` byte-identical — by hash, as asked | `sha256 11acfcccea3b7aae…` **identical** at `cfdb910`, `93c6a9a`, `5bf0eac`, `4f6e0ad` and in the working tree. It is not in the diff; neither is `app.css` or `chrome.mjs` |
| Diff extent `5bf0eac..4f6e0ad` | exactly five files: this audit file, `build.mjs`, `index.html`, `screens.mjs`, `seed.mjs` |
| Ledger | **2** `FOUND AND DEFERRED` markers at both commits; extracted blocks hash identically (`73cdcc57dc2f2907`) |
| Guards, mutation-tested (four separate scratch copies) | vendor-brief bullet removed → **bites**, both directions (`§9.4: enumerates 9 items in the brief, the row says 10` and `row with no source for Detection frequency`); §4.2 dimension renamed → **bites**, both directions; a value deleted from `figures.mjs`'s `const detects` → **bites** with the predicted message; `counts: false` deleted → **bites** (below). **One new shape survives — W21-A-12** |
| Walk at 1280 and 375 px | `hydrograph → statistics → hydrochem → analysis → crosstab`, 3 of 4 hops by real click at both widths (`statistics → hydrochem` has no direct edge and was taken by hash, as in round 2), every hop landed on its target, zero overflow at every hop |
| Ion arithmetic, recomputed from literals retyped by eye (`seed.mjs:1404–1409`; `seed.mjs` **not** imported) | see below — all three refutations independently reproduced |

## The four fixes, each re-measured

### W21-A-7 (P1) — **fixed.** Re-run against the file, not the account.

Round 2's exact mutation, applied to a scratch copy: `.slice(0, 13)` on `ARSENIC_MW05`.
`node build.mjs` exit 0, and the reading row now moves with the series — **n 13**, span
**2023-01 to 2026-01**, range **3.5 – 24.9 µg/L**, and the sentence *"The 13 values the
record holds"*. At `4f6e0ad` unmutated it reads 14 / 2023-01 to 2026-04 / 3.5 – 26.1, which
is what the array holds. The row twelve lines above it (*The exported series*) moves to the
same figures, so the two rows that disagreed under round 2's mutation now agree under it.
`n`, `censored`, `span`, `range` and `says` are all getters over `ARSENIC_MW05` in source.

### W21-A-6 (P1) — **half fixed.** The three refutations are right; the count is not measured. See W21-A-12.

All three refutations reproduced independently, from retyped literals:

- **Piper.** Largest cation is not calcium at **4 of the 6** (MW03B, MW05, MW07, MW09), and
  **2** bores hold a majority anion (MW05 SO4 76.71%, MW12 HCO3 54.79%). The card's derived
  sentence renders exactly those two numbers. ✓
- **Stiff.** MW05/others by ionic strength: 1.9373 (MW07), 2.4510, 2.9533, 3.2627, 3.8379
  (MW12) — narrowest **1.94×**, widest **3.84×**, and **no bore reaches 4×**. ✓
- **Schoeller.** MW05 sulfate against the others: **3.4727×** (MW07), 4.8974, 6.1613,
  8.3043, 11.9375 — ten-fold holds against **1 of 5**, and the nearest is 3.47×. ✓ Naming
  the Schoeller did sharpen it as the gate says: *above the rest* is a claim about the
  nearest bore, and against the nearest it is out by a factor of three.

The three quoted `desc` strings each match `figures.mjs` exactly (one occurrence each),
so **W21-A-9 is fixed** — the Piper is now quoted `Ca-HCO3`, the character the file holds.
The count itself is a different matter, below.

### W21-A-8 (P2) — **fixed**, and confirmed by eye.

Screenshot `fig42-table.png`. `mk-was` occurs **0 times** inside the Figure 4.2 table
(6 times elsewhere in the document, all genuine former values); the two rewritten rows carry
`mk-tag mk-tag--warn` instead, and there is **no `line-through` anywhere in the table**. The
cell reads as a present departure, which is what the column asserts.

### W21-A-10 (P3) — **fixed**, and the limb tested.

Deleting the `counts: false` from *The trend record* on a scratch copy stops the build:
exit **1**, `population reading "The trend record": declares neither an array to count nor
counts: false — a reading that cannot be measured has to say so`. Four readings carry the
field, one carries `countAt`, and the guard now refuses a row declaring neither.

### The censoring consequence (gate item 3) — **derived, and no surface is stale.**

`censoredAnswers` is `new Set(readings.map((r) => r.censored)).size`. Every rendered
surface says **2**: the build line, the `#statistics` heading (*"5 readings of one
population, and 2 answers"*), the table caption (*"3 of the 5 carry censoring at all, and
across all 5 there are 2 different answers"*) and the §9.4 coverage note. I found no
surface still saying three. The row's `was` field does explain the drop — but see
W21-A-11 below: it renders nowhere, so the explanation is in source only.

## Findings

### W21-A-12 · P1 — The fifth instance, in this round's own new derivation: the plate record's numbers are measured and its **verdict** is typed, so the count can only go up

- **The gate's own test, run.** *"Check the record would drop a plate from the count if its
  description ever survived the arithmetic."* It does not. `refuted: true` is a literal at
  `seed.mjs:13779`, `:13786` and `:13793`; `grep -n refuted seed.mjs screens.mjs build.mjs`
  shows it is **never computed** for these entries, and `plateClaimCount` is
  `all.filter((p) => p.refuted).length` over three hardcoded `true`s. The number is `3` by
  construction and `total` is `3` by construction.
- **Reproduction (mutation A, scratch copy, green baseline).** Make the Schoeller's claim
  *true*: `MW05.SO4 19.1 → 55.0`, which is exactly 10× the nearest bore (MW07, 5.5).
  `node build.mjs` **exit 0**, and the screen now renders:
  - headline: **"3 of the 3 plates on this screen carry the withdrawn claim"** — unchanged;
  - Schoeller row: *"the sulfate limb is **10.00×** the next bore — an order of magnitude
    above* the rest *requires the nearest, not the furthest"* — the arithmetic now
    **satisfies** the description while the sentence still presents itself as its refutation;
  - Stiff row: *"the closest is **3.66×** and the widest **7.24×**, mean 5.16× — **four is
    true of none of them**"* — the verdict clause is typed text, and 3.66× is squarely
    "roughly four times", so that sentence refutes itself too.
- **Affected acceptance criterion.** The card's own three claims, all on a drawn screen
  (screenshot `plates-card.png`): *"The count is read off the record below rather than
  written down"*; the table caption *"the arithmetic from the table above that **decides**
  it"*; and the closing *"Named rather than corrected, and **counted rather than
  asserted**."* The arithmetic does not decide it and the count is not counted — a typed
  boolean decides it, one level below where round 2 found the typed word "two".
- **A second limb, by inspection.** `total` is `all.length` — the record's own length, not
  a count of plates on the screen. `#hydrochem` holds three `svg.mk-fig` today (verified in
  a driven browser, descriptions matching one-for-one), so "the 3 plates on this screen" is
  true today by coincidence of two independent threes. A fourth plate on that screen would
  not move it.
- **In fairness, and it is real.** The `measured` strings *are* derived, the three
  refutations *are* correct today (I reproduced all three), the seed comment is candid that
  a surviving plate "would simply carry `refuted: false`", and this is a genuine advance on
  round 2's typed "two of the three". The defect is that the honest half is in a source
  comment and the overclaim is on the screen.
- **Why P1 and not P2.** Identical structure to W21-A-7, which round 2 graded P1 on exactly
  this reasoning: correct today, drift the exposure, and the unimplemented half of the
  round's own stated correction, on the row the wave touched. Grading it lower here would
  make the lineage inconsistent between two rounds of one wave.
- **Why P1 and not P0.** Nothing on the screen is false today; all three plates are
  genuinely refuted by my own arithmetic.
- **Correction.** Derive `refuted` from the arithmetic rather than declaring it — each
  entry already computes the numbers that decide it (`sulfateNearest < 10`,
  `st.highest.times < 4 && st.lowest.times < 4`, `m.notCalcium.length > 0`), so the verdict
  is one comparison per entry — and move the typed verdict clauses ("four is true of none
  of them", "requires the nearest, not the furthest") behind the same test. Take `total`
  from the plates on the screen, or say "of the 3 in this record".

### W21-A-13 · P2 — The record quotes three plate descriptions as verbatim and nothing checks them against `figures.mjs`

- **Reproduction (mutation B).** Replace the Stiff's `desc` with a claim that is true and
  was never withdrawn — *"MW05 has the highest ionic strength of any bore on the plate"* —
  and `node build.mjs` is **exit 0**. The card then renders that substituted sentence inside
  quotation marks as the plate's own description, under a headline still reading "3 of the
  3 plates … carry the withdrawn claim". `grep` confirms no guard in `build.mjs` reads
  `desc` or compares it with `figures.mjs`.
- **This already cost one defect.** W21-A-9 (round 2) was precisely a quotation that did not
  survive a grep of the file it claimed to quote. The character was fixed; the check that
  would have caught it was not added.
- **It is a live risk, not a hypothetical.** `figures.mjs` is fenced until the print test
  (D13) — that is, it is *scheduled to change*, and an approved plate's description is part
  of what that test is for. When it changes, this record will silently describe plates that
  no longer say these things.
- **Correction.** Assert each `desc` appears verbatim in `figures.mjs`, the way the
  population readings assert their markers. Five lines in the guard that already reads that
  file.

### W21-A-14 · P2 — `#statistics` states a typed count of the series in the same sentence as a derived one

- **Reproduction (mutation C, the truncation).** With `ARSENIC_MW05` cut to 13, that screen
  renders, in one sentence: *"**13** quarterly values of Arsenic (filtered) · MW05 ·
  2023-01 to 2026-01 · … A different population from the grid's in every dimension: one
  analyte, one bore, **fourteen rounds**."* Build exit 0.
- `seed.mjs:13148` — the `says` field beside the derived `get line()` — types "fourteen
  rounds" where the clause before it derives the same quantity.
- **Pre-existing**, landed in wave 20 (`25ae33c`), so not introduced by this round; correct
  today, since the series holds 14. P2 rather than P1 for both reasons.
- **Correction.** `${ARSENIC_MW05.length} rounds`, or drop the clause — the derived half of
  the sentence already says it.

### W21-A-11 · P3 — **survives, and now carries load-bearing content.**

Round 2 recorded that the reading row's `was` field renders nowhere. It still does not:
`grep` for *"which is why these five readings now give two answers"* and for *"one of the
three was this table"* in `index.html` both return **0**. This round moved the explanation
of a number that visibly changed — three answers to two — into that dead field. The gate
asked me to confirm the field "explains the drop rather than leaving a reader to reconcile
the commit history with the screen": it explains it **in source**, and a reader of the
screen still has nothing. No on-screen text anywhere explains the drop.

- **Correction.** Render it, as the other `was` fields on that screen are rendered.

### W21-A-5 · P3 — **survives unchanged.**

The sentence still reads *"appeared once in this repository before this wave"*. Measured at
the baseline `cfdb910` across tracked source: `seed.mjs` 1, `VENDOR_REQUIREMENTS.md` 1 —
**two**. Unchanged from round 2's measurement and correction.

### W21-A-15 · P3 — A seed comment still states the superseded count

`seed.mjs:13137–13139`, present tense: *"five readings of arsenic at MW05 exist in this
catalogue and they give three different answers about the censoring."* Every rendered
surface was moved to two; this comment was not. It is a comment, not a drawn screen, which
is the whole of why it is P3 — but it is the sentence a maintainer reads before touching
that block.

### W21-A-16 · P3 — "mean 2.73×" sits between two extremes whose mean is 2.89×

The Stiff row reads *"the closest is 1.94× and the widest 3.84×, mean 2.73×"*. `meanRatio`
is `impacted.strength / meanOther` — MW05 against the **mean strength of the other five** —
which is 2.73. The mean of the five ratios quoted on either side of it is **2.89**. The two
other surfaces that use this number say which it is (*"Against the mean of the other 5
bores"*, *"× the mean of the other 5"*); only this new compressed string does not, and its
placement between the extremes invites the wrong reading.

## Clean, with the evidence that it is clean

- **Hygiene, all of it:** build twice byte-identical at the stated `sha1`; tree clean;
  census five character-identical across all four commits; zero `state:` edits; ledger at 2
  with byte-identical blocks; **`figures.mjs` `sha256`-identical at every commit in the
  wave and in the tree**, which is the check that matters most here given four findings
  across three rounds have been about that file's contents.
- **Diff extent:** five files, none of them fenced.
- **Guards other than W21-A-12:** four mutations, four clean named failures, including both
  limbs of the vendor and dimension guards and the `counts: false` limb added this round.
- **Overflow and the walk:** zero document overflow on 77 screens at three widths by my own
  probe as well as `verify.mjs`; the spine walks at 1280 and 375 px with every hop landing
  and no overflow.
- **The censoring drop:** derived through a `Set().size`, and consistent on all four
  rendered surfaces.
- **The three refutations:** reproduced independently from retyped literals; all three
  correct, and the Schoeller's is the sharpest of them.
- **`#hydrograph`'s "Two sentences withdrawn":** typed, but the body immediately beneath it
  renders exactly those two sentences by reference, so the count cannot drift from what the
  reader sees. Not raised.

## Round 3 verdict

**Wave 21 does not pass the round-3 gate.** The gate is zero P0/P1 and this round records
**one P1** (and no P0), so the wave closes at the three-round cap unpassed, as wave 10 did.

Blocking:
- **W21-A-12** — the plate record's arithmetic is derived and its verdict is typed, so the
  count on the face can only go up. One mutation makes a plate's description true and leaves
  the screen saying "3 of the 3 … carry the withdrawn claim", under a caption claiming the
  arithmetic decides it and a closing line claiming it is counted rather than asserted.

What the round found, stated plainly, because it is mostly good: **both round-2 P1s are
answered on their own terms.** The reading row derives every field and follows the series
under the truncation mutation I re-ran against the file; the third plate is named, and all
three refutations are correct — I reproduced each from literals I retyped rather than
imported, and naming the Schoeller did sharpen it, 3.47× against a claim of ten. The P2 and
both P3s the gate listed are properly fixed, the `counts: false` limb bites, and none of the
five regression surfaces moved. The wave's instinct was right every time; what it did not
do, on the one derivation it added this round, was let the arithmetic reach the verdict.

The gate's own suspicion was the correct one and it was pointed at the right file: *"assume
there is a fifth."* There was, in `plateClaims`, three lines from where it was predicted.

— the standing auditor lineage · round 3 · measured at `4f6e0ad`
