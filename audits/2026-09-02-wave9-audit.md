# Wave 9 audit — round 1 — commit `5d677a9`

Signed: **the standing auditor lineage.** This round runs under `EXPANSION_BRIEF.md`
§11's fallback-auditor provision — Jerry directs that deep audits run on Codex; this
environment has no Codex CLI and no OpenAI credentials, so a capable model session with
no prior implementation context ran the mechanics instead.

Audited against: the "Wave 9 — Soil, sediment and composites (FR-1.7)" section of
`audits/2026-09-01-wave-plan.md`, plus the practitioner review's keep-list. Every number
below was recounted from raw literals in `seed.mjs` (read, not imported) or measured in a
driven Chromium via playwright-core; the implementer's `recount.mjs`/`walk.mjs` were not
used. Screenshots live in the auditor's scratch area, referenced by filename.

## Commands run, and what they returned

| Command / probe | Outcome |
|---|---|
| `node build.mjs` × 2 | exit 0 both; `index.html` sha1 **`de1858c5…`** on both runs — byte-identical, tree clean. 71 screens · 1604 anchors · link graph clean · partition passes with `composite` under Sampling events |
| Census identity | pre-wave (`699f57d`, rebuilt in a worktree): `not built: 15 · engine-only: 35 · shipped: 8 · proposed: 5 · shipped (CLI): 3 · 4 drawn since`. This commit: the same five counts **character-identical**, `5 drawn since`; 1 Sep line `proposed 9→10`, shipped unchanged |
| `CHROMIUM=… node verify.mjs` | exit 0. Nine families green: overflow ×3 widths, ids, names, headings, matrix panning, 13 figures/248 texts, 527 touch targets, 1504 stacked labels, 98 captions ×3 |
| Independent overflow sweep (own script) | all 71 screens × 375/1000/1280 via `data-active`: worst document overflow **0 px** at every width |
| Register hygiene | `git diff 699f57d 5d677a9 -- screens.mjs` touches **zero `state:` lines**; `composite` entry is `now: 'proposed', added: '2026-09-02'` with no `state:` field; `build.mjs` diff is one section list + a dated comment; `app.css` and `audits/` absent from the diff |
| Click walks | rail → `#composite` lands; events→composite→ecoc→receipt→events, composite↔lineage, composite→crosstab/locations — every hop by a real click, every landing correct |
| Screenshots inspected | `w9-1280-composite-top.png`, `w9-el375-composite.png`, `w9-el1280-composite.png`, `w9-el1280-events.png` — real dense content, no blanks, no broken layout |

## The recount, from raw literals

- **Masses.** C01: 5 increments × 500 g = 2,500 g in; 1,000 g submitted; 1,500 g returned
  to the TP04 spoil — 2500−1000=1500 ✓. C02: 4 × 1,000 g = 4,000 g in; 4 × 250 g = 1,000 g
  composited; 4 × 750 g = 3,000 g retained ✓. All render on `#composite`.
- **Openness bounds** (rendered, with their derivations spelled out): sulfate 1180 →
  ceiling **4,720** (“4 × the composite, if every other increment were zero”), floor **80**
  (“4 × the 20 mg/kg reporting limit”); arsenic 9.1 → 36.4 / 8; zinc 49 → 196 / 20. Every
  one equals value×n and LOR×n for n=4. **pH and EC are absent from the table with the
  conservation reason stated** — the `conserved` flag gates exactly those two.
- **Depth.** 18 intervals across 5 pits (4+4+4+3+3), each pit abutting from 0.00 with no
  gaps, each refusal depth equal to its last interval's base. C02's envelope **0.30–1.10 m**
  is the min/max of four *differing* second horizons (0.90 / 1.00 / 0.80 / 1.10), and the
  screen calls it an envelope, not an exact interval.
- **The manifest.** 13 samples = 11 primary + 2 QC (blind duplicate S03, rinsate QC1);
  **23 containers** (day-1 transfer 12 + day-2 transfer 11; C02 contributes 0 — it did not
  exist when the cooler was sealed — and the chain reconciles 23 of 23); **45 results** =
  9 analysed samples × 5 analytes; increments carry 0. All four counts render on `#events`,
  which answers **“Evaluated against: Nothing — no criteria set in the library carries a
  soil or sediment matrix.”**
- **“2 of 9.”** 9 increment positions (5+4). The positions with no number of their own are
  exactly `{Grid node G3, TP04}` — G3 is deliberately off the register, and TP04's best
  non-refusal field screen (330 µS/cm) sits below the plan's 500 threshold, so no discrete
  was submitted. The screen's wording — “2 of the positions sampled have no number of their
  own at all: Grid node G3 and TP04” — matches the derivation (`representedOnly`), and
  “9 positions covered by 2 analyses … 7 were not run” matches 9−2.
- **Duplicate RPDs**, recomputed from the S02/S03 pairs: EC 3.8%, sulfate 4.4%, arsenic
  3.7%, zinc 7.1%; pH is an absolute Δ 0.1, not ratioed — with the wave's dated
  water-limit-on-soil deferral stated beside it.
- **Holding clocks against `AS_AT = 2026-05-24`**: extract suite expires 2026-06-03 =
  **10 days** left (the screen's “10 days left to buy the resolution back”); metals expire
  2026-11-02 = **162 days**. Both render.
- **Background ratios**: 5 rows (the four discretes-plus-composites minus the TP05
  control), sediment D01 excluded *with the no-sediment-background reason*, pH excluded
  with the logarithm reason, and the C02 sulfate ratio renders as the computed 25.7× —
  drawn as “interpretation, not an outcome”, with no mark on any grid.

## The grids, and the separation the wave promised

Measured on the rendered document: the groundwater crosstab is **unchanged** — 11 analytes
× 7 locations, 66 numbered cells, 11 dry cells (MW11's column, each speaking “dry — no
sample exists…”), and **zero soil identifiers** in its header row. The soil grid is its own
table: 11 sample columns (samples, not locations — two of them belong to four places each),
5 analytes, **20 ◇ composited cells** (4 increments × 5), each speaking “no result of its
own … combined before analysis … not a result below a limit of reporting and it is not a
pass.” The matrix statement beside it counts 5 sets / 0 soil off the library.

## Traceability, both directions, by clicks

Forward: the four increment rows on `#events` each link to `#composite` (4/4). Backward:
the four fan-in nodes on `#lineage` (`C02-I1…I4`, each with its pit and interval) each link
to `#composite` (4/4), under the cap “Four increments, one bag — the fan-in a line cannot
draw”, ending in *cannot say* with the price and the 2026-06-03 deadline. The per-increment
table on `#composite` names what resolving each one would take. The honest limit is the
first section's costs panel — “A result on a composite **cannot be attributed to one
increment**” — with the stat tiles above it already carrying “2 positions with no number of
their own” and “10 days left”.

## Deferrals, the freeze, and vocabulary — judged

- **Depth-profile plate:** not drawn. `figures.mjs` is absent from the wave's diff; the
  figure count is unchanged at 13; the grammar proposal is recorded on `#composite` and as
  a dated `FOUND AND DEFERRED` comment, G-76b caveat attached. The freeze held.
- **DQO matrix gap** and **criteria matrix column**: both dated in-repo comments *and*
  visible on `#composite` (“written for water … stricter rather than looser”; “the
  library's matrix column is not holding matrix words”). The stricter-so-safe reasoning is
  sound — a tighter limit cannot pass what a soil limit would fail — and `#criteria` does
  show `Freshwater`/`Groundwater` in the matrix column, so “0 soil sets” is right by
  inspection exactly as stated.
- **No soil import run:** verified — `#imports` has zero soil mentions and no
  `2026-M05-SOIL` run, its three literal tiles are unchanged, so the wave-8 “contributes 0
  to all three … nothing here is wrong today” statement remains true; the composite chain
  states its own omission of the import hops with the not-repeating-the-PFAS-chain reason.
- **Vocabulary:** composite sample / constituent increment / depth interval are FR-1.7's
  own words; **“compositing scheme” minted with the recorded reason** — `rule` is the
  derivation engine's word and a composite is not derived — which this auditor judges
  correct: the minting *protects* the keep-list's derived-vs-composite distinction rather
  than widening the vocabulary. Test pit appears as the advanced method (`Excavator, 600 mm
  mud bucket`) on class-`soil` locations, not as a class. Vapour is declared absent with
  the no-programme reason. TP rows carry ground-surface elevation + datum and logged
  intervals, no toc/screen.

## Keep-list — all eight, spot-measured

dry ≠ missing (MW11: “visited 2026-05-14 — dry” on `#locations`, 11 speaking dry cells on
the crosstab) · capture ≠ sync (both words live on `#field-capture`) · indeterminate
first-class (untouched by this wave; ◇ composited is a *new, distinct* non-state whose
spoken text explicitly disclaims being a non-detect or a pass) · QC failure says what it
did (`#qc` biased-low/qualified wording intact) · anomaly ≠ exceedance (background ratios
drawn as interpretation; the soil grid's `evaluated` count is derived as 0; no exceedance
mark anywhere on soil) · derived results keep components and rule (the “A composite is not
a derived value” panel draws the distinction; PFHxS 1.7 J intact on `#lineage` and
`#criteria`, MDL 0.5 · LOR 2.0 on `#dictionary`) · authored prose never silently rewritten
and reported-then vs record-now (the EVENT_SAMPLES matrix correction keeps the was-state —
eight `Groundwater`, three blank-type words — in a dated comment, and `#events` shows the
1 September sentence quoted with its dated scope-correction rather than rewritten).

## Findings

### W9-A-1 · P3 — The naming comment claims a MOCK- prefix the work order does not carry

- **Evidence:** `seed.mjs` (the SOIL block's naming comment, ~line 5741): “the custody
  form and the laboratory's work order below **both** carry it [the MOCK- prefix]”. The
  custody form does (`MOCK-COC-2026M5-009`, renders 4×); the work order is
  `PAS-WO-268503` and renders 3× with no prefix — consistent with the pre-existing
  convention (`PAS-WO-268841` likewise), so the defect is in the comment's description,
  not the convention.
- **Reproduction:** `grep -o "MOCK-[A-Z-]*268503\|PAS-WO-268503" index.html | sort | uniq -c`
- **Correction:** fix the comment's parenthetical (or prefix the work order, which would
  then diverge from the groundwater seed's own practice).

### W9-A-2 · P3 — Three thousands-grouping styles on one screen

- **Evidence:** `#composite` renders “2,500 g” and “4,720 mg/kg” (comma-grouped) beside
  “1 000 g” / “1 500 g” (space-grouped, inside scheme/surplus prose copied verbatim from
  seed strings) and “4 × 1000 g” (ungrouped). Captured in the DOM line dump
  (`w9-dom3.mjs` output).
- **Correction:** one formatter for the screen's numbers, or normalise the seed prose.

No P0, P1 or P2 findings. Categories checked and clean are stated as such above with the
measurement that makes them so.

## Verdict

**Wave 9 round 1 passes the gate: zero P0/P1.** The census is character-identical with
drawn-since legitimately at 5; the build is byte-identical twice at `de1858c5…`; every
seed-derived number on the drawn screens recounts from raw literals; the composite's chain
walks both directions by real clicks with the limit stated first and in units; the
groundwater grid is untouched by the soil work; the three deferrals meet the dated-comment-
plus-visible-on-screen standard; the figure-family freeze held; and the keep-list survives
intact. Two P3 nits (W9-A-1, W9-A-2) are recorded for the response note and do not hold
the wave.
