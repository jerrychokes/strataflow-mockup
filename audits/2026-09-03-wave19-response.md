# Wave 19 — response to the audit, round 1

**The gate passed on the first round — zero P0, zero P1.** The round measured
rather than accepted: the two-hop live dimension was proved **under mutation**
(three analytes added to the in-force Table 4 set moved a dataset's definition
from `(12)` to `(15)` with nothing touching either, while its citations stayed
byte-identical), every exclusion figure recounted exactly off the raw arrays, all
21 pre-existing chain hops were diffed byte-identical with one appended, and all
five of the wave's corrections to my plan were checked — including that `purpose`
appears zero times in the baseline page. All three findings are accepted and fixed
or recorded in this closing commit.

| Finding | Severity | Response |
|---|---|---|
| W19-A-1 — the wave's own citation check has a latent false positive: `agrees` folded two grounds into one, so a plate that draws water levels, naming no analyte by nature, could never agree however its locations read. Correcting the drawn defect left the count at 1 | P2 | **Accepted and fixed by separating the grounds.** The location test is the disagreement; the analyte test is a question that kind of item **cannot be asked**, and it is now reported beside the finding and not counted against it. The auditor's own experiment is the proof: correcting Figure 4.1's title to name exactly what the dataset selects now takes `disagreeing` **1 → 0**, where before the fix it stayed at 1. The real finding is undisturbed — as the record stands, `disagreeing` is still 1 and `analyteUncheckable` is 1 of 2. A check that cannot be satisfied is worse than one that is failing, because it looks like a defect and cannot be cleared by fixing one. |
| W19-A-2 — **my premise for the eighth line of attack was wrong in both particulars.** `#saved-views` measures 9,582 px, not the 9,843 I gave, and it is **fifth tallest**, not the longest — `#coverage` is 30,650, and `#exchange`, `#lineage` and `#composite` all exceed it | P3 | **Accepted; it corrects me, not the wave.** I asserted two figures to an auditor without measuring either, in a brief whose whole purpose is measurement — and the repository makes no such claim anywhere, so there was nothing to mislead me but my own estimate. The judgement I asked for came back favourable: 3.9× the median sits inside an established band and the twenty panels group into four coherent runs. The one observation worth carrying is that panels 16–20 are the history of this wave's own settlement rather than the dataset object, so if the screen is ever split that is the seam — recorded here rather than acted on, because splitting it adds a screen to the register and no auditor recommended it. |
| W19-A-3 — `staleByDefinition: version !== 1` cannot fire while every dataset is at v1, so that limb of the staleness criterion is argued rather than exercised | P3 | **Accepted, and the limit moves from a code comment onto the face.** It cannot be *exercised* without a version history no recorded act supports, and manufacturing one to make a channel light up is the trade this catalogue has refused six times. What was wrong was where the limit was written: a reader of the screen saw a channel reading zero and a reader of the source saw why. The channel now says on the face that it has never fired, that it is argued rather than demonstrated, and that demonstrating it needs a history the record does not hold. The other limb is real and the auditor exercised it — superseding a cited item moves *by evidence* from 0 to 1. |

**State at close:** build exit 0 and rebuild byte-identical
(`0555a528fbc48676bba427472cb1efe0e8a71d2e` — the hash moved from the audited
`15a2501d…` because both fixes render); verify exit 0 across all nine families;
the 23 Aug census five state counts character-identical; `vendor brief: 77 rows
closed` still printing; the deferral ledger at two with both blocks
byte-identical; `figures.mjs` untouched, so D13's print-test gate holds. Wave 19 —
the governed dataset, the live/frozen seam drawn as an act with a record, the four
absences counted by kind, the lineage hop, and the citation checked at both ends —
is closed under §11 on the auditor's round 1 against `0e8a694` plus these fixes.

**A process note, answered.** The auditor asked whether it was right to treat three
replayed backlog items (wave 1 round 2, wave 6, wave 12) as already discharged
rather than re-run them. It was: all three carry both an audit and a response file
and closed at least a round each. That queue is an artefact of how the lineage is
resumed and not a request.

**What the wave leaves for the next one.** Nothing deferred; the ledger is unchanged
at two, both decision-gated. Wave 20 is the explorer over this dataset, which D1
also unblocked, and D11 has already settled that `#crosstab` becomes one
representation of a population rather than a screen with its own filter bar.
