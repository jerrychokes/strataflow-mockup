# Wave 2 audit — 1 September 2026

**Auditor:** the same independent fresh-context session that audited Wave 1 (not Codex —
unavailable; brief §11's mechanics were followed instead). Audited at commit `79c9dfe`
("Wave 2: the chain of custody, and three findings drawn deciding something") against the
Wave 2 section of `audits/2026-09-01-wave-plan.md`. The implementer's report was treated
as a set of claims; every claim below was re-measured, several with the auditor's own
round-1 probes unchanged. Round-2 wave-2 evidence screenshots are `w2-*.png` in the
auditor's scratch area (kept out of the repo).

## Wave 2 — round 1

### Commands run, and what they returned

| Command | Outcome |
|---|---|
| `node build.mjs` | exit 0 · **67 screens** · both censuses honest about the new screen: "23 Aug … · 1 drawn since" and "1 added in pass 4"; link graph clean; working tree clean after rebuild (byte-identical) |
| `CHROMIUM=… node verify.mjs` | exit 0 · 67 screens × 3 widths · figures 11/194 nodes · **touch: 457 controls, both axes, floor 44px** · stacked labels 974 cells |
| Auditor's independent sweep (round-1 script, unchanged) | exit 0 — zero document overflow on all 67 screens at 375/1000/1280 by both activation paths; crosstab pans 630 px with the row header pinned at 0 px movement |
| Register region diff (`75168c3 → 79c9dfe`) | exactly one added entry — `{ id: 'ecoc', label: 'Chain of custody', now: 'proposed', added: '2026-09-01' }`, **no `state:`** — plus `RELATED` edges; **no existing screen's `state:`/`now:` changed** |
| `git diff 75168c3 79c9dfe -- figures.mjs` | **0 lines** — figures untouched |
| Driven-browser extraction of coverage, ecoc, receipt, events, exceedances, qc, imports, project-home, obligations, licence | detailed below |
| Independent recomputation of the outlier statistic and the window runs | detailed below |
| Adversarial guard test (scratch copy) | deleting the wave-2 coarse-pointer block from `chrome.mjs` makes `verify.mjs` fail with **exactly 18** `coarse-pointer target under 44px` flags — the number the response claimed — including the width-thin cases only a two-axis walk can see (42×45 ghost button; 16×45 hidden-label checkboxes) |

### The new screen, measured (claim 1)

Clean, with evidence:

- **Register honesty about the missing `state`:** the rail dot hovers
  `proposed — added 1 Sep 2026` (not a fabricated 23-Aug state, and not
  `was "undefined"` — `build.mjs` handles the absent field explicitly); the 23-Aug census
  line counts it under "1 drawn since" rather than under any state; the banner's
  "58 of these 67" is now **computed from the register** (`ROUTE_BACKED`), not typed.
  A distinct `NEW·4` badge with its own legend row separates it from the pass-3 `NEW`s.
- **Wiring:** `RELATED` carries `ecoc → [receipt, events, field-capture, imports]` and
  inbound edges from `events`, `receipt`, `field-capture` (matches the plan's
  entry/onward). Filed under Sampling events in the partition (build-enforced); the
  rendered crumb reads Projects / MOCK-WDL / Sampling events / Chain of custody.
- **Route line:** `a proposal — not in the product` — passes the build's guard (build
  exit 0), and the body's "there is no route for one" is the proposal *agreeing* with the
  register, which the guard's present-tense-only banned forms rightly permit.
- **The proposal argues for itself on the page** (§5.10): the notice names FR-1.6/FR-3.8
  as the two ends and the gap between them as the case, and gives the naming argument.
- **Glossary (QB-9):** verified against the app repo — `docs/GLOSSARY.md` has a
  **Custody transfer** entry (`CustodyTransferId`) and **zero** matches for "eCOC"; the
  screen is labelled "Chain of custody" and names COC/eCOC once as practitioner words.

### Acceptance criteria in the rendered #coverage (claim 2)

Verified in the driven browser, from the DOM:

- **Journey 1: `covered`, 15 steps, zero unowned.** Every step in the walk cell is a
  link; extracted targets: Programme→#programme, Round→#events, Sampling event→#events,
  **eCOC→#ecoc**, Certificate→#certificate, Import batch→#imports, Parse→#imports,
  Map→#import-review, Validate→#import-review, Exceptions→#quarantine, Partial
  accept→#import-commit, QA/QC→#qc, DQA→#dqa, Approval→#validation, Results→#crosstab.
  Steps without a link: **none**.
- **Journey 5:** `partially` (as planned), with **eCOC→#ecoc and Custody
  transfer→#ecoc** in the walk; the note names the two remaining holes as proposals
  (field capture, work-queue assignment per findings §7).
- **FR-5.2:** `covered` · owner `exceedances` · note names condition 12(c), four rounds,
  four series, three distinct outcomes. **FR-4.5:** `covered` · owner `qc` · note names
  median/MAD/score over the bore's own record.

### Cross-screen consistency by construction (claim 3)

- **Receipt derives from the chain:** `seed.mjs` builds `CUSTODY` as
  `CUSTODY_CHAIN.transfers.filter(t => t.onReceipt)` — the four lab-end hops are the same
  rows, not a copy. Rendered agreement checked token-by-token: all four hop times
  (14 May 16:20/16:40, 15 May 09:12/09:40), both parties, PF-2026-118420, and seals
  4471/4472 appear identically on #ecoc and #receipt.
- **The 38:** `EVENT_SAMPLES` container fields sum to 38 (auditor's own reduce), equal to
  `CUSTODY_CHAIN.containers`; the auditor's independent day-by-day recount reconciles the
  chain's arithmetic (10 + 20 collected, 30 withdrawn + 8 = 38 dispatched). Rendered
  once each on #ecoc ("38 · seals 4471, 4472", per-hop rows, the covered-samples total),
  #receipt ("Containers received against the chain of custody — 38 of 38") and #events
  ("38 containers across 11 samples — the same 38 the chain of custody sealed … (38 of 38)").
- **The window runs, recounted independently:** from the seeded values —
  MW07 zinc 2.6/3.1/4.8/9.4 vs 8.0 → trailing run 1 (*not triggered*); MW05 zinc
  6.9/7.4/18.2/31.6 → run 2 (*one round short*); MW05 copper 2.1/3.0/4.7/6.4 vs 1.4 →
  run 4, third consecutive at 2026-Q1-GW (*triggered*, matching the lodged obligation's
  "tripped at 2026-Q1-GW"); MW05 EC takes its four values from `EC_MW05`'s tail
  (2180/2330/2710/3410 vs 2500) → run 2 (*one round short*). All four series and all
  three outcomes render on #exceedances with those exact numbers.
- **Counts re-derived, spot-checked:** #obligations stat tiles 2 overdue · 1 at risk ·
  5 on track · 2 met — exactly the auditor's recount of the 10 seeded obligation states;
  #project-home "9 open exceedances" = 9 seeded exceedance rows, "1 round overdue" = the
  single overdue monitoring round (MW11); #licence's 12(c) row matches the condition the
  panel evaluates.

### The four enhancements (claim 4)

- **events:** the manifest carries the full QC taxonomy — field duplicate (blind, with
  parent), field blank, trip blank, **equipment blank (rinsate, named to the bladder pump
  between samples)** — plus the reconciliation sentence tying the manifest sum to the
  custody form, and a link to #ecoc. "Composite summary" is present as a reasoned n/a
  ("No composites… FR-1.7 … staged S8"), the honest form for a groundwater-only seed.
- **exceedances:** the 12(c) panel **decides** — four series, three distinct outcomes,
  the frozen rule version (`consecutive-window v2 · bound to Licence L8842/2019/1
  v2024.2`), the eight quiet series stated as not drawn with the reason, and the
  TARP-window contrast beside it.
- **qc:** the history outlier shows its working, and the working is right — the auditor
  recomputed Iglewicz–Hoaglin from the seeded 11-round record: median 2120, MAD 140,
  modified z = 0.6745 × 1290 ÷ 140 = **6.2** against the 3.5 threshold, ratio 1.61× —
  every rendered figure matches the independent recomputation.
- **imports:** the rows-read statement stands as it was and is right — "42 rows read,
  0 unreadable. The count comes first, before anything else happens", agreeing with the
  register row for IMP-0239.

### Tooling carry-overs (claim 5)

- **W1-A-10 closed as recommended:** the hit walk now runs both axes, reports them
  separately ("across×down"), and the bar is 44, not 43. Viewer chrome is in scope
  (`.mk-rail` at scroll 0, `.mk-chrome-head` pinned into the canvas — the pinning
  rationale is documented in the check), raising the measured set from 372 to 457
  controls.
- **The guard bites:** deleting the wave-2 `@media (pointer: coarse)` block from
  `chrome.mjs` in a scratch copy reproduces **exactly 18 flags**, including the two kinds
  the old vertical-only walk could not see (the 42px-wide ghost icon button; the
  16px-wide visually-hidden-label checkboxes on #entitlement).

### The new screen, looked at (claim 7)

`w2-375-ecoc.png`, `w2-1280-ecoc.png`: real, dense, coherent content at both widths — no
blanks, no broken layout. The photocopier rule holds on the transfers table: signed vs
exception differ by **glyph shape and word** (● signed / ■ exception), not colour alone.
The review-item pattern is at full strength (both entries side by side and unchosen, a
**Refused** option stated, three ways out each naming what it writes — QB-7/PP4). The
blast radius states irreversibility *before* the act ("a transfer cannot be edited or
deleted; a correction supersedes"). The empty state is differentiated and derived ("There
are 5 rounds … and 4 of them have one"), with a raise action — not a "No data".

### Findings

#### W2-A-1 · P3 — Receipt reads all-clear on the seals while a custody question about one of them is open

- **Evidence:** driven extraction of #receipt: its facts row says "Custody seals intact ·
  2 of 2 intact", and its hop-6 exception text names only the cracked container — the
  seal-number discrepancy (4472 field vs 4427 laboratory, raised *by PAS sample receipt*
  per the seed) is invisible on the screen where it was raised, reachable only by
  following the link to #ecoc.
- **Affected criterion:** cross-screen consistency in spirit; no acceptance criterion is
  breached — #ecoc's ownership panel explicitly argues the discrepancy is a custody
  question and receipt's boundary is drawn on both screens, which is why this is P3 and
  not P2.
- **Expected:** a reader of receipt learns an open question exists about the seals it
  declares intact. **Observed:** intact-only. **Recommended correction:** one line on
  receipt's seal row — "intact; one seal number queried — open on the chain of custody" —
  linking to #ecoc.

### Clean categories, stated as such

- **Register and history discipline:** clean — one new entry, no `state:` fabricated, no
  existing `state:`/`now:` touched, figures untouched, rebuild byte-identical.
- **Overflow, ids, names, headings, panning:** clean — repo sweep and independent sweep
  both green at 67 screens.
- **Acceptance rows:** clean — J1 covered/15 steps/zero unowned; J5's custody steps land
  on #ecoc; FR-5.2 and FR-4.5 covered with the claimed owners.
- **Consistency by construction:** clean — every derived number re-derived independently
  (containers, window runs, outlier statistics, obligation states) and every rendered
  value matched.
- **Guards:** clean — the 18-flag reproduction confirms the touch floors are held by the
  check, not by luck.

### Verdict — Wave 2, round 1

**Wave 2 meets its acceptance criteria.** Zero P0, zero P1, zero P2; one P3 (W2-A-1),
which does not block under §11's zero-P0/P1 rule. The journey the wave advanced walks
end-to-end with every step owned; the numbers the wave put on four screens survive
independent recomputation; the tooling debt from Wave 1 is closed and demonstrated to
bite. **The Wave 2 gate passes.**

**Count:** 0 × P0 · 0 × P1 · 0 × P2 · 1 × P3.
