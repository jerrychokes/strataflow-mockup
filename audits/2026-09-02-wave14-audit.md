# Wave 14 audit — round 1 — commit `d20733b`

Jerry directed that deep audits run on Codex; this environment has no Codex CLI and no
OpenAI credentials, so this round runs under `EXPANSION_BRIEF.md` §11's fallback auditor
provision ("any capable model session with no prior context — the mechanics matter more
than the model").

Signed: **the standing auditor lineage.** This round is identified by its round number and
the commit it measures; session-continuity claims in either direction are out of scope.

Audited against: the "Wave 14 — The WATER_LEVELS baseline, re-anchored" section of
`audits/2026-09-01-wave-plan.md`, plus the keep-list. Every claim below was re-measured
with the auditor's own probes — the reconciliation and shape checks from the raw literals
and against a worktree build of `a8cba70` (wave 13's close), the trigger census with its
control re-run over every string field of the six registers (broader than the
implementation's own field list), the plane re-fit with the auditor's own least-squares
(covariance form, not the repo's elimination), rendered checks in a driven Chromium via
playwright-core (never the CLI `--screenshot`). The implementation's `recount.mjs` /
`walk.mjs` were not used. Screenshots live in the auditor's scratch area, referenced by
filename.

## What was run, and what it returned

| Measurement | Result |
|---|---|
| `git diff --stat 1df6970..d20733b` | exactly `README.md`, `figures.mjs`, `index.html`, `screens.mjs`, `seed.mjs` — the spec's diff, nothing else (`app.css`, `audits/`, `ui.mjs`, `chrome.mjs`, `build.mjs`, `verify.mjs` untouched) |
| `node build.mjs`, twice, hashed between | exit 0 both runs; sha1 `a28ce9b1…` before and after — committed `index.html` byte-identical to its own rebuild, and the hash matches the claimed one. 75 screens; census line **character-identical** to a build of `a8cba70` (both clocks: `not built: 15 · engine-only: 35 · shipped: 8 · proposed: 5 · shipped (CLI): 3 · 9 drawn since` and `proposed: 14 · shipped: 59 · shipped (CLI): 2`); anchors 1786 → 1801 |
| `git diff 1df6970..d20733b -- screens.mjs \| grep "state: '"` | **0** — no 23 Aug census edits |
| `CHROMIUM=… node verify.mjs` | exit 0 — all nine families green (overflow ×3 widths, ids, names, headings, matrix panning, 13 figures / 250 texts, 560 touch targets, 1,597 stacked labels, 124 captions ×3) |
| Auditor's overflow sweep (own script) | 75 screens × 375/1000/1280: worst document overflow **0 px** at every width — the `struck()`-nowrap trap the wave says it avoided (a measured 509 px overflow had it used `.sf-result`) is confirmed avoided |
| Auditor's arithmetic probe (own script, raw literals + `a8cba70` worktree) | **69 checks, all pass** — detailed below |
| Auditor's DOM probe + click walk | every drawn-value check passes; hydrograph → map → exceedances → tarp click-walk lands on target at 1280 and 375 px |
| No new figure family | `svg.mk-fig` count 12 in both builds; the only `figures.mjs` export changes are `hydrograph`'s signature (trigger out, dips in) and two exported constants (`HYDROGRAPH_BORES`, `HYDROGRAPH_DOMAIN`) |

## The re-anchoring, re-measured

**The six-bore table, recounted by hand from raw literals** (`LOCATIONS.toc` and
`FIELD_ROUND` `depthToWater`, transcribed from source, not read through the API):
214.82−11.42 = **203.40** (MW01A) · 213.44−9.18 = **204.26** (MW03B) · 208.11−8.47 =
**199.64** (MW05) · 206.35−7.12 = **199.23** (MW07) · 203.90−6.88 = **197.02** (MW09) ·
217.05−12.86 = **204.19** (MW12). Every drawn May 2026 value in `WATER_LEVELS.series`
equals its subtraction exactly and matches the settled record's table. The three columns
agree exactly — better than the acceptance's "within the fit's own residuals" — because
all three are now the same subtraction made in one place.

**MW11**: no series (`WATER_LEVELS.series.MW11` undefined), anchor row carries the dry
reason in place of a number, and the plate's SVG text contains no MW11 — verified in the
DOM, and the plate legend shows only MW05 · MW07 · MW01A · Manual dip
(`w14-1280-hydrograph-plate.png`).

**Offsets derived, not typed**: `anchor.offset` computes to +5.565 / +5.432 / +4.772 /
+2.522 / +0.291 / −1.207 — matching the README's +5.57 / +5.43 / +4.77 / +2.52 / +0.29 /
−1.21 at its two decimals.

**Shape preservation, measured month-by-month against the actual `a8cba70` build** (not
against the new seed's own `was` field alone): across all 246 bore-months, worst
|new − (old + offset)| after the resurvey = **0.0010 m**; MW05's 15 pre-resurvey months
sit exactly 0.31 m above the offset line (worst deviation 0.0010 m); and the `was` value
the new seed carries equals the old build's drawn elevation in **all 246 months, zero
mismatches** — the screen's "what it drew before" column is the genuine old record, not a
re-derivation that could drift.

**The resurvey step**: `SURVEYS` is one record (208.42 until 2024-03-18, 208.11 from it);
the monthly series steps at the 2024-03/2024-04 boundary (readings dated the 15th, so the
March reading correctly still reduces through the old datum); `WATER_LEVELS.steps` holds
exactly one step, MW05, −0.31 m; the plate rules and names it with the datum-not-water
sentence in its hover. `CONSTRUCTION.MW05.surveys` **is** `SURVEYS.MW05` by property
reference; `LOGGER_SERIES` reduces each dip through `bore.surveys` at its own date
(seed.mjs:6485, 6578); `NEST`'s two heads compute from `tocAt` − dip (203.40, 204.26 —
reproducing the three literals it replaced); `LOGGER_SERIES.referenceElevation` is
**199.64** — the hourly and monthly plates now stand on one baseline, and the
beside-not-on panel is re-trued rather than retired (below).

## The trigger decision, re-measured and judged

**The auditor's own census, broader than the implementation's**: every string field of
all six registers walked recursively (the implementation searched named fields only) for
`m AHD`, for `µS/cm`, and — beyond the spec — for the literal `195.6`:

| Register | `m AHD` | `µS/cm` (control) | `195.6` |
|---|---:|---:|---:|
| LICENCE.conditions (6 of 24) | 0 | 0 | 0 |
| OBLIGATIONS | 0 | 0 | 0 |
| EXCEEDANCES | 0 | 2 strings (1 row) | 0 |
| TARP | 0 | 0 | 0 |
| CRITERIA_LIBRARY | 0 | 0 | 0 |
| WINDOW_CONDITION.series | 0 | 2 strings | 0 |

Zero level criteria, zero occurrences of the number itself, and a control that fires —
the search is capable of finding a criterion and there is none to find. (The
implementation's row-counting census reports 0 / 2; mine string-counts 0 / 4; the two
disagree only in units of counting, not in substance.)

**The pre-wave occurrences**: grep over the `a8cba70` tree finds `195.6` in exactly two
functional places, both `#hydrograph` — the `trigger: 195.6` figure argument
(screens.mjs:2737) and the configuration-row string (screens.mjs:2742) — plus one mention
inside the wave-8 deferral comment that *describes* those two, which is the record of the
finding rather than a third placement. The record's "exactly two places in the catalogue"
enumerates precisely what it means and holds.

**The number's provenance, verified**: against the old series the line sat 0.32 m above
the first drawn value (195.6 − 195.283, probe-confirmed as `startedAbove` 0.32) and was
exceeded in exactly **4 of 41 months — 2023-03 through 2023-06, the first wet season's
peak** — a ceiling grazing one trace on one plate, as the record says.

**The licence's six monitoring-obligation conditions, read in full by the auditor**:
12(b) Table 4 quality at the compliance boundary; 12(c) the three-consecutive-quarters
window on Cu/Zn/EC; 14 discharge returns; 18 the AER; 21 notify-on-awareness; 23
abstraction volume. None names a water level at any bore; nothing in the record could
re-derive a level trigger. **The withdraw-as-scenery reading survives.** The plan's
alternative — correcting the trigger's value — would have required inventing a condition
no instrument imposes, and the drawn reversibility path (a licence condition naming a
level at a named bore, flowing through the same machinery the chemistry conditions use)
is the correct one. Judged sound.

**The withdrawal panel's arithmetic, all derived and all verified**: 194.07 → 199.64
(drawnBefore is the genuine old value, probe-matched to the a8cba70 build); counterfactual
4.04 m above in **41 of 41** months (recomputed); "2.02 m below the plate" is computed
from the exported `HYDROGRAPH_DOMAIN` (floor 197.62 = min 198.02 − 0.4 padding), not
typed; **8 of 9** exceedances at MW05 (recounted from the EXCEEDANCES literals: eight
MW05 rows, one MW07); all six registers named on the panel with the µS/cm control stated;
the struck row renders `Trigger line · 195.6 m AHD (TSF licence)` with line-through in
the DOM, inside a wrapping parent — same two tokens as `struck()`, no `.sf-result`
nowrap, chrome untouched; reversibility stated. Every rendered occurrence of `195.6` and
`194.07` in `index.html` was grepped in context: all are the withdrawal record or the
before-column — nothing still asserts the line or the old level as current.

## The fit, re-fitted

The auditor's own least-squares (covariance form, own code) over the seven control points
— five heads recomputed from the auditor's raw toc/dip literals, STY01 203.5 and STY02
198.1 as the declared estimates: **bearing 102.8°, gradient 1.736 m/km, worst residual
0.549 m at MW01A** — matching the plate's 103° / 1.74 m/km / 0.55 m. `HEADS` computes the
five dipped bores by reference (`FIELD_ROUND` + `LOCATIONS.toc`), marks STY01/STY02
`estimated` (printed on the plate caption: "7 superficial bores, 5 reduced from the
round … STY01, STY02 estimated" — `w14-1280-potentiometric.png`), keeps MW11 as a dry
position with `h: null`, and records what moved per row: MW05 −0.16, MW07 +0.53, MW09
+0.02, MW12 −1.41 — the auditor's recount of the old stated heads against toc−dip gives
exactly 0.53 and 1.41 out at MW07/MW12, confirming the found-and-fixed claim that two
heads had been tuned to the plane. The before-values 119° / 1.93 / 0.04 render beside the
new ones on `#map` (DOM-verified).

## Found-and-fixed, ledger, and the unchanged surfaces

- **The backwards panel**: the `a8cba70` tree really does say re-anchoring would move
  MW05 "from four metres above the licence trigger line to just below it" — backwards on
  both ends (the old series sat 1.53 below; the new sits 4.04 above) — and the new panel
  records that sentence verbatim with both computed corrections and "it stood for a day".
- **The two corrected estimates**: measured spans old **10.52** / new **5.38** (probe,
  three drawn traces over all months) against the record's estimated 9.6 / 4.3; May
  separation MW05/MW07 **0.277 → 0.410 m** — further apart, not nearer — rendering as
  0.28 → 0.41. Both corrections true; the on-screen new-span is derived
  (`HYDROGRAPH_DOMAIN.hi − lo − 0.8`), not typed.
- **Ledger 5 → 4**: `FOUND AND DEFERRED` count 5 → 4; the wave-8 heading is now
  `## SETTLED (wave 14)`; the original 35-line body (table included) is preserved
  **byte-identical** inside the settled block (diffed against `a8cba70`); the four
  standing records (wave 7, wave 9 ×2, wave 10) are **byte-identical** in 46-line window
  diffs; `## SETTLED` count 4 → 5.
- **Verified-unchanged**: `TREND`, `ARSENIC_MW05`, `RAINFALL`, `EC_MW05` —
  JSON-identical between `a8cba70` and `d20733b` (all four checked, exceeding the
  spec's "spot-check two"). `#water` reads `WATER_LEVELS` zero times.
- **Keep-list (reported-then vs record-now)**: `#snapshot`, `#report`, `#submissions`,
  `#exceedances`, `#tarp`, `#licence` render **no** water-level tokens at all
  (DOM-extracted), so no issued-report context silently re-renders a level; `#narrative`
  likewise holds no level value — its evidence set is chemistry-only, so the withdrawal's
  "the interpretation argues a seepage mound" link is corroboration, not a moved value
  left stale. `#location` states the derive-at-measurement-date rule and holds no typed
  level. The 41-of-41 counterfactual coheres with the chemistry story rather than
  contradicting it.
- **Prior-finding regressions checked in passing**: the site-map legend collision
  (W1-A-6) is fixed and spaced (`w14-1280-map.png`); the empty `aria-labelledby`
  (W1-A-9) is gone, with the finding cited at the removal site.

## Findings

**None at any severity.** Every acceptance criterion of the wave spec was re-measured and
holds; the two coordinator-brief items that did not reproduce verbatim are both
paraphrase artifacts, not defects: "moved values beside befores on `#narrative`" (the
narrative holds no level values at all — nothing there to move, which the keep-list check
confirms is the correct state), and "2.02 m below the re-anchored axis" (the drawn
sentence is "2.02 m below the plate", computed from the exported axis domain).

## Verdict

**Wave 14 passes round 1 under §11's zero-P0/P1 gate — zero findings at any severity.**
The re-anchoring is exact where it claims exactness (six subtractions), millimetre-tight
where it claims preservation (246 bore-months against the real prior build), the trigger
withdrawal is measured, correctly decided against the licence record's own contents, and
drawn with derived arithmetic end to end; the fit moves honestly with its inputs and
reproduces under an independent solver; the ledger keeps its history byte-for-byte; and
the build, sweep, walk and hygiene checks are all green.
