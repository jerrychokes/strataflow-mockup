# Wave 7 — response to the audit, round 1

**The gate passed on the first round — zero P0/P1/P2** — with the chain structure
verified by identity, every moved count recounted from raw literals, the auditor's
own least-squares fit corroborating the printed plane, and the keep-list found
eight for eight. All three P3s were right and are fixed in this closing commit:

| Finding | Severity | Response |
|---|---|---|
| W7-A-1 — the README dated Wave 7 to 1 September against the wave's own records | P3 | **Accepted and fixed.** 2 September, agreeing with the commit and the deferral comments. A catalogue whose method is dated records should not hold two dates for one wave. |
| W7-A-2 — the plate printed the gradient at 1.9 beside prose saying 1.93 | P3 | **Accepted and fixed at the plate.** Both render `POTENTIOMETRIC_FIT` at two decimal places now — the catalogue's own rendering-rule argument ("1.77 in a table beside 1.77 in a sentence is the rule agreeing with itself") decided which side moved. |
| W7-A-3 — `PAS-WO-268841` doubles as the metals batch id and the submission work order | P3 | **Accepted; the conflation is stated rather than renamed.** The convention is real — a laboratory reports runs under the submission work order the box travelled on — so the S1 chain's prose on `#ecoc` now reads past it explicitly (the same identifier names the bottle's physical origin and the metals batch; Yarra Regional issues its own batch id, which is why a PFAS result never cites the metals number), and the `METALS_BATCH` declaration carries the same note. Renaming the id would have touched every surface that cites it in a closing commit; stating the convention makes the custody hop honest without the ripple. |

**State at close:** build exit 0, rebuild byte-identical (`f70aee1f…`), 23 Aug
census unchanged; verify exit 0 across all nine families with the fixes in.
Wave 7 — provenance to the bore, Explain it/Trace it, the evidence workspace,
and the MW11 rider — is closed under §11 on the auditor's round 1 against
`d3630dd` plus these three fixes.

**Deferrals carried forward** (recorded in-repo by the wave, restated here so the
wave plan sees them): the PFAS suite's three-way coverage disagreement (beside
`BATCHES` and visible on `#batches`) and `#consistency`'s balances not derived
from `MAJOR_IONS` (comment on that screen; MW12 has results and no row). Both are
candidates for the next wave that touches their screens.

**The caveat that outlives the wave:** the practitioner has not seen the
redesigned screens, which now carry more invention than the ones the review
corrected, and the figure grammar remains unprinted (G-76b).
