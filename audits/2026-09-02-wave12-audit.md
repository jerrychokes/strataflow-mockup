# Wave 12 audit — round 1 — commit `39187d4`

Jerry directed that deep audits run on Codex; this environment has no Codex CLI and no
OpenAI credentials, so this round runs under `EXPANSION_BRIEF.md` §11's fallback auditor
provision ("any capable model session with no prior context — the mechanics matter more
than the model").

Audited against: the "Wave 12 — Configuration portability and the estate view (OM-5,
OM-2)" section of `audits/2026-09-01-wave-plan.md`, plus the keep-list. HEAD at audit time
was `c13cbec`, which differs from `39187d4` only by `audits/2026-09-02-figure-grammar-proposals.md`,
so the working tree's build inputs are the wave commit's. Nothing here is taken from the
implementation's own probes (`…/scratchpad/wave12/`); every measurement below was
re-instrumented by the auditor, and the seed recounts were made from the raw literals in
`seed.mjs` read as text, never by importing it.

— the standing auditor lineage

## Commands run, and what they returned

| Measurement | Outcome |
|---|---|
| `node build.mjs` twice, `sha1sum index.html` before/between/after | exit 0 both runs; all three hashes `41ce52b6…` — rebuild byte-identical and equal to the committed file; tree clean |
| Build census | 23 Aug line `not built: 15 · engine-only: 35 · shipped: 8 · proposed: 5 · shipped (CLI): 3` — **character-identical** to the pre-wave build (worktree at `eb87874`, rebuilt, compared); `9 drawn since` (was 7); 75 screens (was 73); 1769 anchors; link graph clean |
| `git diff eb87874 39187d4 -- screens.mjs` filtered to `state:` lines | **zero** — no 23 Aug census edits; both new register rows are `now: 'proposed', added: '2026-09-02'` with no `state:` field |
| `CHROMIUM=… node verify.mjs` | exit 0 — nine families green (overflow ×3 widths, ids, names, headings, matrix panning, figure collisions, 550 coarse-pointer targets, 1567 stacked labels, 119 captions ×3 widths) |
| Auditor's own sweep (playwright-core, own script) | 75 screens × 375/1000/1280: worst document overflow **0 px** everywhere |
| Walk by real clicks at 1280 | instance→estate→instance→package→criteria→package→estate→diagnostics: **7/7 hops landed** on their targets |
| Hygiene | `39187d4` touches README, build.mjs, chrome.mjs, index.html, screens.mjs, seed.mjs only — `app.css`, `figures.mjs`, `audits/` untouched; the +90 chrome lines (`.mk-vendor…`) sit at the **end** of `CHROME_CSS`; 0 figures (`svg.mk-fig`/`svg.sf-map`) on both new screens — no new figure family; 7 inherited `FOUND AND DEFERRED` records intact (none removed in the diff), 1 added (wave 12's bundleGap) |
| Screenshots taken and inspected | #estate at 1280 (full) and 375 (full + frame-top close-up), #package at 1280 — all render as complete content |

## Recounts, from raw literals

- **36** = ANZG-assessed analytes × sampled locations, counted off the `CROSSTAB` literal
  (seed.mjs:866–907): exactly 6 rows carry a non-`N` first outcome slot (arsenic, cadmium,
  copper, nickel, zinc, PFOS + PFHxS) × exactly 6 sampled columns (7 minus MW11, which is
  `NOT_SAMPLED` in every row). Rendered as 36 on #package with the same breakdown.
- **18** = `HARDNESS.derived` (3 entries: Cd, Ni, Zn — seed.mjs:4139–4142) × 6. Rendered.
- **Diff 1 · 1 · 1** — three items, three kinds (criteria library changed 2018.1→2018.2,
  ESdat ELDF-4 identical with equal SHA-256s, DWER template new into an empty kind);
  "consequential" defined on the screen as reaches-committed-results, and only the criteria
  item does. Past rounds 0 / locked periods 0 / issued reports 0 each carry its reason;
  outcomes render "not known — the test has not been run", deferred to #criteria by link
  (6 links; walked by click).
- **Exportable 9** = `FORMATS.length` (4) + `CRITERIA_LIBRARY.length` (5) + report
  templates 0, counted off the literals; the rendered table says "Counted off the
  registers themselves" and shows 5 · 4 · 0.
- **Estate drift arithmetic** against `OPS_AS_AT` 2026-08-23, releases recounted from the
  6-row literal (v0.6.0 06-12 → v0.8.0 08-21): WDL v0.7.2 **1 behind · 7 days on it**
  (installed 08-16), confirmed 25 d (bundle 07-29); TAL v0.8.0 **0 behind · 1 day**, never
  confirmed; BOO v0.7.0 **3 behind · 28 days**, ageing 48 d; NUL v0.6.0 **5 behind ·
  69 days**, stale 66 d; YAN v0.7.2 **1 behind · 5 days**, refuted 3 d. Headline **5
  deployments · 4 of 5 behind · furthest 5 releases/69 days · 1 drift confirmed · 1 of 5
  confirmed within 30 days** — all re-derived by hand and all match the rendering.
  Deployments-per-release column: 1+0+1+0+2+1 = **5**. Every date on the screen is a
  source record's own; every "how the vendor knows" sentence names a no-telemetry
  mechanism (its own install act, the contract, a dated bundle) — DR-2 checked
  sentence-by-sentence, clean apart from W12-A-2 below.
- **One-source claims**: the releases row for v0.7.2 and the WDL row read
  `INSTANCE.version/.released/.commit` by property reference (seed.mjs:8270, 8295);
  TAL's package is `CONFIG_PACKAGE.id` by reference (8321); #instance's Configuration row
  reads `CONFIG_PACKAGE.inForce.id` (screens.mjs:5307). Verified in source, and the
  rendered values agree across #instance, #estate and #package.
- **`OPS_AS_AT` names the existing clock**: all seven listed literals verified in place and
  on 2026-08-23 (`INSTANCE.backup.last`, scheduler tick, `ENTITLEMENT.remaining` 130 days
  = 08-23→12-31 recounted, `MEMBERS[0]/[1]` 07:41, `MEMBERS[3]` 06:55,
  `SAVED_VIEW_LIST` used 2026-08-23); the wave-11 two-clocks deferral text (seed.mjs:420,
  README:859) is untouched by the diff.
- **Anchors block** (seed.mjs:7789–7832 and the estate block at 8206+): every quotation
  checked against the app repo. `ConfigKind`, `versionOf`'s comment ("left alone rather
  than normalised…"), the unversioned-item throw, the omitted-kind sentence, the
  `kind:name:version:checksum` hash lines, `planDeployment`'s union and its "slower path"
  sentence, `driftAgainst`, diagnostics.ts's "G-56c's inventory: each artifact's own
  version and SHA-256, never its content", version.ts's "a checkout is not a release" and
  "nothing transmits it anywhere", release.yml's CI gate and its "the ones I remembered to
  name" — **all verbatim** (several wrap across source lines; matched with context). The
  one anchor that does not survive its source is W12-A-2.
- **bundleGap card**: #diagnostics's "In the bundle" table = exactly **4** rows (version
  and commit, migration state, recent errors, schema and policy inventory), none a
  configuration category; #entitlement's contents preview = exactly **8** rows, nearest
  "Instance version, schema version, reference-content versions"; the strings "SHA" and
  "checksum" appear on **neither** screen. The product-side half was verified too:
  diagnostics.ts really does carry the configuration category with per-item versions and
  SHA-256s. The card's numbers are right; see W12-A-3 for one word that is not.
- **Vocabulary**: "estate" occurs **0** times in the rendered #package text; the minting of
  "package" (bundle spent on the diagnostic bundle since 23 Aug — true, `BUNDLE`/#diagnostics)
  and "item" (artifact spent on the stage artifact — true, #imports/#quarantine) is said on
  the screen itself; "estate" is `PORTFOLIO.says`' own sense and OM-2's own phrase.
  Judged: all three decisions sound and visible rather than silent.
- **Coverage**: OM-2 `proposed` with the 1 Sep sentence quoted and a dated 2 Sep correction
  ("the first half stands and the second does not"); OM-5 `proposed` with its dated note;
  `prdCovered` filters `=== 'covered'` only, so the stat is unchanged by the two flips —
  read in source (screens.mjs:10538–10563).
- **README**: headline 72→75 and the four job rows re-derived with the rot dated and
  named (J1 14→18, J6 5→6, J8 7→9, J9 4→5); all four verified against the rendered rail
  (J1=18, J6=6, J8=9, J9=5; jobs sum 75; by-section groups 5+10+7+9+15+9+6+4+6+1+3 = 75
  with "Vendor operations | 1").

## The frame judgement (OM-2), by screenshot at both widths

The frame holds. At 1280 and 375: the hatched `.mk-vendor` frame is labelled at **both**
edges ("VENDOR SURFACE — NOT THIS DEPLOYMENT, AND NOT REACHABLE FROM IT" /
"END OF THE VENDOR SURFACE. EVERYTHING ABOVE BELONGS TO STRATAFLOW, NOT TO MOCK-WDL"),
and the hatched side borders run the frame's full height, so a cropped mid-scroll
screenshot still shows a marked surface. The counter-chrome carries M. Ferreira ·
Strataflow operator and contains **zero** interactive elements (probed: no links, buttons,
inputs or tabbable elements inside `.mk-vendor__chrome`). The statement panel names both
sessions explicitly. The crumb reads "Strataflow — vendor operations / Deployment estate"
with no project trail and no link (verified in build.mjs's crumb table and the rendering).
The related links sit **outside** the bottom edge, under a "Leaving this frame" sentence.
The two `sf-button` controls inside the frame ("Record an upgrade", "Open a bundle") are
the vendor's own actions on the vendor's own surface — consistent with the frame, not a
leak. The catalogue's real chrome is untouched. Nothing on or around the screen implies
the customer session reaches it. The filing (a one-screen "Vendor operations" group) is
argued in build.mjs and the partition guard now owns it; the reasoning is sound.

## Findings

### W12-A-1 · P0 — Two drawn screens give the in-force ANZG set two irreconcilable provenances

- **Reproduction:** `#entitlement`'s version block renders "ANZG 2018 criteria — **2018.1 ·
  loaded 2026-01-14** · reviewed by a licensed practitioner" (hand-typed at
  screens.mjs:9854, pre-existing). Wave 12's `#criteria` now renders "**Arrived in
  MOCK-CFG-2024.1 · 2024-02-19** / carried unchanged since, most recently by
  MOCK-CFG-2026.2" and the sentence "2018.1 is not something anybody here wrote: it
  arrived on 2024-02-19 in MOCK-CFG-2024.1, the first configuration package this instance
  took, **and every package since has carried it unchanged**" (from
  `CONFIG_PACKAGE.inForceFrom`, seed.mjs:8080, reading the pre-existing
  `CRITERIA_DRAFT.active.at`).
- **Why it is the wave's:** pre-wave (worktree at `eb87874`, rebuilt), #criteria showed
  only the phrase "Loaded with the shipped reference content" with **no date rendered**,
  so #entitlement's 2026-01-14 stood alone. The wave surfaced 2024-02-19 with a narrative
  ("every package since has carried it unchanged") that leaves no event for a 2026-01-14
  load — no package in the catalogue carries that date, and the wave's own model gives
  criteria content no other arrival path. Two drawn screens now assert incompatible
  provenance for the same artifact — in the wave whose subject is making arrival
  provenance answerable, and whose own #instance panel argues that one-source reads exist
  so screens "cannot disagree". Nothing records the tension (the only "2026-01-14" in the
  repo outside this line is an unrelated instrument calibration).
- **Expected:** one provenance for 2018.1, or two dated statements that name how both are
  true.
- **Recommended correction:** derive #entitlement's ANZG line from
  `CRITERIA_DRAFT.active` / `CONFIG_PACKAGE.inForceFrom` (the screen already reads
  `INSTANCE.*` by property two rows up), or date the difference on its face. The analyte
  dictionary and schema lines on the same panel are also hand-typed and worth the same
  pass while it is open.

### W12-A-2 · P1 — The screen says the release publishes signed build provenance; the release's own file says it currently attests nothing

- **Reproduction:** `#package` renders "A checksum, not a signature. **The release
  publishes signed build provenance for the two images it builds** and for nothing else —
  configuration is not one of them…" (from the seed's anchor bullet at seed.mjs:7825–7827).
  The named source, `.github/workflows/release.yml`, conditions both attestation steps on
  `!github.event.repository.private` and its comment records the reality: "the images
  pushed, then `attest` failed with *'Feature not available for user-owned private
  repositories'*… **these images carry no attestation, and nobody should verify one that
  was never written**."
- **Grading:** the operative claim (configuration is checksummed, never signed; the only
  provenance path that exists at all targets exactly the two image digests) is true and
  verified. The clause "publishes signed build provenance" is false of the repository as
  it stands, against an anchors block whose own preamble is "Nothing below is drawn from
  memory. Each of these was read in the app repo." Not graded P0 because it contradicts no
  other drawn screen and the falsehood strengthens rather than weakens the screen's
  argument (today *nothing* is signed, which is more reason a checksum is not a
  signature) — but it blocks the gate as a P1 all the same.
- **Recommended correction:** one clause — e.g. "the release declares signed build
  provenance for the two images it builds and for nothing else — and on this repository
  even that is skipped, so today nothing is signed at all; configuration has no signing
  step either way." Fix the seed bullet in the same edit.

### W12-A-3 · P3 — The bundleGap record's "not among them at all" is contradicted by the prose one line above the table it counts

- **Reproduction:** the deferral record (seed.mjs:8479, rendered on #estate) says
  #diagnostics "lists four things in the bundle … and the configuration category is **not
  among them at all**". The four-row table indeed lacks it — but #diagnostics's own intro
  sentence directly above the table reads "…a diagnostic bundle is something the customer
  runs and sends: version, **configuration**, logs, schema state, recent errors", naming
  the five product categories including configuration. The enumeration gap the record
  describes is real (no per-item versions, no SHA-256, on either screen — measured); "at
  all" overstates it by one sentence.
- **Recommended correction:** soften to "not among the four its table enumerates", or fold
  the prose sentence into the record as the third inconsistent description.

## Clean categories, stated as such

Build determinism and census integrity; the register's two new rows; the partition with
the new one-screen group; verify's nine families and the auditor's independent overflow
sweep; the 7-hop click walk; every seed recount listed above (impact, diff, exportable,
drift arithmetic, headline, deployments-per-release, bundleGap counts); the anchors block
except W12-A-2; the vocabulary decisions and the estate-absent-from-#package claim (0
occurrences); the one-source property references; the seven OPS_AS_AT literals and the
untouched two-clocks deferral; the keep-list reported-then sentence on the activation
panel naming the snapshot and the period lock (both of which are drawn screens in the
catalogue); OM-2/OM-5 coverage flips with `prdCovered` unchanged; the README re-derivation
(all four corrected rows re-verified against the rendered register); the frame shift as
judged above; the inherited deferral records; no new figure family.

## Verdict

**Wave 12 does not pass round 1 under §11's zero-P0/P1 gate.** Blocking: **W12-A-1** (P0 —
cross-screen contradiction on the in-force criteria set's provenance, made live by this
wave's own enhancement) and **W12-A-2** (P1 — a product-mechanics claim on #package that
its named source contradicts). Both corrections are small and localised; everything else
this wave claims was independently re-measured and held, including every number on both
new screens and the frame decision the wave was most likely to get wrong.

**Count:** 1 × P0 · 1 × P1 · 1 × P3.
