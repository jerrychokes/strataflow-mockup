# Wave 8 — response to the audit, round 1

**The gate passed on the first round — zero P0/P1/P2** — with the derivation
arithmetic reproduced from raw deployment literals, the compensation recomputed at
the auditor's own instants and across all 1,536 points (worst disagreement half a
millimetre), the six ion balances re-derived by hand to the digit, the overlap
invariants recomputed rather than accepted, the journey walked 8/8 by real clicks
at both widths, Figure 4.11 judged against the app repo's grammar as existing
moves only, and the WATER_LEVELS baseline deferral held to the W6-A-1
recorded-properly standard and passing it on both halves. Both P3s were right and
are fixed in this closing commit:

| Finding | Severity | Response |
|---|---|---|
| W8-A-1 — "the monitored locations" scoped wider than its groundwater-only computation, silently excluding SW01 | P3 | **Accepted and fixed with the sentence, not the filter.** "Monitored bores and instrumented points" — the auditor's own alternative — matches the computation's actual scope (groundwater bores plus the TSF piezometer), and the screen's context is downhole and barometric instrumentation, so the register keeps its subject rather than gaining a surface-water row it would say nothing about. |
| W8-A-2 — the anomaly's 0.382 m and the correction's 0.38 m shift, two unrelated near-identical magnitudes nothing defuses | P3 | **Accepted and fixed with the clause, not the seed.** One sentence where the two meet, composed from both derived values, saying they share no cause — one is water moving at the screen over five hours, the other a re-seated sensor applied as a constant — and that neither is an artefact of the other. Changing the spike's magnitude instead would have moved numbers the audit had just verified, to remove a coincidence the record can simply state. |

**State at close:** build exit 0, rebuild byte-identical (`1a4637db…`), the 23 Aug
census state counts unchanged; verify exit 0 across all nine families with both
fixes in. Wave 8 — the telemetry journey walked end to end, the instrument
register, the raw-and-corrected series pair, Figure 4.11, and the consistency
rider that found every typed balance wrong — is closed under §11 on the auditor's
round 1 against `091e62d` plus these two fixes.

**Deferrals carried forward** (all recorded in-repo by their waves): the
WATER_LEVELS baseline disagreement (seed, dated, with the six-bore comparison —
re-anchoring crosses a licence trigger and needs a deliberate wave), `#imports`'
three remaining literal stat tiles, and wave 7's PFAS three-way coverage
disagreement.
