# Wave plan — 1 September 2026

Phase 4 of `EXPANSION_BRIEF.md`. Waves are ≤ 8 screens or ≤ 1 week, whichever is
smaller; ordering follows the brief (import → report → obligations → field → figures →
water → configuration; deferred scope last). Every wave ends with `node build.mjs`
green and the §10 sweep green at 375, 1000 and 1280 px, and then the independent audit
loop (§11) before the next wave starts. Sources: the pass-4 findings note and the
extended `#coverage` matrix — a wave item without a matrix row behind it does not
belong here.

**Recommended first wave: Wave 1.** Nothing else should land on top of screens that
measurably overflow and prose that states falsehoods.

---

## Wave 1 — Make the drawn record true and sound

**Objective:** the catalogue stops contradicting its own measurements. No new
screens; no new capability.
**Journeys advanced:** all, as integrity — every walk crosses these screens.
**Screens modified:** the thirteen with measured document overflow (`crosstab`
+519 px at 375, `qc-limits`, `hardness`, `hydrograph`, `imports`, `entitlement`,
`programme`, `project-home`, `projects`, `dqa`, `submissions`; `hardness` and
`hydrograph` at 1280 too) — the fix is containment (`overflow-x: auto` matrices with
frozen identity columns, per the viewport strategy), never removal; plus the ~40
screens whose route line still asserts a 23 August state (mechanical refresh from the
register's `now` field — generate the line from register data so it cannot rot
again).
**Tooling:** `verify.mjs` joins the repo — the driven-browser sweep from pass 4
(overflow at three widths, duplicate ids, unnamed controls, heading order) with
`playwright-core` as a dev dependency; the auditor runs the same script.
**Seed changes:** none. **Navigation changes:** none.
**Acceptance:** sweep reports zero document overflow at all three widths; build
checks green; no screen prints a state that disagrees with the register; the README
gains a dated pass-4 section (drawn 23 Aug, re-derived and repaired 1 Sep).
**Regression risk:** low — layout containment and generated prose only.

## Wave 2 — Journey 1 closed: eCOC, and the loop's missing states

**Objective:** the EDD-to-approved-data walk is fully owned, end to end.
**Journeys advanced:** 1 (to covered), 5 (partially).
**New screen (1): `ecoc`** — passes the New-Screen Test: the chain of custody is a
durable legal record with its own lifecycle (created in the field → transferred →
received); `receipt` owns only the laboratory end, and no existing screen owns
creation and transfer. Entry: `events`, `field-capture`; onward: `receipt`,
`imports`. Filed under Sampling events.
**Enhanced (4):** `events` (QC sample taxonomy and composite summary on the round),
`exceedances` (FR-5.2 — a rolling-window condition drawn *deciding an outcome*, not
described), `qc` (FR-4.5 — spike/outlier-vs-history as a raised finding type),
`imports` (row-count statement verified against the F-02 answer).
**Seed:** the custody chain for round 2026-Q2-GW; a window-condition exceedance.
**Acceptance:** Journey 1 renders with no unowned step; coverage rows FR-5.2 and
FR-4.5 flip to covered; sweep green.
**Regression risk:** medium — `events` is on the J1/J5 spine; verify both walks.

## Wave 3 — Journeys 3, 4 and 8 deepened: the report's governance

**Objective:** issuing, amending and re-configuring are drawn as decidable
interactions, not assertions.
**Journeys advanced:** 3, 4, 8.
**Enhanced (6):** `report` (FR-7.5 golden-output comparison state; FR-7.2 corporate
template configuration as a tab), `report-figures` (numbering and cross-reference
states), `snapshot` (regeneration diff against the issued artifact), `certificate`
(amendment impact preview: derivations → exceedances → trigger states → reports, one
chain), `criteria` (J8's test-against-real-records and impact preview drawn deciding
something; FR-2.2's non-detect binding operable), `signoff` (what exactly was
approved, bound to the snapshot).
**Seed:** an amended certificate with a changed outcome; a criteria version pair.
**Acceptance:** Journeys 3/4/8 walk with every step interactive-in-kind; FR-2.2,
FR-7.2, FR-7.5 flip to covered.
**Regression risk:** medium — supersession propagation must stay consistent across
`certificate`, `supersession`, `exceedances`, `snapshot` (§10 cross-screen counts).

## Wave 4 — Water and loads

**Objective:** the one product route the catalogue never drew, drawn.
**Journeys advanced:** 7-adjacent; FR-1.12/FR-5.5/FR-5.6 close.
**New screen (1): `water`** — trivially passes the New-Screen Test: the product
already ships `/projects/:id/water` ("Water take against entitlement",
`reachedFrom: locations`). Extraction against the entitlement year in force,
approaching-limit flags, mass load by reporting period (FR-5.5) beside it.
**Enhanced (2):** `licence` (entitlement conditions link through), `locations` (the
take drawn at the bore it is drawn from).
**Seed:** entitlement, monthly extraction volumes, one approaching-limit month.
**Acceptance:** FR-5.5 and FR-5.6 covered; the Locations section gains `water` in
the by-section view and the partition check still passes.
**Regression risk:** low — additive.

## Wave 5 — The portfolio, honestly drawn

**Objective:** Journey 9 stops being blocked on a one-site seed.
**Journeys advanced:** 9 (to covered).
**Seed workstream (the named prerequisite):** a second `MOCK-` project in
`seed.mjs` — distinct site, small deterministic dataset, byte-identical rebuild
preserved, `MOCK-` naming throughout. This is the wave's real work.
**Enhanced (≤5):** `projects` (two real rows), `project-home` (the second project's
own state), `obligations` (cross-project rows that are actually cross-project),
`search` (results spanning projects), `coverage` (counts re-derived).
**Acceptance:** Journey 9 walks across two projects; no screen shows a count the
seed contradicts.
**Regression risk:** medium — every screen reads the seed; the sweep and the
cross-screen count checks are the guard.

## Wave 6 — Field work depth (gate-dependent)

**Objective:** Journey 5 at full depth — gated on the brief §12 **practitioner
gate**, due after wave 2 and binding before this wave. (The other gate this plan
named is resolved: the **work queue stands as a proposal** — Jerry, 1 Sep 2026,
findings §7 — so assignment and hand-over are designed against it as a proposal.)
The gate that remains: `field-capture`, the QA/QC workspace, the lineage
panel and the interpretation editor are the four invention-heaviest screens in the
catalogue, and this wave deepens the first of them.
**Scope (≤5):** `field-capture` from proposal to designed depth (one-handed 375 px
first), assignment/hand-over per the work-queue decision, `purge`/`receipt`
continuity with `ecoc`.
**Acceptance:** Journey 5 fully owned; coarse-pointer targets pass by hit-testing.

## Deferred — recorded, not scheduled

Telemetry and instruments (Domain K + the deferred journey; PRD S8) · external-party
access (OD-2) · the integration estate (Domain R beyond FR-3.11) · soil/sediment
matrices and composites (FR-1.7) · EQuIS export (FR-7.6, OD-8) · the estate view
(OM-2, vendor-side) · configuration packages (OM-5's import/export surface) · new
figure families (Durov and any addition to the twelve plates — each needs a grammar
proposal put to Jerry first, and every figure drawn inherits the G-76b unprinted
caveat until the §7.4 print test happens).


---

# Re-planned after the practitioner review — 1 September 2026

The review (`2026-09-01-practitioner-review.md`) landed four P0 design changes and
a binding keep-list. Two of the four are workflow redesigns and two are
provenance/interpretation redesigns; splitting them respects the wave cap, and
PR-1 is explicitly a **replace** — the drawn field-capture entry axis was judged
wrong, so Wave 6 stops being a deepening wave and becomes a redesign wave.

## Wave 6 (rescoped) — the field and QA workflow, redesigned to the review

**Scope:** PR-1 — `field-capture` rebuilt around the **bore session** (the round
grid demoted to summary; the session carries SWL → condition → pump → purge/
stabilisation series in line (PR-1c) → final chemistry → samples/QC → filtration/
preservation → CoC → observations → completion), disposition states (PR-1a), the
round-completion preflight (PR-1b); `purge` refolded as the session's series view;
`events` gains dispositions and the preflight summary. PR-2 — `qc` rebuilt around
**Unresolved → Reviewed → Dispositioned → Ready for validation** with the
need-decision counts as the headline (PR-2a), every propagated qualifier stating
its propagation basis (PR-2b), re-run naming its DQO version with
current-vs-used surfaced (PR-2c); `qc-limits` carries the version pairing; `dqa`
reads the dispositions.
**Riders:** PR-3b (the PFHxS unqualified-detect-below-LOR, fixed in seed and
everywhere it renders), the project-settings facility select, the programme
one-day countdown.
**Constraints:** the keep-list, verbatim, plus every §5 convention.
**Acceptance:** the bore session walks one-handed at 375px; the QA screen's
primary question is "what is still mine to decide"; build + verify green; the
review's hesitation points re-walked and closed.

## Wave 7 (new) — provenance and interpretation, redesigned to the review

**Scope:** PR-3a/PR-3c — `lineage` extended backwards through location/version,
construction, programme, field event, sample, custody and receipt, stitched from
the screens that already hold each hop, with **Explain it** (narrative) and
**Trace it** (compact graph) views; `result-detail` carries the same chain. PR-4 —
`narrative` becomes an evidence workspace: pinnable evidence sets per passage
(PR-4a), reference tokens distinguishing renumbering from content change (PR-4b),
an explicit numeric rendering rule (PR-4c).
**Rider (carried from the wave 6 audit, W6-A-1):** blank the crosstab's MW11
column so the grid matches the field record — two visits, found dry, zero
samples — moving the censored counts and every surface computed from
`CROSSTAB` deliberately rather than in passing; then remove the interim
contradiction notice on `#crosstab` and re-true the `#data-states` sentence
to "drawn empty", which the blanked column will have made simply true.
**Acceptance:** the lineage answers the review's list of questions (which bore,
which screen interval, who sampled, filtration, preservation, custody, receipt
temperature, holding time, travelling QC) by link; the evidence set shows a
reviewer exactly what the author saw.

## Waves 8+ — the deferred bucket, unchanged in content, renumbered

Telemetry and instruments · external-party access · integration estate ·
soil/composites · EQuIS export · estate view · configuration packages · new figure
families (each behind a grammar proposal to Jerry). Planned as before when their
turn arrives.

---

# Waves 8+ planned — 2 September 2026

Jerry's standing instruction (1–2 Sep) continues the pipeline into the deferred
bucket and beyond: *continue waves until scope runs out, pinging at each close.*
The bucket is planned one wave at a time, as the original plan said it would be.
Drawing a deferred domain does not reschedule it in the product: every screen
these waves add is `proposed`, and the PRD's S8 markings stand.

## Wave 8 — Telemetry, loggers and instruments (Domain K, the deferred journey)

**Objective:** the one journey the catalogue has never walked, walked — the
telemetry exception: Instrument → Raw series → Gap/anomaly → Correction →
Corrected series → Re-evaluation → Alert/TARP impact → Audit (FR-1.10, FR-3.10).
**New screens (2):** `instruments` — the instrument register as a durable asset
record with its own lifecycle (deployment history at bores, calibration records
the wave-6 preflight already reads, service state, the barometric source named
per location group), which no existing screen owns; and one screen for the
raw-and-corrected series pair at a bore — raw retained untouched, corrected
derived with the compensation rule and its inputs carried (PP3), a gap drawn as
a gap, an anomaly raised as a finding, a correction recorded with reason and
reach, and the re-evaluation impact stated against the records that exist.
Names resolve against the glossary before anything is minted.
**Enhanced (≤3):** `hydrograph` (FR-1.10's own words — continuous series drawn
*separately from* discrete dips, in the same approved grammar, no new family),
`imports` (a logger file as an import kind, vendor format and barometric source
named), `location` (the instrument at the bore).
**Rider (carried from wave 7):** `#consistency`'s ionic balances derived from
`MAJOR_IONS` rather than typed, MW12 given the row its results owe — the
typed-copy drift class this pipeline exists to remove. The PFAS three-way
coverage disagreement stays recorded where wave 7 put it; it needs a design
decision interacting with `#criteria`'s non-detect demonstration and belongs to
a wave that redesigns those screens deliberately.
**Constraints:** the keep-list; the telemetry-journey coverage rows and the
"not walked" sentence updated with dated corrections, never rewritten; every
figure in the existing hydrograph/timeseries grammar — **no new figure family**
(Durov-class additions still need a grammar proposal to Jerry first); G-76b's
unprinted caveat inherited.
**Acceptance:** the deferred journey walks end to end with no unowned step;
FR-1.10/FR-3.10 flip to drawn-as-proposed with the S8 marking intact; build,
verify and the §11 audit loop green.

## Waves 9+ — the remaining bucket, planned at their turn

External-party access (OD-2) · integration estate (Domain R beyond FR-3.11) ·
soil/sediment matrices and composites (FR-1.7) · EQuIS export (FR-7.6, OD-8) ·
estate view (OM-2) · configuration packages (OM-5) · figure families (behind
grammar proposals). When the bucket and the accumulated riders are spent, the
pipeline closes with a summary rather than inventing scope.

## Wave 9 — Soil, sediment and composites (FR-1.7) — planned 2 September 2026

**Objective:** the one sample structure the catalogue has never drawn — a sample
that is *made of other samples* — and the depth axis that water samples do not
have. FR-1.7: depth intervals on soil and sediment samples; composite samples
with traceability to constituent increments. Vapour stays undrawn (no seed
context to draw it honestly); the coverage row says drawn-for-soil-and-sediment.
**New screen (1):** the composite sample record — increments with their depth
intervals and positions, the compositing decision as a record (who, when, what
scheme, why), traceability both ways, and the honest limit stated: a result on a
composite cannot be read back to one increment, and the screen says what that
buys (coverage) and what it costs (resolution) rather than pretending otherwise.
New-Screen Test: a composite has its own lifecycle (planned → increments
collected → composited in field or laboratory → analysed) and no existing screen
owns the increment-to-composite step. Naming anchors to FR-1.7's own nouns —
composite sample, constituent increment, depth interval — and the glossary's
location class / matrix distinction binds throughout (a sediment sample at a
surface-water location is the distinction's own example).
**Enhanced (≤4):** `events` (a soil investigation beside the groundwater round —
locations of class soil, depth-intervalled samples, the composite collected
across them); `locations` (class soil drawn as a class with its own record
shape, not a bore missing its casing); `lineage` (a composite result's chain
shows the increments — the one place "which increment" is answered with the
honest *cannot say*); `crosstab` or `results` (the matrix filter honestly
backed: what a soil row looks like, or the filter stating what it holds today).
**Seed:** a small deterministic soil investigation near the TSF — a handful of
class-soil locations, depth intervals, discrete samples plus one or two
composites, matrix recorded never inferred, evaluated only where a criteria set
in the seed legitimately applies (not_evaluated is the honest default per the
glossary's matrix entry — no criteria set is invented in passing).
**Constraints:** the keep-list; no new figure family; every count computed;
deferrals recorded in-repo; G-76b inherited.
**Acceptance:** the composite's chain walks both directions with the limit
stated; depth intervals render on sample records; FR-1.7 flips to
drawn-as-proposed for soil and sediment; build, verify and the §11 loop green.

## Wave 10 — Data exchange: the format estate and the EQuIS-bound export (FR-7.6, Domain R) — planned 2 September 2026

**Objective:** the catalogue's data exchange drawn as an estate rather than a
scatter — everything that crosses the boundary, in either direction, as a
record — and FR-7.6's deferred export drawn deciding something. FR-3.11's
import side is covered (`migration`, reconciliation proof); this wave draws its
mirror.
**New screen (1):** the format estate — every format the project exchanges,
each as a record: direction, owner, version, what it preserves and what it
cannot carry. Inbound: the ESdat EDD, the incumbent migration formats, wave 8's
logger vendor file. Outbound: the regulator-format return and the
EQuIS-compatible EDD. The EQuIS export is the screen's working example, drawn
with the **outward mapping stated** — ADR-0009 says practitioner vocabulary is
canonical and file formats map inward, so an export maps *outward* (LOR → EQL)
and the mapping table is the ADR rendered — a **reconciliation statement
mirroring the import's** (row and outcome counts proved against the record, the
FR-3.11 symmetry), and the honest loss statement: what the target format has no
column for, named rather than silently dropped. DR-2 binds: no Strataflow-
operated relay — an export is a file with a manifest, never a held URL, and the
screen says so where a reader would look for a "send" button.
**Enhanced (≤3):** `migration` (the symmetry sentence: import proves what
arrives, export proves what leaves, one discipline read twice);
`mapping-profiles` (the outward profile beside the inward ones, or the honest
note that profiles are inward-only today); `crosstab` or `report` (the existing
export menu's EQuIS entry backed by the estate record instead of dangling).
**Seed:** one deterministic export record — the 2026-Q2 groundwater round as an
EQuIS-compatible EDD: counts computed from the manifests and grid the seed
already holds, the outward mapping table, the loss statement (the qualifier
channel and dispositions the format cannot carry are the honest candidates),
and a reconciliation that proves the counts match the record.
**Constraints:** the keep-list; no new figure family; counts computed;
deferrals recorded in-repo; the accumulated deferral records untouched; G-76b
inherited.
**Acceptance:** FR-7.6 flips to drawn-as-proposed with the S8 marking intact;
the estate answers "what crosses the boundary and on whose terms" for every
format the seed knows; the export's reconciliation and loss statements recount
from raw literals; build, verify and the §11 loop green.

## Wave 11 — External-party access (OD-2) — planned 2 September 2026

**Objective:** the dimension the PRD deliberately kept as a seam, drawn as that
seam — a consultancy engaged on one project, scoped by the same project
boundary RLS already enforces, without inventing the multi-client identity
model v1 refuses (PRD §14 OD-2: "the external-party dimension is added at S8
when a consultancy customer is real"; §7's test is that the IA accommodates it
without route restructuring, and this wave is that test run).
**New screen (1):** the external-party engagement — the party as a record (an
obviously-fictional consultancy), the engagement with its scope stated in the
role vocabulary the register already holds (a Reader or Contributor binding, on
one project, through its own directory group), a sponsor inside the operator
who owns the engagement, an end date the access dies with, and the boundary
drawn honestly: what the party sees is the project's data through the same
scoping every internal principal gets — nothing bespoke — and what they cannot
see is everything the boundary excludes, stated rather than implied. The
screen also states what v1 does not have (no consultancy identity model, no
cross-client view for the consultant) and why that is a decision.
**Enhanced (≤3):** `roles` (the external binding beside the internal ones,
visibly time-bounded and sponsor-owned); `project-home` (the engagement
visible where a project manager would look for who else is in here); `audit`
(an external principal's action attributed like any other and distinguishable
— the trail answers "what did the consultants touch" without a special
mechanism).
**Seed:** one MOCK-convention consultancy, one engagement (scope, sponsor,
granted/expiry dates), one or two external principals with bindings, one
recorded action in the audit trail. Counts computed; the wave-5 membership
structures extended, never forked.
**Binding on this wave's audit round (carried from the wave 10 cap):**
re-verify W10-A-6's post-cap fix — the format inventory's `Fields: 42` and
`21` against the spec's 30 + 12 and the fixture's 21-column header — before
this wave can close.
**Constraints:** the keep-list; glossary vocabulary (reader/contributor,
principal, directory group — "engagement" anchored or recorded); no new
figure family; deferrals recorded in-repo; G-76b inherited.
**Acceptance:** the engagement walks from party to binding to boundary to
audit by link; nothing implies a route restructure; OD-2's coverage row flips
to drawn-as-proposed with the S8 marking intact; build, verify and the §11
loop green — including the W10-A-6 re-verification.

## Wave 12 — Configuration portability and the estate view (OM-5, OM-2) — planned 2 September 2026

**Objective:** the two `partially` rows in the operations domain closed by
drawing what each says is missing — the package surface OM-5's row names, and
the estate view OM-2's row places out of frame. The second is the delicate one:
it is the **vendor's** surface, not MOCK-WDL's, and drawing it means shifting
frame honestly rather than sliding a vendor screen under a customer's chrome.
**New screen (1): the configuration package** (customer-side). Export and
import of what OM-5 enumerates — criteria libraries, report templates, EDD
format definitions — as versioned packages: what a package contains (each item
with its own version, the "configuration, never code" rule already on
`#formats`), provenance on import (who built it, from which instance, when),
and the import drawn deciding something: a diff against what is in force, the
impact stated in the catalogue's own discipline (a criteria change previews
what it would re-evaluate — wave 3's machinery, referenced not redrawn), and
activation as a decision separate from arrival. Nothing imports silently.
**New screen (2): the estate view** (vendor-side, and saying so). N
deployments with version, upgrade window, backup state and support posture —
the R4 question ("version drift is the primary failure mode") answered from
the records a no-telemetry vendor actually has: the releases it cut, the
deployments it performed, and the diagnostic bundles customers chose to send
(DR-2 — data ages honestly, "as of the bundle of <date>", never a live feed).
**The frame shift is the design problem:** the screen states whose surface it
is, its principal is not A. Nakamura, nothing implies a customer can reach it,
and the catalogue's chrome must not silently claim otherwise — how that is
drawn is the wave's judgement call, made explicitly and recorded. The OM-2
coverage note ("out of frame here") gets a dated correction, not a rewrite.
**Enhanced (≤2):** `instance` (the deployment's own version panel gains the
package and estate links where they belong); `criteria` or `report` (the
in-force version naming the package it arrived in, closing the portability
loop from the receiving end).
**Seed:** one package (contents versioned per item, provenance, a diff with
one consequential change), and a small vendor estate (a handful of MOCK
deployments — Wandalup among them at its known version — with dated bundles).
Counts computed; every date derived or dated on its face (the two-clocks
lesson).
**Constraints:** the keep-list; DR-2/DR-3 absolute (no telemetry, no relay —
any sentence about how the vendor knows something must name a no-telemetry
mechanism); glossary vocabulary with recorded decisions; no new figure family;
deferrals recorded in-repo; the eight standing deferral records untouched;
G-76b inherited.
**Acceptance:** OM-5 and OM-2 flip to drawn-as-proposed with their honest
notes; the import walks arrival → diff → impact → activation by link; the
estate answers R4's question with every value dated to its source; build,
verify and the §11 loop green.

## Wave 13 — Deferral settlement (accumulated scope) — planned 2 September 2026

**Objective:** the deferred bucket is spent, so the pipeline turns to the debts
its own record has accumulated — the four standing deferrals that are payable
by derivation and alignment, taken together because each is the same move:
replace a described disagreement with one source read N times. No new screens.
**Scope (4 settlements):**
1. **The bundle-contents gap** (wave 12's record): one derived contents list —
   the diagnostic bundle's categories with the configuration inventory's
   per-item versions and checksums — that `#diagnostics` and `#entitlement`
   both read, so the estate's configuration column rests on a field the
   catalogue describes. Remove the warn card on `#estate` and the two seed
   deferral records, replaced by short landed-notes.
2. **The two clocks, named on every face** (wave 11's record, wave 12's
   OPS_AS_AT): every date-bearing surface states which clock it reads;
   `ENTITLEMENT.remaining` derives from OPS_AS_AT instead of typing 130 days;
   the MEMBERS last-seen values and the other OPS_AS_AT literals keep their
   dates but gain the clock statement where they render. Moving AS_AT itself
   is deliberately NOT this wave — re-deriving every countdown in the
   catalogue is its own change with its own blast radius, and the honest
   settlement is that no reader can mistake which as-at a number is on.
3. **`#imports`' three literal stat tiles** (wave 8's record): derived off
   `IMPORTS` now that runs carry `kind` — deciding and stating what "this
   quarter" and "held" mean across an EDD run, a migration run and a logger
   download; the wave-8 "contributes 0 to all three" reasoning becomes a
   computed fact instead of a protected assumption.
4. **The criteria library's matrix column** (wave 9's record): the rows gain
   a real matrix field (glossary words), the receptor/class conflation is
   drawn as what each row actually is, and `#composite`'s "0 soil sets"
   becomes right by construction — the filter computes over the new field.
**Not this wave, restated so the record stays honest:** the WATER_LEVELS
baseline (crosses a licence trigger; needs its own deliberate wave), the PFAS
three-way (interacts with the non-detect demonstration), the DQO matrix
dimension and the qualifier scheme gap (both cross-screen design decisions),
and the depth-profile plate (gated on the FP-1 verdict).
**Constraints:** the keep-list; no new figure family; every settled deferral's
record replaced with a dated landed-note, never silently deleted; the five
unsettled records untouched; census counts character-identical; G-76b
inherited.
**Acceptance:** the four disagreements each resolve to one source read N
times, measured by the audit's own reference checks; the deferral ledger
shrinks from nine to five with the history preserved; build, verify and the
§11 loop green.

## Wave 14 — The WATER_LEVELS baseline, re-anchored (wave 8's record) — planned 2 September 2026

**Objective:** the largest standing lie in the seed settled on its own terms.
The monthly water-level series was shaped (`base = loc.toc − 12.5`) to spread
three hydrograph traces apart and never reconciled with the field record; the
deferral's own six-bore table shows the dip-derived and fitted columns agreeing
within 0.2 m while the drawn series sits metres away. Its record said the
repair "needs its own deliberate wave" — this is that wave, alone, because the
blast radius is every surface that reads a water level.
**The decision, taken in the plan so the wave executes rather than invents:**
re-anchor `WATER_LEVELS` to the field record's baseline (the majority
evidence: two of three columns agree), preserving each bore's drawn *shape*
(the seasonal structure, the wet-season recoveries, the MW05 decline) while
correcting its *datum* — the series' internal variation is fiction doing its
job; its absolute level is fiction contradicting the record, and only the
second is the defect.
**The consequence, drawn rather than dodged:** re-anchored, MW05 sits ~4 m
ABOVE the 195.6 m AHD licence trigger level rather than below it. That is not
an accident to smooth over — it coheres with the seed's own story (the TSF
seepage narrative, the mound the interpretation describes) — so the wave draws
what a level-trigger exceedance obliges: the trigger drawing on the hydrograph
tells the truth, and every surface that states MW05's level against the
trigger (exceedances, TARP, obligations, the licence screen, the narrative's
evidence set) is enumerated and made consistent, with the new exceedance
carried as the record it would be — or, if enumeration shows the 195.6 trigger
was itself typed to sit above the old wrong series, the trigger level is the
thing corrected, with the licence record's own provenance deciding which. The
implementer measures first, decides against the evidence, and records the
decision — the one thing it must not do is leave the level story and the
chemistry story disagreeing about which side of the line MW05 is on.
**Blast radius, enumerated up front (verify each):** the hydrograph (Figure
4.1 and its trigger line), the logger plate's beside-not-on relationship
(wave 8 anchored it to the field record — after this wave the two plates
SHOULD share a baseline, and the "why this is a second plate" panel must be
re-trued or retired), `HEADS`/the potentiometric fit (already field-anchored —
must now agree), the water balance, trend/statistics surfaces reading levels,
`#location`'s derived elevations, and the wave-8 deferral record itself
(settled-note, original preserved).
**Constraints:** the keep-list (reported-then vs record-now: past rendered
levels in issued-report contexts are handled by the catalogue's own
supersession honesty, not silent rewrite); no new figure family; counts and
levels computed; census character-identical; the four remaining deferral
records untouched; G-76b inherited.
**Acceptance:** the six-bore table's three columns agree within the fit's own
residuals; the hydrograph, logger plate, fit and dips tell one level story;
the trigger consequence is drawn end to end; the ledger shrinks five → four
with history preserved; build, verify and the §11 loop green.

## Wave 15 — The PFAS three-way, reconciled (wave 7's record) — planned 2 September 2026

**Objective:** the oldest standing deferral settled in wave 14's shape —
measure first, decide against the evidence, draw every consequence. Four
readings of how far the PFAS suite reached on 2026-Q2 disagree: the batch
states 4 samples; the manifest gives the PFAS tests to one sample
(WDL-26Q2-003, 16 tests and 5 containers against the round's 14 and 4); the
crosstab carries a PFOS + PFHxS total at 6 locations; and the `2026-Q2-PFAS`
event in `EVENTS` reads differently again. All four predate wave 7, which
recorded rather than chose because the cheapest reading empties four cells of
the row `#criteria` demonstrates the non-detect rule over.
**The method, not the answer, fixed in the plan:** enumerate what each
reading rests on — the batch's sample list against the manifest's containers
and travelling QC (a batch of 4 may reconcile with one primary plus its
field blank, trip blank and duplicate, which would make two of the four
readings the same fact described twice), the certificate, the custody chain's
PFAS bottle, and the crosstab's six cells' own provenance (a censored `<2.0`
asserts an analysis happened — if no analysis did, those cells are the
trigger-line class of scenery and become not-analysed, which is a different
mark and a different sentence). Decide against that evidence, record the
decision and its measurements in the seed, and reconcile all four surfaces to
one source read four ways.
**The consequences, enumerated up front (verify each):** `#criteria`'s
non-detect demonstration (the MW05 sum must survive on whichever reading
wins — it is the one analysis every reading agrees happened);
`PFAS_COMPONENTS`/`PFAS_LIMITS`; `CROSSTAB_SHAPE`'s censored count and
`INDETERMINATE` (11 = cadmium 6 + PFAS 5 — if PFAS cells become
not-analysed, both move, and every surface derived from them re-derives:
`#indeterminate`'s costs, `#data-states`, the golden checks, `#exchange`'s
reconciliation rows); the lineage's PFAS chain (wave 7's — unaffected in
substance, verify); the dictionary. Every moved value renders beside its
before.
**Constraints:** the keep-list (a censored value is a laboratory's own
notation — it is never invented, and removing an invented one is recorded);
no new figure family; counts computed; census character-identical; the three
remaining deferral records untouched (DQO matrix dimension · qualifier
scheme gap · depth-profile plate); settled-note in the established shape,
ledger four → three; G-76b inherited.
**Acceptance:** one source, four surfaces, zero disagreement; the non-detect
demonstration stands; every moved count recounted; build, verify and the
§11 loop green.

## Wave 16 — The DQO's matrix dimension (wave 9's record) — planned 2 September 2026

**Objective:** the deferral's own words are the spec: "giving the DQO an
applicability dimension by matrix, with an effective date and a version …
the same class of deliberate change as adding a criteria set: it belongs to
a wave that redesigns `#qc-limits`." This is that wave.
**The change:** the data quality objective library gains matrix
applicability in the glossary's five words — each limit row states which
matrix it was written for, with an effective date and a version, versioned
the way the criteria library and the DQO's own 2025.2/2026.1 pairing
already are. The water limits keep their history (they were written for
water and have judged water rounds; nothing about their past changes); the
soil investigation's duplicate is then judged honestly: either a soil
limit exists in the library with its own provenance (a conventional wider
RPD for a heterogeneous matrix — anchored to what the seed's own record
can support, never invented with false authority: if no source in the
record supports a number, the honest state is "no soil limit in the
library — judged against the water limit, stated as stricter", which is
what the record says today, now as a computed fact on a matrix-aware
library rather than a comment), or the gap renders as a first-class row.
Measure first: what the record supports decides which.
**The consequences, enumerated up front:** `#qc-limits` (the redesign
target — the library table gains the matrix column and applicability
statement; the 2025.2/2026.1 version machinery must compose with it);
`#composite` and the soil manifest (the matrixGap notes become the
settled record); `#qc`'s findings if any state a limit's scope; `#dqa`'s
dimensions if they cite DQO rows; the wave-9 duplicate RPD (7.1% —
verify which limit judges it after the change and that the verdict is
stated with its limit's own matrix); `#exchange`/`#package` if either
enumerates DQO content. Every moved statement records its before.
**Constraints:** the keep-list; no new figure family; counts computed;
census character-identical; settled-note in the established shape, ledger
four → three; the two standing records untouched (qualifier scheme gap ·
depth-profile plate); G-76b inherited.
**Acceptance:** every limit row answers "for which matrix, since when,
under which version"; the soil duplicate's verdict names the limit and
matrix that judged it; no comment carries what the library now states;
build, verify and the §11 loop green.

## Wave 17 — The qualifier's scheme, and the origin the deferral did not name (wave 10's record) — planned 3 September 2026

**The debt.** Wave 10 recorded, on the export screen and beside the
`schemeGap` derivation: *"The QA/QC register records each qualifier's basis
and not its scheme, and an export is the first surface that needs the
second."* It named four affected codes (`J` and `T` applied to 10 results
between them, `L` and `B` proposed and unapplied), named a fifth (`U`, on
`#result-detail`) the same field would have to classify, and deferred
deliberately: *"a seed change with four screens on the other end of it and
a judgement in the middle, not a column to add in passing."* It is the last
deferral in the ledger that can be built without a decision of Jerry's.

**Measured before planning, and the measurement moved the wave.** Four
readings, each reproducible:

1. **The gap is two fields, not one.** `docs/GLOSSARY.md` makes *qualifier
   scheme* (USEPA · ESdat · Strataflow) **and** *qualifier origin*
   (laboratory · manually) separate first-class terms. The export rule the
   deferral quoted — a Strataflow reason code *"must not be exported as
   though a laboratory had said it"* — turns on **origin**, which the
   deferral did not name. Scheme says which vocabulary a letter is drawn
   from; origin says who asserted it; the export needs both and the register
   carries neither.
2. **Wave 10's own expectation is half contradicted by the authority.**
   That note says *"`J` and `B` read as USEPA, `T` and `L` are the ones a
   real decision would have to be taken about"*. `docs/reference/esdat-edd-spec.md`
   names the codes `Lab_Qualifier` may carry: **`U, J, J+, J-, R, UJ`**,
   semicolon-separated. `J` and `U` are in that set; **`B` is not**, and
   neither are `T` and `L`. So the wave-10 guess was right about `J` and
   wrong about `B`, measured against the spec rather than against
   familiarity — and that correction is the wave's first recorded before.
3. **The concept is already modelled twice, under two words, and the
   glossary's word is neither of them consistently.** `PFAS_LIMITS` carries
   `scheme: 'USEPA'`, `qualifier: 'J'`, `origin: 'laboratory'` with the
   sentence *"The laboratory said it; nobody here inferred it."*
   `RESULT_DETAIL.qualifiers[0]` carries the same idea as
   `source: 'laboratory'`. The register carries neither. Three records, one
   concept, three vocabularies — the "one source read N times" move this
   pipeline keeps making, and QB-9/ADR-0009 decide the word: **origin**.
4. **The same letter, twice, from two origins.** `J` appears on the PFAS
   result (reported by Yarra Regional on the deliverable) *and* on `FD-2`
   (assigned by this instance's field-duplicate rule against the project
   DQO). Same letter, same meaning, different asserter — and today nothing
   on any screen tells them apart. That collision is the concrete case the
   glossary's export rule was written for, and it is already in the seed.

**What the wave builds.**
- **The register gains `scheme` and `origin`**, each carrying the evidence
  that decided it rather than a bare assignment, on one qualifier record the
  five codes are read from — `#qc`, `#qualifiers`, `#lineage`,
  `#result-detail` and the export screen all reading that one source rather
  than four descriptions of it. `RESULT_DETAIL`'s `source` becomes `origin`
  in the same change (the glossary's word), with its before recorded.
- **Origin is derived, not decided.** For every code the record already
  shows whether a deliverable carried it (the PFAS `J`, the `U`) or a QA/QC
  check in this instance raised it (`FD-2`'s `J`, `MS-1`'s `L`, `EB-1`'s
  `B`, `ST-1`'s `T` — and `MS-1`'s own comment records that the certificate
  *"qualifies no results"*). That is a measurement, and it must read as one.
- **Scheme is decided only where the record decides it.** `U` and the
  laboratory `J` are USEPA on the glossary's own sentence (*"An ESdat
  deliverable's `U` is USEPA's `U`"*) and the deliverable's vocabulary
  travels with it in the spec's `Lab_Qualifiers` block. For the four
  review-raised codes the record shows what raised them and **not** which
  vocabulary the reviewer drew the letter from. Both readings are real — the
  letter borrowed from USEPA with the meaning it has there, or this
  instance's own reason code wearing a familiar letter — and they have
  different export consequences. **Do not pick one silently.** Draw the
  decision as a decision, with its two readings, its consequence each way,
  and its count; the wave-14 trigger, wave-15 custody and wave-16 soil-limit
  precedents all say the same thing and each is cited in the seed already.
- **The export states what it may write, counted.** The `schemeGap` prose
  ("the register does not carry one") is replaced by a per-code statement:
  the code, its origin, its scheme or the fact that the scheme is undecided,
  and whether it may be written into `Lab_Qualifier` — with the reason, and
  the counts derived from the one record. State what is measured: the spec
  **names** six codes for that field; it does not say the field rejects a
  seventh. "Not among the codes the specification names" is the honest
  sentence — *"forbidden by the schema"* is not, and would be this wave's
  own invented authority.

**The consequences, enumerated up front:** `#qc` (four findings state a
qualifier and its basis; each gains scheme/origin from the record, and FD-2
and ST-1 say *assigned by this instance's rule*, not *reported*);
`#qualifiers` (the basis-versus-scheme distinction is this screen's subject
now that both exist); `#lineage` and `#result-detail` (the laboratory side
of the same collision — the PFAS `J` and the `U`); the export/exchange
screen (`schemeGap` → the resolved statement plus the counted residue);
`#dqa` if it counts qualified results. Every moved statement records its
before, in the seed **and** on the face that changed.

**Constraints:** the keep-list; no new figure family; every count computed
from the one record and none typed; the 23 Aug census five state counts
character-identical; the settled note in the established shape quoting the
original deferral verbatim, ledger **three → two**; the two standing records
untouched (depth-profile plate · narrative blanks); G-76b inherited; no
number, code or vocabulary asserted that the record does not support.

**Acceptance:** every qualifier in the instance answers *which vocabulary,
who asserted it, and may the export write it* — or says, as a first-class
drawn decision with its readings and consequences, that the record does not
yet decide the first; the same letter `J` is visibly two different
assertions with two different fates; no comment carries what the record now
states; wave 10's `B`-reads-as-USEPA expectation is corrected against the
spec with its before quoted; build twice byte-identical, verify's nine
families clean, and the §11 loop green at zero P0/P1.

## Wave 18 — The brief becomes the seventh enumeration, and the saved views become one record — planned 3 September 2026

**The wave.** Two things, and they belong together because both are about a
claim being checkable rather than asserted. The vendor requirements brief
(`VENDOR_REQUIREMENTS.md`, committed on D12) becomes a closed enumeration on
`#coverage`, which is deliverable #2 of the brief's own list. And the
saved-views defect — found during the assessment and re-measured before it was
recorded — is fixed as the wave's rider.

### Half one · the seventh enumeration

`#coverage` reconciles six closed enumerations today and refuses to claim a
screen for a behaviour-shaped requirement. The brief becomes the seventh, held
to the same standard.

**The rows are a closed list taken from the brief itself** — one per numbered
subsection from §4.2 through §14.4, plus §15's lineage questions, §16's five
scenarios, §17's ten screen states and §20's completion criteria. Every row
carries the brief's own section number and its own words for what it asks.

**Nothing is typed that can be derived.** The verdicts, the screens each row
cites, and the notes live in one seed record; the screen computes every count
from it — per verdict, per priority, per section. A row's verdict is
`covered` / `partially` / `not`, the same three the other six enumerations use,
and the note says what is missing in the same voice.

**The cross-check that makes it honest, and it is new.** Several verdicts sit
on screens the register marks `proposed` or `not built`. The enumeration
**resolves every screen it cites against the register** and says so: a row
covered by a proposal reads as covered *by a proposal*, counted separately.
Fourteen of the 75 screens are proposals and eight of them carry this brief's
weight, so this is not a hypothetical — it is most of what a reader would
otherwise misread. `#coverage`'s existing "What a green row here does not
mean" section gains the sentence this makes true.

**The scenarios are walked, not asserted.** A–E carry the result of the walk
already measured: how far each got, which hop broke it, and what would
complete it. Scenario B is satisfied end to end; C breaks at its first hop
because no route runs from an issued report to the result rows it stands on;
E is not walkable. Those are findings, and they render as findings.

**Where this brief and the product's own requirements disagree, the row says
so.** Four such rows exist and all four are now settled decisions (D1, D4, D5,
D6) — each cites its decision ID and its verdict, so a reader can see that the
map workspace is drawn under a restated NG4 rather than in defiance of it.

### Half two · the saved-views rider

**The defect, re-measured and upheld.** `#saved-views` holds four typed literal
rows in `screens.mjs`. `seed.mjs` holds a different four-row list for the same
objects, rendered on `#crosstab`. Only one name is common — *TSF downgradient
— metals* — and the two disagree about what it selects (*6 metals · rolling 3
years* against *Dissolved metals (8) · Rolling 8 quarters*), about its owner
and about its last-used date. Its claimed downstream (*Figures 4.2, 4.6, 4.7*)
does not match what `#report-figures` cites. Three surfaces, three answers,
about the object §4.5 asks to be governed.

**The fix is one record read three times, and the disagreements are settled by
measurement where the record settles them.**
- The **downstream is derived**, not typed: filter the report items for those
  whose source names the view. That resolves the figure-list disagreement by
  computation rather than by choosing.
- The **analyte count is corroborated**: `#crosstab`'s own suite selector
  carries *Dissolved metals (8)*, so the seed's 8 is evidence and the screen's
  *6 metals* is the drift. Record the before.
- The **period disagreement has no third source.** Measure first: if nothing
  else in the record constrains *rolling 3 years* against *rolling 8
  quarters*, do not choose silently — state the disagreement on the screen with
  both readings and which record each came from, in the shape wave 15 used for
  the PFAS three-way. Inventing a resolution here would be the same trade this
  catalogue has refused four times.
- Every corrected value records its before in the seed **and** on the face that
  changed.

**Consequences enumerated up front:** `#saved-views` (rebuilt from the record),
`#crosstab` (its view table reads the same record), `#report-figures` (its
source citations resolve to the record rather than to a string), and
`#coverage` if any journey row cites saved views.

### Constraints and acceptance

**Constraints:** the keep-list; no new figure family; **no figure work at all —
the print test gates that (D13)**; every count computed and none typed; the 23
Aug census five state counts character-identical; zero `state:` lines in the
`screens.mjs` diff; the deferral ledger untouched at two standing records; no
number, screen state or verdict asserted that the record does not support.

**Acceptance:** every numbered section of the brief has a row with a verdict, a
screen list resolved against the register, and a note; every count on the screen
recomputes from the one record; a row covered by a proposal says so and is
counted separately; the four decided conflicts cite their decision IDs; the
saved views are one record read by three surfaces with every before recorded and
the unresolvable disagreement stated rather than settled; build twice
byte-identical, verify's nine families clean, and the §11 loop green at zero
P0/P1.

## Wave 19 — The governed dataset: a population that can be named, versioned and cited (D1) — planned 3 September 2026

**The keystone.** Two of the four assessments reached this independently from
different sections: **§4.3, §4.4, §4.5, §5.6, §6.4, §9.5 and §12.2 are each
asking, from a different direction, for one object** — a population that can be
selected, named, versioned, owned, carried between workspaces, and cited by a
figure years later at the exact version used. D1 accepted it. Wave 18 made the
saved views one record; this wave promotes that record into the object the brief
is asking for, and the waves behind it (the explorer, the analysis object) build
on it rather than beside it.

**The contradiction to draw rather than hide.** A live query and a frozen dataset
are opposite things, and this product's discipline is that an outcome freezes onto
the record at evaluation date and a licence varied next year moves nothing already
decided. Both failure modes are already named by the catalogue's own arguments:
build the population as session state and *"two people asking for the compliance
boundary numbers get two questions"* — the disagreement `#saved-views` says the
product exists to end; build it as a snapshot and it diverges silently, and a stale
figure becomes undetectable, which is exactly what `#report-figures` catches today
for certificates and would lose for datasets. **Draw the seam.** A dataset being
refined is live; a dataset a figure cites is frozen at a version; the transition
between them is an act with a record, not a save button.

**The trap this catalogue is uniquely exposed to.** A population is **not a row
set.** The crosstab's most careful work is that *dry*, *not analysed*, *composited*
and *not evaluated* are four different absences and none is a pass. A dataset that
filters to "validated results only" and quietly drops those cells destroys the
distinction the whole grammar rests on. **The population readout counts what was
excluded and why, per kind** — the shape `#crosstab` already prints in one line
(*61 results (was 66) · 11 cells at a bore that returned nothing · 5 where the
suite was never run*), made first-class and computed.

**D10 governs one of those kinds and is already decided.** A
proposed-but-unapplied qualifier is **inside** the population — nine zinc results
are in exactly that state — and the dataset says so where it is read. Excluding
them would be the product taking a decision the practitioner has not.

**What the object carries.** Name; a stated purpose, which no view has today; the
definition as an **inspectable set of dimensions** rather than a prose cell;
creator and current owner, kept apart; a version; a lifecycle — draft, shared,
approved, superseded — modelled the way `#criteria` and `#qc-limits` already model
one, not invented; variant lineage, so a variant names its parent and neither
overwrites the other; and its downstream dependents, derived rather than typed as
wave 18 established.

**Two seams it must close, both measured as broken.**
- `#report-figures` cites a dataset **at a version**, and staleness derives from
  the dataset version as well as the certificate. Today the Source cell is text.
- `#lineage` gains the missing node. Its 21-hop chain jumps from evaluation to
  consequence with **no query or dataset hop at all**, which is why §9.5's chain
  has no first link.

**A recorded boundary that is honoured, not overturned.** `#search` states:
*"Search covers identifiers and names, not result values — a number is found
through the crosstab's filters, where the criteria set and period that make it
meaningful are also chosen."* The dataset reaches values through a definition that
carries a criteria set and a period. It does not become a free-text box.

**The PRD amendment is Jerry's, and the screen says so.** D1 accepted the
capability *and* the amendment that carries it. This repository cannot amend the
PRD, so the object is drawn with the amendment recorded as pending — which is what
`#saved-views` already says and must keep saying until it lands.

**Consequences enumerated up front:** `#saved-views` (becomes the dataset
register), `#crosstab` (reads a dataset, and its edit chips become a variant),
`#report-figures` (versioned citation, staleness from the dataset), `#lineage`
(the new hop), `#coverage` (the §4.5 and §9.5 rows move, and the enumeration is
build-checked so they must move honestly), and `#statistics`/`#hydrograph`/
`#background` if any names its population.

**Constraints:** the keep-list; **no figure work of any kind — the print test
gates it (D13)**; every count computed and none typed; the 23 Aug census five
state counts character-identical; zero `state:` lines in the diff; the ledger at
two; no lifecycle state, owner or version asserted that the record does not
support; every moved statement records its before.

**Acceptance:** a dataset answers *what it selects, who owns it, which version,
what state, what it excludes and why, and what depends on it*; the live/frozen
seam is drawn as an act with a record; the exclusion accounting counts all four
absences by kind and puts proposed-but-unapplied qualifiers inside with the reading
stated; a figure cites a dataset at a version and goes stale when either the
dataset or the certificate moves; `#lineage` has a dataset hop; build twice
byte-identical, verify's nine families clean, the vendor enumeration still closed
by the build, and the §11 loop green at zero P0/P1.

## Wave 20 — The explorer over the dataset, and the crosstab becomes one view of it (D1, D11) — planned 3 September 2026

**What it builds.** §4.2's dimensions, §4.3's progressive construction and
population readout, and §4.4's six representations over one population — all of
it over the governed dataset wave 19 built, not beside it.

**The constraint that shapes the whole wave: this catalogue is static HTML.**
`build.mjs`'s only script does screen navigation, a rail filter and slide-overs.
Nothing filters live, so §4.3's *"show how the resulting population changes as
filters are applied"* is delivered as **a sequence of linked drawn states**, each
carrying its own population readout — which is what §17 asks a mockup for, and
what the vendor response already recorded as the deliberate reading. **Say so on
the screen.** A drawn sequence that implies working filters would be the one
claim this catalogue has never made.

**The population readout is wave 19's exclusion accounting, made the spine.** At
every step: what remains, what left, and **why it left, per kind** — the four
absences are not interchangeable and none is a pass. A step that narrows to
"validated results only" and silently drops the dry cells destroys the
distinction the whole grammar rests on, so the readout counts them out loud.

**§4.3's worked scenario is the wave's honesty test.** The brief asks for
*groundwater → Unit C → dissolved metals → arsenic and manganese → 2021–2026 →
validated only → rejected excluded → nominated criteria*. The assessment measured
that six of its eight terms cannot be expressed and that **neither manganese nor
a "Unit C" exists in this seed**. Two exits, and only one is honest here:
- **Do not seed them.** Adding an analyte means inventing fourteen rounds of
  results at seven bores, a reporting limit and a criterion, to make an example
  runnable — and this catalogue has refused to invent a record six times, most
  recently for a soil limit and a licence trigger.
- **Walk the scenario the record can express**, with the site's own equivalents
  named as equivalents, and **enumerate the terms it cannot express and why** —
  hydrostratigraphic unit is an attribute and not a facet; manganese is not in
  the dictionary; the record begins in 2023, not 2021. Counted, not apologised
  for. That is the same move `#composite` and `#qc-limits` already make.

**§4.4's six representations, over one population.** Table, crosstab, time series,
statistics, map, exceedance — each stating the population it was run over and the
version it read. The active population is visible in every one, which is the
requirement's actual demand and the thing `#coverage` measured as absent: today
every representation re-declares its own scope in prose and none inherits.

**D11 is decided and this wave executes it.** `#crosstab` becomes one
representation: **it keeps its cell grammar, its four absence states and its
keyboard contract, and loses its private filter bar.** It is one of only four
screens a practitioner has read, so the change goes onto the walk (D13) rather
than being treated as settled — say that on the screen.

**A recorded boundary that is honoured, not overturned.** `#search` states that
*"Search covers identifiers and names, not result values"*, because a number is
found through a query that also chooses the criteria set and period that make it
mean anything. The explorer reaches values that way. It does not become a
free-text box, and the screen says which of the two it is.

**Consequences enumerated up front:** a new explorer screen (registered
`now:'proposed'` with an `added:` date and **no `state:` field**, per §5);
`#crosstab` (demoted, grammar intact); `#saved-views` (a query becomes a dataset
by an act already drawn); `#exceedances`, `#statistics`, `#hydrograph`, `#map`
(each names the population it ran over); `#coverage`'s §4.2/§4.3/§4.4 rows, which
the build's enumeration guard will hold to honesty.

**Constraints:** the keep-list; **no figure work — D13's print test gates it**;
every count computed; the 23 Aug census five state counts character-identical;
zero `state:` edits; the ledger at two; the vendor enumeration still closed by the
build; no dimension claimed as expressible that the record cannot express.

**Acceptance:** a population can be built through a drawn sequence with the
readout changing at each step and the exclusions counted by kind; six
representations name one population and its version; the crosstab keeps its
grammar and loses its filters; the brief's own scenario is walked as far as the
record allows with every unexpressable term named and counted; nothing implies a
live filter; build twice byte-identical, verify's nine families clean, and the
§11 loop green at zero P0/P1.

## Wave 21 — The analysis object, and the population under a graph (§9.3–9.5, D10) — planned 3 September 2026

**The chain's first link.** §9.5 requires every analysis to be traceable through
*query → included observations/results → QA/QC state → analytical settings →
output*. The assessment measured that **no analysis object exists**, so the chain
has nothing to start at: `#lineage` traces *one result* through twenty-one hops,
which is a different shape from *n results through one method to one output*.
Wave 19 built the population and wave 20 built the explorer over it. This wave
builds the thing that reads a population, applies a method, and produces an
output that can be cited.

**What an analysis carries.** The dataset and **the version of it** that was read;
the method and its parameters; the population as at the moment of computation,
with **each member's QA/QC state at that moment** — not as it reads today; the
exclusions, by kind, in wave 19's accounting; the output; and what depends on the
output. An analysis that cannot say which of its inputs carried an open finding
is a graph with an assertion under it.

**D10 is decided and rides here.** A proposed-but-unapplied qualifier is **inside**
the population — the nine zinc results are exactly that — and **the plate says
so**. Excluding them would be the product taking a decision the practitioner has
not; including them silently would be the product hiding one. The count belongs
to the register (results in a batch) and not to the grid (cells in a round), a
distinction wave 19 established and this wave must not blur.

**`#hydrochem` is the second half, and it is the weakest analysis screen in the
catalogue by measurement** — roughly a tenth of `#hydrograph`'s body, with no
configuration block, no table view, no exclusion list, no population count, no
per-sample meq/L conversion, and no grouping or symbolisation control. The
charge-balance result reaches it only as a caption. The brief's own sentence is
the standard: *"A graph without an inspectable underlying population is
inconsistent with Strataflow's defensibility proposition."*

**What that half may and may not do.** It gains what `#hydrograph` already has —
configuration saved with the figure rather than the session, a table view, the
excluded records named, the population counted, the unit conversions shown per
sample. It **does not gain a plate**: FP-1, FP-2 and FP-3 are approved but D13
gates every figure family behind a print test that has not happened, and
`figures.mjs` is untouched. This wave makes the existing plates inspectable; it
does not draw new ones.

**§9.4's measured absences, and the one that is a broken promise.**
`#result-detail` links *"Summary statistics — Kaplan–Meier, carried as censored,
never substituted"* to `#statistics`, and **`#statistics` contains no summary
table** — a link pointing at nothing. Percentiles exist once, on `#background`,
with no facility behind them; minimum and maximum are drawn and never stated as
numbers; **detection frequency appears nowhere in the repository**; exceedance
frequency is a consecutive-run count, which is a different measure. Each is
either supplied from the record or **counted as absent with the reason** — the
same choice every wave since 14 has faced, answered the same way.

**A thing the record does not hold, and must not gain by invention.** §9.3's last
bullet asks for water type or facies. *Calcium-bicarbonate* and *sulfate-dominated*
are sentences in a prose panel; there is no classification, no rule and no
per-sample assignment. Measure whether the ion table supports deriving one. If it
does, derive it and say from what. If it does not, **count the absence** — do not
assign a facies to six bores because the words are already in the prose.

**Consequences enumerated up front:** a new analysis surface (registered
`now:'proposed'`, an `added:` date, **no `state:` field**); `#hydrochem`
(inspectable); `#statistics` (the summary table its own inbound link promises, and
the population it runs over); `#background` (percentile provenance); `#lineage`
(the analysis hop joins the dataset hop wave 19 added); `#report-figures` (a
figure cites an analysis, which cites a dataset at a version); `#coverage`'s §9.3,
§9.4 and §9.5 rows, held honest by the build's enumeration guard.

**Constraints:** the keep-list; **no figure work — `figures.mjs` untouched**;
every count computed; the census five state counts character-identical; zero
`state:` edits; the ledger at two; nothing asserted that the record does not
support; every moved statement records its before.

**Acceptance:** an analysis names its dataset and version, its method and
parameters, its population with each member's QA/QC state as at computation, its
exclusions by kind, and what depends on it; the nine proposed-qualifier results
are inside and said to be; `#hydrochem` can be inspected — population, exclusions,
conversions, configuration and a table view; the promise `#result-detail` makes to
`#statistics` is either kept or withdrawn with its before recorded; §9.4's
absences are supplied or counted; facies is derived or counted, never assigned;
build twice byte-identical, verify's nine families clean, the vendor enumeration
still closed, and the §11 loop green at zero P0/P1.
