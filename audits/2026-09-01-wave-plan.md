# Wave plan — 1 September 2026

Phase 4 of `EXPANSION_BRIEF.md`. Waves are ≤ 8 screens or ≤ 1 week, whichever is
smaller; ordering follows the brief (import → report → obligations → field → figures →
water → configuration; deferred scope last). Every wave ends with `node build.mjs`
green and the §10 sweep green at 375, 1000 and 1280 px, and then the independent audit
loop (§11) before the next wave starts. Sources: the pass-4 findings note and the
extended `#coverage` matrix — a wave item without a matrix row behind it does not
belong here.

**Recommended first wave: Wave 1.** Nothing else should land on top of screens that
measurably overflow and prose that states falsehoods.

---

## Wave 1 — Make the drawn record true and sound

**Objective:** the catalogue stops contradicting its own measurements. No new
screens; no new capability.
**Journeys advanced:** all, as integrity — every walk crosses these screens.
**Screens modified:** the thirteen with measured document overflow (`crosstab`
+519 px at 375, `qc-limits`, `hardness`, `hydrograph`, `imports`, `entitlement`,
`programme`, `project-home`, `projects`, `dqa`, `submissions`; `hardness` and
`hydrograph` at 1280 too) — the fix is containment (`overflow-x: auto` matrices with
frozen identity columns, per the viewport strategy), never removal; plus the ~40
screens whose route line still asserts a 23 August state (mechanical refresh from the
register's `now` field — generate the line from register data so it cannot rot
again).
**Tooling:** `verify.mjs` joins the repo — the driven-browser sweep from pass 4
(overflow at three widths, duplicate ids, unnamed controls, heading order) with
`playwright-core` as a dev dependency; the auditor runs the same script.
**Seed changes:** none. **Navigation changes:** none.
**Acceptance:** sweep reports zero document overflow at all three widths; build
checks green; no screen prints a state that disagrees with the register; the README
gains a dated pass-4 section (drawn 23 Aug, re-derived and repaired 1 Sep).
**Regression risk:** low — layout containment and generated prose only.

## Wave 2 — Journey 1 closed: eCOC, and the loop's missing states

**Objective:** the EDD-to-approved-data walk is fully owned, end to end.
**Journeys advanced:** 1 (to covered), 5 (partially).
**New screen (1): `ecoc`** — passes the New-Screen Test: the chain of custody is a
durable legal record with its own lifecycle (created in the field → transferred →
received); `receipt` owns only the laboratory end, and no existing screen owns
creation and transfer. Entry: `events`, `field-capture`; onward: `receipt`,
`imports`. Filed under Sampling events.
**Enhanced (4):** `events` (QC sample taxonomy and composite summary on the round),
`exceedances` (FR-5.2 — a rolling-window condition drawn *deciding an outcome*, not
described), `qc` (FR-4.5 — spike/outlier-vs-history as a raised finding type),
`imports` (row-count statement verified against the F-02 answer).
**Seed:** the custody chain for round 2026-Q2-GW; a window-condition exceedance.
**Acceptance:** Journey 1 renders with no unowned step; coverage rows FR-5.2 and
FR-4.5 flip to covered; sweep green.
**Regression risk:** medium — `events` is on the J1/J5 spine; verify both walks.

## Wave 3 — Journeys 3, 4 and 8 deepened: the report's governance

**Objective:** issuing, amending and re-configuring are drawn as decidable
interactions, not assertions.
**Journeys advanced:** 3, 4, 8.
**Enhanced (6):** `report` (FR-7.5 golden-output comparison state; FR-7.2 corporate
template configuration as a tab), `report-figures` (numbering and cross-reference
states), `snapshot` (regeneration diff against the issued artifact), `certificate`
(amendment impact preview: derivations → exceedances → trigger states → reports, one
chain), `criteria` (J8's test-against-real-records and impact preview drawn deciding
something; FR-2.2's non-detect binding operable), `signoff` (what exactly was
approved, bound to the snapshot).
**Seed:** an amended certificate with a changed outcome; a criteria version pair.
**Acceptance:** Journeys 3/4/8 walk with every step interactive-in-kind; FR-2.2,
FR-7.2, FR-7.5 flip to covered.
**Regression risk:** medium — supersession propagation must stay consistent across
`certificate`, `supersession`, `exceedances`, `snapshot` (§10 cross-screen counts).

## Wave 4 — Water and loads

**Objective:** the one product route the catalogue never drew, drawn.
**Journeys advanced:** 7-adjacent; FR-1.12/FR-5.5/FR-5.6 close.
**New screen (1): `water`** — trivially passes the New-Screen Test: the product
already ships `/projects/:id/water` ("Water take against entitlement",
`reachedFrom: locations`). Extraction against the entitlement year in force,
approaching-limit flags, mass load by reporting period (FR-5.5) beside it.
**Enhanced (2):** `licence` (entitlement conditions link through), `locations` (the
take drawn at the bore it is drawn from).
**Seed:** entitlement, monthly extraction volumes, one approaching-limit month.
**Acceptance:** FR-5.5 and FR-5.6 covered; the Locations section gains `water` in
the by-section view and the partition check still passes.
**Regression risk:** low — additive.

## Wave 5 — The portfolio, honestly drawn

**Objective:** Journey 9 stops being blocked on a one-site seed.
**Journeys advanced:** 9 (to covered).
**Seed workstream (the named prerequisite):** a second `MOCK-` project in
`seed.mjs` — distinct site, small deterministic dataset, byte-identical rebuild
preserved, `MOCK-` naming throughout. This is the wave's real work.
**Enhanced (≤5):** `projects` (two real rows), `project-home` (the second project's
own state), `obligations` (cross-project rows that are actually cross-project),
`search` (results spanning projects), `coverage` (counts re-derived).
**Acceptance:** Journey 9 walks across two projects; no screen shows a count the
seed contradicts.
**Regression risk:** medium — every screen reads the seed; the sweep and the
cross-screen count checks are the guard.

## Wave 6 — Field work depth (gate-dependent)

**Objective:** Journey 5 at full depth — gated on the brief §12 **practitioner
gate**, due after wave 2 and binding before this wave. (The other gate this plan
named is resolved: the **work queue stands as a proposal** — Jerry, 1 Sep 2026,
findings §7 — so assignment and hand-over are designed against it as a proposal.)
The gate that remains: `field-capture`, the QA/QC workspace, the lineage
panel and the interpretation editor are the four invention-heaviest screens in the
catalogue, and this wave deepens the first of them.
**Scope (≤5):** `field-capture` from proposal to designed depth (one-handed 375 px
first), assignment/hand-over per the work-queue decision, `purge`/`receipt`
continuity with `ecoc`.
**Acceptance:** Journey 5 fully owned; coarse-pointer targets pass by hit-testing.

## Deferred — recorded, not scheduled

Telemetry and instruments (Domain K + the deferred journey; PRD S8) · external-party
access (OD-2) · the integration estate (Domain R beyond FR-3.11) · soil/sediment
matrices and composites (FR-1.7) · EQuIS export (FR-7.6, OD-8) · the estate view
(OM-2, vendor-side) · configuration packages (OM-5's import/export surface) · new
figure families (Durov and any addition to the twelve plates — each needs a grammar
proposal put to Jerry first, and every figure drawn inherits the G-76b unprinted
caveat until the §7.4 print test happens).


---

# Re-planned after the practitioner review — 1 September 2026

The review (`2026-09-01-practitioner-review.md`) landed four P0 design changes and
a binding keep-list. Two of the four are workflow redesigns and two are
provenance/interpretation redesigns; splitting them respects the wave cap, and
PR-1 is explicitly a **replace** — the drawn field-capture entry axis was judged
wrong, so Wave 6 stops being a deepening wave and becomes a redesign wave.

## Wave 6 (rescoped) — the field and QA workflow, redesigned to the review

**Scope:** PR-1 — `field-capture` rebuilt around the **bore session** (the round
grid demoted to summary; the session carries SWL → condition → pump → purge/
stabilisation series in line (PR-1c) → final chemistry → samples/QC → filtration/
preservation → CoC → observations → completion), disposition states (PR-1a), the
round-completion preflight (PR-1b); `purge` refolded as the session's series view;
`events` gains dispositions and the preflight summary. PR-2 — `qc` rebuilt around
**Unresolved → Reviewed → Dispositioned → Ready for validation** with the
need-decision counts as the headline (PR-2a), every propagated qualifier stating
its propagation basis (PR-2b), re-run naming its DQO version with
current-vs-used surfaced (PR-2c); `qc-limits` carries the version pairing; `dqa`
reads the dispositions.
**Riders:** PR-3b (the PFHxS unqualified-detect-below-LOR, fixed in seed and
everywhere it renders), the project-settings facility select, the programme
one-day countdown.
**Constraints:** the keep-list, verbatim, plus every §5 convention.
**Acceptance:** the bore session walks one-handed at 375px; the QA screen's
primary question is "what is still mine to decide"; build + verify green; the
review's hesitation points re-walked and closed.

## Wave 7 (new) — provenance and interpretation, redesigned to the review

**Scope:** PR-3a/PR-3c — `lineage` extended backwards through location/version,
construction, programme, field event, sample, custody and receipt, stitched from
the screens that already hold each hop, with **Explain it** (narrative) and
**Trace it** (compact graph) views; `result-detail` carries the same chain. PR-4 —
`narrative` becomes an evidence workspace: pinnable evidence sets per passage
(PR-4a), reference tokens distinguishing renumbering from content change (PR-4b),
an explicit numeric rendering rule (PR-4c).
**Rider (carried from the wave 6 audit, W6-A-1):** blank the crosstab's MW11
column so the grid matches the field record — two visits, found dry, zero
samples — moving the censored counts and every surface computed from
`CROSSTAB` deliberately rather than in passing; then remove the interim
contradiction notice on `#crosstab` and re-true the `#data-states` sentence
to "drawn empty", which the blanked column will have made simply true.
**Acceptance:** the lineage answers the review's list of questions (which bore,
which screen interval, who sampled, filtration, preservation, custody, receipt
temperature, holding time, travelling QC) by link; the evidence set shows a
reviewer exactly what the author saw.

## Waves 8+ — the deferred bucket, unchanged in content, renumbered

Telemetry and instruments · external-party access · integration estate ·
soil/composites · EQuIS export · estate view · configuration packages · new figure
families (each behind a grammar proposal to Jerry). Planned as before when their
turn arrives.

---

# Waves 8+ planned — 2 September 2026

Jerry's standing instruction (1–2 Sep) continues the pipeline into the deferred
bucket and beyond: *continue waves until scope runs out, pinging at each close.*
The bucket is planned one wave at a time, as the original plan said it would be.
Drawing a deferred domain does not reschedule it in the product: every screen
these waves add is `proposed`, and the PRD's S8 markings stand.

## Wave 8 — Telemetry, loggers and instruments (Domain K, the deferred journey)

**Objective:** the one journey the catalogue has never walked, walked — the
telemetry exception: Instrument → Raw series → Gap/anomaly → Correction →
Corrected series → Re-evaluation → Alert/TARP impact → Audit (FR-1.10, FR-3.10).
**New screens (2):** `instruments` — the instrument register as a durable asset
record with its own lifecycle (deployment history at bores, calibration records
the wave-6 preflight already reads, service state, the barometric source named
per location group), which no existing screen owns; and one screen for the
raw-and-corrected series pair at a bore — raw retained untouched, corrected
derived with the compensation rule and its inputs carried (PP3), a gap drawn as
a gap, an anomaly raised as a finding, a correction recorded with reason and
reach, and the re-evaluation impact stated against the records that exist.
Names resolve against the glossary before anything is minted.
**Enhanced (≤3):** `hydrograph` (FR-1.10's own words — continuous series drawn
*separately from* discrete dips, in the same approved grammar, no new family),
`imports` (a logger file as an import kind, vendor format and barometric source
named), `location` (the instrument at the bore).
**Rider (carried from wave 7):** `#consistency`'s ionic balances derived from
`MAJOR_IONS` rather than typed, MW12 given the row its results owe — the
typed-copy drift class this pipeline exists to remove. The PFAS three-way
coverage disagreement stays recorded where wave 7 put it; it needs a design
decision interacting with `#criteria`'s non-detect demonstration and belongs to
a wave that redesigns those screens deliberately.
**Constraints:** the keep-list; the telemetry-journey coverage rows and the
"not walked" sentence updated with dated corrections, never rewritten; every
figure in the existing hydrograph/timeseries grammar — **no new figure family**
(Durov-class additions still need a grammar proposal to Jerry first); G-76b's
unprinted caveat inherited.
**Acceptance:** the deferred journey walks end to end with no unowned step;
FR-1.10/FR-3.10 flip to drawn-as-proposed with the S8 marking intact; build,
verify and the §11 audit loop green.

## Waves 9+ — the remaining bucket, planned at their turn

External-party access (OD-2) · integration estate (Domain R beyond FR-3.11) ·
soil/sediment matrices and composites (FR-1.7) · EQuIS export (FR-7.6, OD-8) ·
estate view (OM-2) · configuration packages (OM-5) · figure families (behind
grammar proposals). When the bucket and the accumulated riders are spent, the
pipeline closes with a summary rather than inventing scope.

## Wave 9 — Soil, sediment and composites (FR-1.7) — planned 2 September 2026

**Objective:** the one sample structure the catalogue has never drawn — a sample
that is *made of other samples* — and the depth axis that water samples do not
have. FR-1.7: depth intervals on soil and sediment samples; composite samples
with traceability to constituent increments. Vapour stays undrawn (no seed
context to draw it honestly); the coverage row says drawn-for-soil-and-sediment.
**New screen (1):** the composite sample record — increments with their depth
intervals and positions, the compositing decision as a record (who, when, what
scheme, why), traceability both ways, and the honest limit stated: a result on a
composite cannot be read back to one increment, and the screen says what that
buys (coverage) and what it costs (resolution) rather than pretending otherwise.
New-Screen Test: a composite has its own lifecycle (planned → increments
collected → composited in field or laboratory → analysed) and no existing screen
owns the increment-to-composite step. Naming anchors to FR-1.7's own nouns —
composite sample, constituent increment, depth interval — and the glossary's
location class / matrix distinction binds throughout (a sediment sample at a
surface-water location is the distinction's own example).
**Enhanced (≤4):** `events` (a soil investigation beside the groundwater round —
locations of class soil, depth-intervalled samples, the composite collected
across them); `locations` (class soil drawn as a class with its own record
shape, not a bore missing its casing); `lineage` (a composite result's chain
shows the increments — the one place "which increment" is answered with the
honest *cannot say*); `crosstab` or `results` (the matrix filter honestly
backed: what a soil row looks like, or the filter stating what it holds today).
**Seed:** a small deterministic soil investigation near the TSF — a handful of
class-soil locations, depth intervals, discrete samples plus one or two
composites, matrix recorded never inferred, evaluated only where a criteria set
in the seed legitimately applies (not_evaluated is the honest default per the
glossary's matrix entry — no criteria set is invented in passing).
**Constraints:** the keep-list; no new figure family; every count computed;
deferrals recorded in-repo; G-76b inherited.
**Acceptance:** the composite's chain walks both directions with the limit
stated; depth intervals render on sample records; FR-1.7 flips to
drawn-as-proposed for soil and sediment; build, verify and the §11 loop green.

## Wave 10 — Data exchange: the format estate and the EQuIS-bound export (FR-7.6, Domain R) — planned 2 September 2026

**Objective:** the catalogue's data exchange drawn as an estate rather than a
scatter — everything that crosses the boundary, in either direction, as a
record — and FR-7.6's deferred export drawn deciding something. FR-3.11's
import side is covered (`migration`, reconciliation proof); this wave draws its
mirror.
**New screen (1):** the format estate — every format the project exchanges,
each as a record: direction, owner, version, what it preserves and what it
cannot carry. Inbound: the ESdat EDD, the incumbent migration formats, wave 8's
logger vendor file. Outbound: the regulator-format return and the
EQuIS-compatible EDD. The EQuIS export is the screen's working example, drawn
with the **outward mapping stated** — ADR-0009 says practitioner vocabulary is
canonical and file formats map inward, so an export maps *outward* (LOR → EQL)
and the mapping table is the ADR rendered — a **reconciliation statement
mirroring the import's** (row and outcome counts proved against the record, the
FR-3.11 symmetry), and the honest loss statement: what the target format has no
column for, named rather than silently dropped. DR-2 binds: no Strataflow-
operated relay — an export is a file with a manifest, never a held URL, and the
screen says so where a reader would look for a "send" button.
**Enhanced (≤3):** `migration` (the symmetry sentence: import proves what
arrives, export proves what leaves, one discipline read twice);
`mapping-profiles` (the outward profile beside the inward ones, or the honest
note that profiles are inward-only today); `crosstab` or `report` (the existing
export menu's EQuIS entry backed by the estate record instead of dangling).
**Seed:** one deterministic export record — the 2026-Q2 groundwater round as an
EQuIS-compatible EDD: counts computed from the manifests and grid the seed
already holds, the outward mapping table, the loss statement (the qualifier
channel and dispositions the format cannot carry are the honest candidates),
and a reconciliation that proves the counts match the record.
**Constraints:** the keep-list; no new figure family; counts computed;
deferrals recorded in-repo; the accumulated deferral records untouched; G-76b
inherited.
**Acceptance:** FR-7.6 flips to drawn-as-proposed with the S8 marking intact;
the estate answers "what crosses the boundary and on whose terms" for every
format the seed knows; the export's reconciliation and loss statements recount
from raw literals; build, verify and the §11 loop green.

## Wave 11 — External-party access (OD-2) — planned 2 September 2026

**Objective:** the dimension the PRD deliberately kept as a seam, drawn as that
seam — a consultancy engaged on one project, scoped by the same project
boundary RLS already enforces, without inventing the multi-client identity
model v1 refuses (PRD §14 OD-2: "the external-party dimension is added at S8
when a consultancy customer is real"; §7's test is that the IA accommodates it
without route restructuring, and this wave is that test run).
**New screen (1):** the external-party engagement — the party as a record (an
obviously-fictional consultancy), the engagement with its scope stated in the
role vocabulary the register already holds (a Reader or Contributor binding, on
one project, through its own directory group), a sponsor inside the operator
who owns the engagement, an end date the access dies with, and the boundary
drawn honestly: what the party sees is the project's data through the same
scoping every internal principal gets — nothing bespoke — and what they cannot
see is everything the boundary excludes, stated rather than implied. The
screen also states what v1 does not have (no consultancy identity model, no
cross-client view for the consultant) and why that is a decision.
**Enhanced (≤3):** `roles` (the external binding beside the internal ones,
visibly time-bounded and sponsor-owned); `project-home` (the engagement
visible where a project manager would look for who else is in here); `audit`
(an external principal's action attributed like any other and distinguishable
— the trail answers "what did the consultants touch" without a special
mechanism).
**Seed:** one MOCK-convention consultancy, one engagement (scope, sponsor,
granted/expiry dates), one or two external principals with bindings, one
recorded action in the audit trail. Counts computed; the wave-5 membership
structures extended, never forked.
**Binding on this wave's audit round (carried from the wave 10 cap):**
re-verify W10-A-6's post-cap fix — the format inventory's `Fields: 42` and
`21` against the spec's 30 + 12 and the fixture's 21-column header — before
this wave can close.
**Constraints:** the keep-list; glossary vocabulary (reader/contributor,
principal, directory group — "engagement" anchored or recorded); no new
figure family; deferrals recorded in-repo; G-76b inherited.
**Acceptance:** the engagement walks from party to binding to boundary to
audit by link; nothing implies a route restructure; OD-2's coverage row flips
to drawn-as-proposed with the S8 marking intact; build, verify and the §11
loop green — including the W10-A-6 re-verification.

## Wave 12 — Configuration portability and the estate view (OM-5, OM-2) — planned 2 September 2026

**Objective:** the two `partially` rows in the operations domain closed by
drawing what each says is missing — the package surface OM-5's row names, and
the estate view OM-2's row places out of frame. The second is the delicate one:
it is the **vendor's** surface, not MOCK-WDL's, and drawing it means shifting
frame honestly rather than sliding a vendor screen under a customer's chrome.
**New screen (1): the configuration package** (customer-side). Export and
import of what OM-5 enumerates — criteria libraries, report templates, EDD
format definitions — as versioned packages: what a package contains (each item
with its own version, the "configuration, never code" rule already on
`#formats`), provenance on import (who built it, from which instance, when),
and the import drawn deciding something: a diff against what is in force, the
impact stated in the catalogue's own discipline (a criteria change previews
what it would re-evaluate — wave 3's machinery, referenced not redrawn), and
activation as a decision separate from arrival. Nothing imports silently.
**New screen (2): the estate view** (vendor-side, and saying so). N
deployments with version, upgrade window, backup state and support posture —
the R4 question ("version drift is the primary failure mode") answered from
the records a no-telemetry vendor actually has: the releases it cut, the
deployments it performed, and the diagnostic bundles customers chose to send
(DR-2 — data ages honestly, "as of the bundle of <date>", never a live feed).
**The frame shift is the design problem:** the screen states whose surface it
is, its principal is not A. Nakamura, nothing implies a customer can reach it,
and the catalogue's chrome must not silently claim otherwise — how that is
drawn is the wave's judgement call, made explicitly and recorded. The OM-2
coverage note ("out of frame here") gets a dated correction, not a rewrite.
**Enhanced (≤2):** `instance` (the deployment's own version panel gains the
package and estate links where they belong); `criteria` or `report` (the
in-force version naming the package it arrived in, closing the portability
loop from the receiving end).
**Seed:** one package (contents versioned per item, provenance, a diff with
one consequential change), and a small vendor estate (a handful of MOCK
deployments — Wandalup among them at its known version — with dated bundles).
Counts computed; every date derived or dated on its face (the two-clocks
lesson).
**Constraints:** the keep-list; DR-2/DR-3 absolute (no telemetry, no relay —
any sentence about how the vendor knows something must name a no-telemetry
mechanism); glossary vocabulary with recorded decisions; no new figure family;
deferrals recorded in-repo; the eight standing deferral records untouched;
G-76b inherited.
**Acceptance:** OM-5 and OM-2 flip to drawn-as-proposed with their honest
notes; the import walks arrival → diff → impact → activation by link; the
estate answers R4's question with every value dated to its source; build,
verify and the §11 loop green.

## Wave 13 — Deferral settlement (accumulated scope) — planned 2 September 2026

**Objective:** the deferred bucket is spent, so the pipeline turns to the debts
its own record has accumulated — the four standing deferrals that are payable
by derivation and alignment, taken together because each is the same move:
replace a described disagreement with one source read N times. No new screens.
**Scope (4 settlements):**
1. **The bundle-contents gap** (wave 12's record): one derived contents list —
   the diagnostic bundle's categories with the configuration inventory's
   per-item versions and checksums — that `#diagnostics` and `#entitlement`
   both read, so the estate's configuration column rests on a field the
   catalogue describes. Remove the warn card on `#estate` and the two seed
   deferral records, replaced by short landed-notes.
2. **The two clocks, named on every face** (wave 11's record, wave 12's
   OPS_AS_AT): every date-bearing surface states which clock it reads;
   `ENTITLEMENT.remaining` derives from OPS_AS_AT instead of typing 130 days;
   the MEMBERS last-seen values and the other OPS_AS_AT literals keep their
   dates but gain the clock statement where they render. Moving AS_AT itself
   is deliberately NOT this wave — re-deriving every countdown in the
   catalogue is its own change with its own blast radius, and the honest
   settlement is that no reader can mistake which as-at a number is on.
3. **`#imports`' three literal stat tiles** (wave 8's record): derived off
   `IMPORTS` now that runs carry `kind` — deciding and stating what "this
   quarter" and "held" mean across an EDD run, a migration run and a logger
   download; the wave-8 "contributes 0 to all three" reasoning becomes a
   computed fact instead of a protected assumption.
4. **The criteria library's matrix column** (wave 9's record): the rows gain
   a real matrix field (glossary words), the receptor/class conflation is
   drawn as what each row actually is, and `#composite`'s "0 soil sets"
   becomes right by construction — the filter computes over the new field.
**Not this wave, restated so the record stays honest:** the WATER_LEVELS
baseline (crosses a licence trigger; needs its own deliberate wave), the PFAS
three-way (interacts with the non-detect demonstration), the DQO matrix
dimension and the qualifier scheme gap (both cross-screen design decisions),
and the depth-profile plate (gated on the FP-1 verdict).
**Constraints:** the keep-list; no new figure family; every settled deferral's
record replaced with a dated landed-note, never silently deleted; the five
unsettled records untouched; census counts character-identical; G-76b
inherited.
**Acceptance:** the four disagreements each resolve to one source read N
times, measured by the audit's own reference checks; the deferral ledger
shrinks from nine to five with the history preserved; build, verify and the
§11 loop green.

## Wave 14 — The WATER_LEVELS baseline, re-anchored (wave 8's record) — planned 2 September 2026

**Objective:** the largest standing lie in the seed settled on its own terms.
The monthly water-level series was shaped (`base = loc.toc − 12.5`) to spread
three hydrograph traces apart and never reconciled with the field record; the
deferral's own six-bore table shows the dip-derived and fitted columns agreeing
within 0.2 m while the drawn series sits metres away. Its record said the
repair "needs its own deliberate wave" — this is that wave, alone, because the
blast radius is every surface that reads a water level.
**The decision, taken in the plan so the wave executes rather than invents:**
re-anchor `WATER_LEVELS` to the field record's baseline (the majority
evidence: two of three columns agree), preserving each bore's drawn *shape*
(the seasonal structure, the wet-season recoveries, the MW05 decline) while
correcting its *datum* — the series' internal variation is fiction doing its
job; its absolute level is fiction contradicting the record, and only the
second is the defect.
**The consequence, drawn rather than dodged:** re-anchored, MW05 sits ~4 m
ABOVE the 195.6 m AHD licence trigger level rather than below it. That is not
an accident to smooth over — it coheres with the seed's own story (the TSF
seepage narrative, the mound the interpretation describes) — so the wave draws
what a level-trigger exceedance obliges: the trigger drawing on the hydrograph
tells the truth, and every surface that states MW05's level against the
trigger (exceedances, TARP, obligations, the licence screen, the narrative's
evidence set) is enumerated and made consistent, with the new exceedance
carried as the record it would be — or, if enumeration shows the 195.6 trigger
was itself typed to sit above the old wrong series, the trigger level is the
thing corrected, with the licence record's own provenance deciding which. The
implementer measures first, decides against the evidence, and records the
decision — the one thing it must not do is leave the level story and the
chemistry story disagreeing about which side of the line MW05 is on.
**Blast radius, enumerated up front (verify each):** the hydrograph (Figure
4.1 and its trigger line), the logger plate's beside-not-on relationship
(wave 8 anchored it to the field record — after this wave the two plates
SHOULD share a baseline, and the "why this is a second plate" panel must be
re-trued or retired), `HEADS`/the potentiometric fit (already field-anchored —
must now agree), the water balance, trend/statistics surfaces reading levels,
`#location`'s derived elevations, and the wave-8 deferral record itself
(settled-note, original preserved).
**Constraints:** the keep-list (reported-then vs record-now: past rendered
levels in issued-report contexts are handled by the catalogue's own
supersession honesty, not silent rewrite); no new figure family; counts and
levels computed; census character-identical; the four remaining deferral
records untouched; G-76b inherited.
**Acceptance:** the six-bore table's three columns agree within the fit's own
residuals; the hydrograph, logger plate, fit and dips tell one level story;
the trigger consequence is drawn end to end; the ledger shrinks five → four
with history preserved; build, verify and the §11 loop green.
