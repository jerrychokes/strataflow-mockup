# Wave 6 — response to the audit, round 1

The audit measured everything the wave set out to build and found it built — the
bore-session axis, the dispositions, the preflight, the decision layer, the
propagation bases with their refusals, the DQO pairing, all three riders, all eight
keep-list items — and blocked the gate on one thing, correctly: a deferral that
existed only in a commit message, under a sentence a drawn screen measurably
contradicts. All five findings are accepted and fixed in this closing commit.

| Finding | Severity | Response |
|---|---|---|
| W6-A-1 — the MW11 contradiction recorded nowhere in the repository, under `#data-states`' false "drawn empty" | P1 | **Accepted and fixed — the record now exists in the three places records live here.** A comment beside `CROSSTAB` in `seed.mjs` names the contradiction, its provenance (both sides predate the wave) and its owner; a visible warning notice on `#crosstab` says the MW11 column should be empty and why it is not yet; and the `#data-states` sentence now describes the pattern the state *asks for* and states plainly that the catalogue's own crosstab does not draw it yet. The blanking itself — which moves the censored counts and every surface computed from `CROSSTAB` — is **wave 7's carried rider**, recorded in the wave plan's Wave 7 section, not a commit message. The auditor's contrast was the correct instrument: the qualifier-channel note on `#lineage` is what "recorded" looks like, and MW11 now has its equivalent. |
| W6-A-2 — "string-identical in the DOM" false in two cosmetic respects | P3 | **Accepted and fixed by unification, not restatement.** The empty unit no longer leaks an em dash into `#purge`'s stacked labels (`pH —` → `pH`), and the stable marker is the derived `all ${params.length} holding` on both screens — the typed word "six" was itself a small instance of the typed-count class this wave existed to remove. Re-measured after the fix: the seven MW05 series rows are byte-identical across `#field-capture` and `#purge`. The overclaim was the orchestrator's commit message, not repo prose — the repo said "one source, read twice" throughout, and now the stronger sentence is true as well. |
| W6-A-3 — condition-before-dip deviates from the review's listed order under a claim of fidelity | P3 | **Accepted; the order stays, the claim is corrected.** The auditor's own assessment — checking the headworks before opening the bore is defensible, arguably better, field practice — is the reason the order stays. The deviation and its reason are now stated where the session is documented: condition, access and headworks are inspected before the dipper goes down the hole; from the dip onward the order is the review's own. |
| W6-A-4 — the README's pass-4 paragraph still asserts "no practitioner has looked at any of it" | P3 | **Accepted and fixed** with the same dated-rider treatment the sibling sentence received: true when written, corrected 1 September 2026, pointing at the review file. |
| W6-A-5 — ST-1 types two numbers the seed derives | P3 | **Accepted and fixed.** ST-1's detail is now composed from `PURGE.failing.what` — the register row and `#purge`'s failing panel are one sentence read twice. It sits beside the `PURGE` derivation because the QA/QC register is declared before the field round exists; the comment says so. |

**On the observation** (the preflight prose's "2 things hold it" against three
counted hold lines): the screen is coherent as the auditor found — two of the three
lines are the same sync backlog and the prose says so — and the imprecision was the
commit message's, so nothing on the screen changes.

**State at close:** build exit 0 (68 screens, 1204 anchors — the crosstab notice
added one — link graph clean, the 23 Aug census unchanged) and verify exit 0
(nine families, all three widths) with all five fixes in. Round 2 goes back to the
standing auditor lineage.
