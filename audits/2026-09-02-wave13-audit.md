# Wave 13 audit — round 1 — commit `a8cba70`

Jerry directed that deep audits run on Codex; this environment has no Codex CLI and no
OpenAI credentials, so this round runs under `EXPANSION_BRIEF.md` §11's fallback auditor
provision ("any capable model session with no prior context — the mechanics matter more
than the model").

Signed: **the standing auditor lineage.** This round is identified by its round number and
the commit it measures; session-continuity claims in either direction are out of scope.

Audited against: the "Wave 13 — Deferral settlement (accumulated scope)" section of
`audits/2026-09-01-wave-plan.md`. Every claim below was re-measured — recounts from the raw
literals in `seed.mjs`, quotation anchors by grep against the app repo at
`/home/user/strataflow`, rendered checks in a driven Chromium via playwright-core (never
the CLI `--screenshot`), with the auditor's own probes, not the implementation's.
Screenshots live in the auditor's scratch area, referenced by filename.

## What was run, and what it returned

| Measurement | Result |
|---|---|
| `git diff --name-only b95741e a8cba70` | exactly `README.md`, `index.html`, `screens.mjs`, `seed.mjs` — the spec's four files, nothing else (`app.css`, `audits/`, `figures.mjs`, `chrome.mjs`, `ui.mjs`, `build.mjs`, `verify.mjs` all untouched) |
| `node build.mjs`, twice, hashed between | sha256 `04ab2782…` before and after both runs — committed `index.html` byte-identical to its own rebuild; sha1 `c2fe56bb…` matches the claimed hash. 75 screens; census line **character-identical** to a build of `b95741e` (both clocks of it); "9 drawn since"; no new screens |
| `git diff … screens.mjs \| grep "state: '"` | **0** — no 23 Aug census edits |
| `CHROMIUM=… node verify.mjs` | exit 0 — all nine families green (overflow ×3 widths, ids, names, headings, matrix panning, 13 figures/248 texts, 558 touch targets, 1,597 stacked labels, 122 captions ×3) |
| Auditor's own overflow sweep (independent script) | 75 screens × 375/1000/1280: worst document overflow **0 px** at every width |
| Auditor's click walk | diagnostics → entitlement → instance → estate → package → criteria → composite → … → quarantine: every link clicked lands on its target |

## The four settlements, re-measured

**Settlement 1 — bundle contents.** The five category `what` strings match
`apps/web/lib/ops/diagnostics.ts` **verbatim** (lines 367–424), the OM-1 opening quote is
verbatim (line 14), `DiagnosticsCategoryName` is the closed five-name union (line 308), and
`schemaReport()` selects from `kysely_migration` and nothing else (lines 288–290).
`NOT_INCLUDED` holds exactly **6** entries whose subjects match the mockup's six.
`BUNDLE_CONTENTS.categories[1].inventory` is the property reference `CONFIG_INVENTORY` in
source; the inventory checksum `3d81ba0c5e47` is **one literal** in `seed.mjs`; the per-item
`7e2d915cb083` is one definition literal read by reference at the estate drift row
(`seed.mjs:9121`, old literal recorded in the comment beside it) and renders on four
surfaces. Size recount: 2+6+3100+84+14+2 = **3,208 KB → "3.1 MB"** — the sum of its parts,
as claimed. Both bundle screens render every category string (diagnostics ×1 each,
entitlement ×2 each); the estate's warn card is now `mk-card--good` inside `.mk-vendor`,
dated "Recorded 2 September 2026 (wave 12) — settled 2 September 2026 (wave 13)"
(`w13-estate-settled.png`). The found-and-fixed holds against the product:
`apps/web/lib/ops/log.ts`'s `REDACTED_KEYS` was read in full — passwords, secrets, tokens,
authorization, cookie, apikey, connection string, database URL, stacks, and the customer's
value/result/content keys — and **neither `principal` nor `subject` appears anywhere in the
file**; "Any principal identifier" survives on `#diagnostics` only inside the correction
record quoting what the screen used to say. `#package`'s held-here reads **2** off the
inventory, prints the old counting ("counted the EDD-format kind as 'held here: 4', straight
off the format register's length") and names the two refused rows; `versionOf()`'s refusal
is quoted verbatim from `config-bundle.ts:101-105`, and `FORMATS` does hold 4 rows of which
2 carry version `'—'`. *But see W13-A-1: one panel below, the same screen still counts those
two as exportable.*

**Settlement 2 — two clocks.** `ENTITLEMENT.remaining` is a getter —
`daysBetween(OPS_AS_AT, term end)` — and the arithmetic recounts: 8+30+31+30+31 = **130**.
The clock sentence exists as **one template literal** in `seed.mjs` (`OPS_CLOCK.head`/`tail`),
is rendered through **one helper** (`opsClock()`, `screens.mjs:135`) with exactly **16 call
sites**, and "Administration clock — as at 2026-08-23" renders exactly **16 times**. No date
literal moved: every date in a removed diff line (the five criteria-row effective dates)
reappears identically in the added lines. The auditor enumerated every rendered ISO date
after `2026-08-23` independently (17 distinct values, 38 occurrences, plus the prose-form
September dates) and the classification holds: the four after-both-clocks records the note
names (`2026-08-24` ×2, `2026-08-27`, `2026-09-01` golden run) are present and are event
dates, each beside its screen's clock statement; the rest are due dates, windows, terms and
settled stamps — with one class the note's sentence does not name (W13-A-3).

**Settlement 3 — import tiles.** Recounted from the raw `IMPORTS` literals: committed =
231 + (18,740 − 112) = **18,859** (the `kind: 'series'` logger and the reversed IMP-0240
each contribute 0 by the stated filter); held = 3 + 112 = **115**, excluding the reversed
run's 6 — exactly the "old 9 = 6 + 3, two errors in opposite directions" decomposition;
runs 5; reversed 1. `QUARANTINE` enumerates exactly **3** held rows and the rendered
reconciliation states the migration's 112 as a count on a run rather than rows anybody can
open. Every tile's definition (`counts`, `includes`, `was`) is a field on the same object as
its `value`; the quarter is derived from `AS_AT` as a half-open window; the
logger-contributes-0 panel is computed and rendered (`w13-imports.png` — tiles, definition
table with its "What it said before" column, reconciliation, logger panel, all inspected by
eye at 1280).

**Settlement 4 — criteria matrix.** All five `CRITERIA_LIBRARY` rows read
`matrix: 'Water'`; `MATRIX_WORDS` is the glossary's closed five; `Freshwater` moved to
`protects`, `Groundwater` to `locationClass`, and `wasDrawnAs` renders under all five matrix
cells ("read *Freshwater* until 2 Sep 2026" ×5 in the document). `#composite`'s zero is a
field match — `CRITERIA_LIBRARY.filter((c) => c.matrix === 'Soil' || c.matrix ===
'Sediment')` (`seed.mjs:6907`) — with the old regular-expression counting recorded beside
it. `#exchange`'s reconciliation and `CONFIG_PACKAGE.exportable` did not move (exports
5+4+0 = 9) — which is what the spec asked, and is also where W13-A-1 lives.

**The ledger.** `FOUND AND DEFERRED` = **5** in `seed.mjs`, and the five subjects are
exactly the claimed five (WATER_LEVELS baseline, PFAS three-way, DQO matrix, depth-profile
plate, qualifier scheme); the three deferral markers touched by the diff are the settled
conversions, and each settled note preserves the original — "What wave 11 wrote, unedited",
the matrix record's verbatim "What the record said" quotation, the bundleGap quote on
`#estate`, wave 8's words in `screens.mjs`. Settled notes count **5** by one consistent rule
(the inline `settled 2 September 2026, wave 13` marker; the two-clocks settlement owns two
records), and 9 − 4 closed ledger records = 5 standing, which is the README's arithmetic.
Keep-list: the screens that carry its eight promises (field-capture, qc, dqa, exceedances,
results) are untouched by the diff, and the marker vocabulary still renders (indeterminate
×94, anomaly ×11, reported-then/record-now, MW11 dry).

## Findings

### W13-A-1 · P1 — `#package` still counts the two refused formats as exportable, one panel below the settlement that refuses them

- **Reproduction:** render `#package`. The held-here panel reads *"EDD format definition ·
  revision · 2"*, states *"An item that declares no version is refused, never defaulted"*,
  names `ESdat legacy export` and `EQuIS EDD (4-file)`, and says of the old counting that it
  *"was this panel disagreeing with itself"*. The next panel down ("The other direction —
  building one here") reads **"9 items this instance could export … EDD format definition ·
  4 · The format registry"**, captioned *"Counted off the registers themselves"* — the exact
  counting rule the settlement just corrected, on the same screen, for the same kind. In
  source: `exportable` computes `exports: … FORMATS.length` (`seed.mjs:8785-8788`) while
  `KINDS.held` reads `CONFIG_INVENTORY` three declarations above.
- **Why it is wrong and not merely different:** by the settlement's own anchor,
  `configBundle()`/`versionOf()` **throws** on an unversioned artifact
  (`config-bundle.ts:97-108`) — an export built from this instance could not carry those
  two rows, so "could export … 4" is false under the rule quoted verbatim beside it, and
  "9 items this instance could export" should read 7. The record's own sentence — *"they
  are formats the migration was read with rather than configuration this deployment
  holds"* — says the instance does not hold them; what it does not hold it cannot export.
  The panel's "what it would refuse" prose covers only customer data and never reconciles
  the 4 against the 2.
- **Affected acceptance criterion:** *"the four disagreements each resolve to one source
  read N times"* — the EDD-format count now renders from **two** sources on one screen
  (`CONFIG_INVENTORY` → 2, `FORMATS.length` → 4). A reader finds two counts of one kind,
  which is the state the settled note says it exists to prevent. Noted fairly: the wave
  spec itself ordered *"verify neither moved"* for `exportable`, so the implementation
  followed its instructions — the instruction and the settlement conflict, and the fix
  round may need to amend the spec rather than just the seed.
- **Recommended correction:** derive `exportable`'s EDD count from `CONFIG_INVENTORY` (with
  the two refused rows named beside it, exactly as held-here now does), or — if register
  liveness is the point — rename and recaption the panel so it counts register rows rather
  than claiming exportability, and state on it why two of them could never leave. Then
  re-verify `#exchange` and any other reader.

### W13-A-2 · P3 — the truncation claim contradicts itself in the seed record

`seed.mjs` (above `CONFIG_INVENTORY`): *"twelve hex characters of a SHA-256, shown
truncated exactly as the product's own manifest renders one (`sha256.slice(0, 16)`)"* —
the citation is real (`diagnostics.ts:457` slices 16), but twelve is not sixteen, so the
digests are *not* truncated "exactly as" the product renders them. Unrendered — the
sentence lives only in the seed comment. Fix the sentence (or the digests) either way.

### W13-A-3 · P3 — the post-as-at date classification omits `#package`'s "if activated" preview dates

The two-clocks record classifies every rendered date after `OPS_AS_AT` as the four named
event records plus "due dates, contract terms, upgrade windows, a holding-time expiry and
this wave's own settled-notes". The auditor's own enumeration found two renderings of
`2026-09-01` on `#package` — *"2018-08-01 → 2026-09-01 → if activated"* and *"Activating
version 2026.1 on 2026-09-01"* — which are hypothetical action dates, none of the stated
classes (and rightly not among the four *records*). The classification sentence should name
the class, since the record's own lesson is that the first draft missed a date by recalling
instead of enumerating.

### W13-A-4 · P3 — two corrected numbers print their before-state everywhere except on a screen

The imports tiles print their old literals on the screen; `#package` prints "held here: 4"
in its settled note; `#entitlement` renders the old 4-rows-vs-8-rows list shape. But the
bundle size (3.6 MB) and the old exclusion count (2) appear in the README and the seed
record only — `index.html` contains no "3.6" and no rendered old exclusion count, while the
corrected values (3.1 MB, 6 exclusions) render. Uneven application of the wave's own
"printed with what they said before" convention; history is preserved, just not where the
number is.

## Clean categories, stated as such

- Diff scope, census identity, byte-identical rebuild, no `state:` edits — clean (table above).
- Quotation anchors — every explicitly quoted clause checked against
  `diagnostics.ts`, `config-bundle.ts`, `log.ts` is verbatim; no wave-12-style misquote found.
- All four recounts (bundle size, 130 days, four import tiles, zero soil sets) — reproduce
  from raw literals.
- One-source-N-readers — holds for the bundle list (3 screens), the clock sentence (1
  helper, 16 sites, 16 renders), the tiles (1 object), the matrix filter (1 field), and the
  checksums — with the one exception filed as W13-A-1.
- Ledger — 5 deferred (subjects verified, texts untouched), 5 settled notes each preserving
  the original record; nothing deleted.
- Layout, ids, names, headings, figures, touch targets, stacked labels, captions — verify.mjs
  green and the document-overflow result reproduced independently at all three widths.

## Verdict

**Wave 13 does not pass round 1 under the zero-P0/P1 gate.** One P1 — W13-A-1, the
`#package` exportable panel contradicting the wave's own settlement one panel away — and
three P3s. Everything else the wave claims was reproduced by independent measurement: the
recounts, the property references, the verbatim anchors, the ledger's history, the census,
and the build's determinism. The P1 is small in surface (one derivation and one caption)
but it is the wave's own class of defect — a count read off a register's length beside the
record that explains why that counting is wrong — and the gate exists for exactly that.

---

# Round 2 — commit `b5a7ed5`

Signed: **the standing auditor lineage.** Fallback-auditor status unchanged (no Codex CLI,
no OpenAI credentials; §11's fallback provision). Round 2 measures the fix commit
`b5a7ed5`; the response (`2026-09-02-wave13-response.md`) was read as claims, and every
claim below was re-measured with the auditor's own probes.

## What was re-measured

| Measurement | Result |
|---|---|
| `git diff a8cba70 b5a7ed5` | exactly the two audit files, `index.html`, `screens.mjs` (5 lines), `seed.mjs` (33 lines) — the four fixes and their records, nothing else; **0** `state:` lines touched |
| `node build.mjs`, twice, hashed between | exit 0; committed `index.html` byte-identical to both rebuilds (sha256 `3b2787f4…`; sha1 `b78017d7…` — the response's claimed hash, verified) |
| Census | build census line **character-identical** to a build of `a8cba70` in a worktree: `23 Aug 2026 · not built: 15 · engine-only: 35 · shipped: 8 · proposed: 5 · shipped (CLI): 3 · 9 drawn since` |
| `CHROMIUM=… node verify.mjs` | exit 0 — all nine families green (75 screens ×3 widths, ids, names, headings, panning, 13 figures/248 texts, 558 touch targets, 1,597 stacked labels, 122 captions ×3) |
| Auditor's own overflow sweep | 75 screens × 375/1000/1280: worst document overflow **0 px** at every width |
| Rendered-date inventory | ISO-date multiset of `index.html` **identical** between `a8cba70` and `b5a7ed5` — the fix round introduced no new post-as-at dates, so round 1's enumeration remains the classification's base |

## The four findings, re-measured

**W13-A-1 — fixed, and verified as one source.** In source, `exportable`'s EDD count is now
`CONFIG_INVENTORY.items.filter((i) => i.kind === 'edd-format').length` (`seed.mjs:8805-8810`)
— a property reference into the **same** `CONFIG_INVENTORY` the held-here settlement reads,
not a re-typed literal — and `counts.exportable` reduces over that same array. Rendered
(driven browser, eyeballed at `w13r2-package-full.png`): the heading reads **"7 items this
instance could export"**, the EDD row reads **2** with the registry cell *"— 2 of its 4: the
two declaring no revision cannot cross, and this panel said 4 (and 9 in its heading) before
it read the inventory"*, the caption carries the versioned-item reason, and `"9 items this
instance"` appears **zero** times in the document. Cross-checks: held-here still reads 2 off
the inventory (5 + 2 + 0 = 7); a grep for every reader of `exportable` finds only the
`#package` panel and the derived count — no other consumer (including `#exchange`) renders
an EDD export count, so the two-sources state did not move screens. The remaining
`FORMATS.length` read (`counts.registerRows`) states a different fact — rows on the
registry — which is the distinction the settled note itself draws. The seed comment records
the first draft and cites W13-A-1. The process note is answered as suggested: the response's
opening paragraph records the wave plan's "verify neither moved" instruction as the
finding's co-author, which is the spec amendment in the right place.

**W13-A-2 — fixed.** The seed comment now states both truncations — the product's sixteen
(`sha256.slice(0, 16)`, re-verified verbatim at `diagnostics.ts:457`) and this catalogue's
twelve as a stated display choice — with the first draft's claim recorded. Comment-only, as
the finding was.

**W13-A-3 — fixed.** The classification gains the named category: the two `if activated`
preview renderings (*"2026-09-01 → if activated"* on `#criteria`, *"Activating version
2026.1 on 2026-09-01"* on `#package` — both re-found in the document, ×2 and ×1) are
classed as previews of a conditional future event with no as-at to be after, and the note
records this as the second measurement-driven correction. The enumeration base is unchanged
(date multiset identical between commits), so no third class appeared with the fix.

**W13-A-4 — fixed in substance; the response misnames the screen (→ W13-A-5).** The muted
paragraph renders — *"What this panel said before the configuration inventory was
enumerated (wave 13): 3.6 MB, and 2 omissions. Both moved because both derive now…"* — and
it sits in the right place: beside the omissions sentence in the bundle panel, on the same
screen as the document's **only** render of the corrected `3.1 MB`, with `3.6 MB` now
appearing exactly once (inside the correction, as the before-state). The import-tiles
treatment, applied where the number lives.

### W13-A-5 · P3 — the response memo names `#diagnostics`; the paragraph renders on `#entitlement`

- **Reproduction:** walk every text node of the rendered document for *"What this panel said
  before the configuration inventory"* — one occurrence, whose `.mk-screen` ancestor is
  `#entitlement`; the corrected `3.1 MB` also renders only there. The response's W13-A-4 row
  says **"Accepted and fixed on `#diagnostics`"**.
- **Why it matters at all:** the fix landed on the correct screen — the one that renders the
  numbers being corrected — so nothing drawn is wrong; but the response file is part of the
  audit trail, and this wave's whole subject is records that agree with measurements. A
  reader following the response to `#diagnostics` finds no paragraph.
- **Recommended correction:** one word in `2026-09-02-wave13-response.md` (or a dated
  correction beside it, per the house convention). Nothing on a screen needs to move.

## Round 2 verdict

**Wave 13 passes round 2 under the zero-P0/P1 gate, and the wave closes.** The P1 is fixed
by the wave's own move and verified as one source read by every renderer; the three P3s are
fixed as described; hygiene (diff scope, byte-identical rebuild against the response's own
hash, census identity, nine verify families, independent zero-overflow sweep) is clean. One
new finding, W13-A-5, is a P3 in the response memo's prose, not on a drawn screen — open,
non-blocking, one-word fix.
