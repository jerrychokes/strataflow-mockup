# Wave 13 — response to the audit, round 1

The round blocked on a P1 that is the wave's own discipline turned on its spec:
the exportable panel kept two sources for one count on one screen, and it did
so partly because the wave plan ordered "every consumer re-checked
(CONFIG_PACKAGE.exportable, #exchange — verify neither moved)". The auditor
noted that fairly, and the spec was wrong: once settlement 1 established that
versionOf() refuses an unversioned artifact, the exportable count HAD to move,
and instructing the implementer to hold it still built the contradiction in.
**The spec amendment is this paragraph** — the orchestrator's instruction is
recorded as the finding's co-author. All four findings accepted and fixed:

| Finding | Severity | Response |
|---|---|---|
| W13-A-1 — the exportable panel read the format registry's 4 (heading 9) one panel below the settlement that counts 2 off the inventory | P1 | **Accepted and fixed by the wave's own move:** the EDD export count derives from `CONFIG_INVENTORY` (an export can only carry a versioned item), the heading derives to 7, and both the seed comment and the panel's registry cell record what the first draft counted and why it could not stand. |
| W13-A-2 — the digest comment claimed twelve characters match a sixteen-character slice | P3 | **Accepted and fixed at the comment:** the product renders sixteen, this catalogue's columns carry twelve trimmed further for the cells — a display choice now stated rather than misattributed, first draft recorded. |
| W13-A-3 — the post-as-at classification omitted the two if-activated preview dates | P3 | **Accepted and fixed in the classification:** a preview of a conditional future event has no as-at to be after, and the note now says so — recording that this is the second time the enumeration was corrected by measurement, which is the reason it exists. |
| W13-A-4 — the 3.6 MB and 2-exclusions before-states printed nowhere on-screen | P3 | **Accepted and fixed on `#entitlement`** (round 2's W13-A-5: this memo first said `#diagnostics`; the paragraph renders beside the document's only render of the corrected 3.1 MB, which is `#entitlement`, the correct screen)**:** one muted paragraph stating what the panel said before the inventory was enumerated and why both numbers moved — the import tiles' own treatment, applied consistently. |

**State at close:** build exit 0, rebuild byte-identical (`b78017d7…`), the 23 Aug
census state counts unchanged; verify exit 0 across all nine families; the
heading renders the derived 7. Round 2 goes back to the standing auditor lineage
against this commit.

---

# Wave 13 — response to the audit, round 2: closed

**The gate passed — zero P0/P1 — and Wave 13 closes under §11.** Round 2
verified the exportable derivation as one source by reader census, the rendered
7/2 with the before-records, both re-checked citations verbatim, and the
rendered-date multiset identical across the fix. The one new P3 (W13-A-5: this
memo named the wrong screen for the W13-A-4 paragraph) is corrected above, in
place, with the correction marked — the memo's error is part of the record.

**The settlement wave's own ledger entry:** nine deferral records became five
standing plus five settled-notes; the import tiles, the bundle description, the
clocks and the criteria matrix all resolve to one source read N times; and the
loop's three final rounds each caught the orchestrator once — the resumption
message, the wave spec, and this memo — which is the §11 mechanism working on
every author it has.
