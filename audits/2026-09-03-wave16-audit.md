# Wave 16 audit — round 1, against commit `fae3a64`

Signed: **the standing auditor lineage**. This round is identified by its round number
and the commit it measures; session-continuity claims in either direction are out of
scope. *Jerry has directed that deep audits run on Codex; this environment has no Codex
CLI and no OpenAI credentials, so this round runs under `EXPANSION_BRIEF.md` §11's
fallback auditor provision — any capable model session with no prior context, the
mechanics mattering more than the model.*

Audited: the Wave 16 section of `audits/2026-09-01-wave-plan.md` (the deferral's own
words as spec), at `fae3a64` in a scratch worktree; pushed HEAD `b0e826f` is the
pre-wave baseline every "untouched" claim below was diffed against. Dated 3 September
because the round crossed midnight UTC; every measurement ran against `fae3a64`.

## Mechanics, all measured

| Check | Result |
|---|---|
| `node build.mjs` twice | exit 0 both; `sha1sum index.html` identical both runs: `4f478643844364a05f412f29731a57ded612fb56` — matching the stated hash; worktree clean after, so the committed `index.html` **is** the rebuild |
| Census | first line `23 Aug 2026 · not built: 15 · engine-only: 35 · shipped: 8 · proposed: 5 · shipped (CLI): 3 · 9 drawn since` — the five state counts character-identical to the required string; second clock printed beside it |
| `CHROMIUM=… node verify.mjs` | exit 0, all nine families: 75 screens × 375/1000/1280 px no document overflow; 13 figures / 250 text nodes no collisions; 562 coarse-pointer targets ≥ 44 px; 1,661 stacked labels none below own min-content; 133 captions × 3 widths contained; no duplicate ids, unnamed controls, heading skips; matrix panning intact |
| Diff scope | `git diff --stat b0e826f fae3a64` names exactly `README.md`, `index.html`, `screens.mjs`, `seed.mjs` — no figure, stylesheet or audits file touched (no new figure family by construction) |
| `state:` history | `git diff b0e826f fae3a64 -- screens.mjs` contains zero `state:` lines — the 23 Aug census is unedited at source, not only in the printed line |

## The wave's claims, re-measured independently

**Ledger.** `grep -c "FOUND AND DEFERRED" seed.mjs` = **3** (pre-wave: 4). The three
survivors are the claimed three — wave-15 narrative blanks (7219), wave-9 depth-profile
plate (8787), wave-10 qualifier scheme gap (9286) — and each block diffs **byte-identical**
against its `b0e826f` counterpart. Exactly **1** `SETTLED (wave 16)` heading (8388); the
settled note quotes the original deferral **verbatim** (line-by-line diff of the
blockquote against the pre-wave text, empty), with only the grep-counted marker line
closed — and it states that reasoning itself. The wave-13/14/15 settled notes stand.

**Glossary authority.** `MATRIX_WORDS = ['Water', 'Soil', 'Sediment', 'Vapour', 'Biota']`
(seed.mjs:2854) against the app repo's `docs/GLOSSARY.md:825` — "The **material a sample
is made of** — water, soil, sediment, vapour or biota." Five words, same five, same order.

**Closure identities, tested by `===` in Node against the worktree module** — all four hold:
`QC_LIMITS.rules === DQO.limits` · `SOIL.duplicate.limit.rule === QC_LIMITS.rules[0]` ·
`DQO_APPLICABILITY.gap === SOIL.duplicate.limit` · `QC_LIMITS.matrices === MATRIX_WORDS`.
`QC_LIMITS.resolve` is one function with the two told-apart outcomes; the `held: false`
branch hands back the water row and the sentence naming it, and returns a null rule —
never an invented number — where no limit exists for the check at all.

**The arithmetic, recounted by hand from raw literals** (S02 `[6.6, 940, 880, 8.2, 41]`
vs S03 `[6.5, 905, 842, 7.9, 44]`, LORs `[0.1, 10, 20, 2, 5]`, seed.mjs:8152–8164,
recomputed in Python without importing the module): EC 3.79%, sulfate 4.41%, zinc
**7.06% ≈ 7.1%** (the worst percentage, as claimed); **arsenic 8.2/7.9 does not reach
5 × LOR = 10**, so absolute difference 0.3 against allowance 4 — the found-and-fixed
applicability defect is real and correctly applied; pH not judged; every pair within.
The rendered `#composite` applicability table matches this digit for digit, including
"absolute difference · 0.3 against 4 — 2 × LOR" and "not judged" for pH.

**The counted absence.** The `#composite` evidence table renders **six** rows — sampling
plan, both DQO versions, PAS QA plan rev 8, certificate/work order, AS/NZS 5667.1, the
criteria library — every one answering **No**; the count is `G.searched.length` and the
"all N state none" sentence is a filter over verdicts, so prose and table cannot disagree.
The verdict names the judge in full — "no Field duplicate RPD limit for soil. What judged
it is the water one — ≤ 30%, DQO 2025.2, in force since 2025-07-01" — the before-state is
quoted under "Recorded 2 September 2026 (wave 9), and this is what it said", and the
stricter-for-heterogeneous-soil argument is explicitly labelled "reasoning rather than a
citation".

**The library.** Every row of the `#qc-limits` table answers matrix / since / version:
all ten matrix **Water** with a per-row source-line justification ("The matrix is a
measurement, not a default"), the **2** rows that moved under 2026.1 carrying
**2026-05-21** and the **8** carried forward carrying **2025-07-01**, the `since` derived
by `startOf()` from the version span rather than typed. The matrix histogram recounts
from raw literals: Water 10 limits / **12** samples / **6** QC (11 event-round water + the
soil round's one water rinsate; 5 + 1 controls), Soil 0 / **11** / **1** (the blind
duplicate), Sediment 0 / **1** / 0, Vapour and Biota 0 / 0 / 0 — every figure matches.

**"No comment carries what the library now states."** An adversarial grep for typed limit
literals across both source files finds only: the `was:` / `wasTyped:` before-records
(the convention's required history), the as-raised import-exception row ("38.2% against a
30% acceptance limit" — **byte-identical at `b0e826f`:3148**, the keep-list's
reported-then, correctly not re-judged), one hypothetical what-if reading its live half
from `DQO.changes`, and the regulator's rhetorical "why 30%?". `#dqa`'s dimensions and
`#qc`'s FD-1/FD-2 read `limitOf()`/the row, each carrying its "was typed beside the
library" before-record; `#dqa`'s Settled-by column names rule · version · matrix · limit.

**The walk.** By real clicks at 1280 **and** 375 px: `#qc-limits` → `#composite` →
`#qc-limits` → `#qc` → `#dqa`, every hop landing on its target. Both screens eyeballed at
375 px (screenshots `375-qc-limits.png`, `375-composite.png`, auditor's scratch area):
dense, real content end to end, no blanks, no breakage.

## Findings

### W16-A-1 · P2 — Wave 13's dated ledger figure in the README is rewritten in place, against the convention the two prior waves established on that same sentence

- **Evidence:** `git log -L 1001,1001:README.md`. Wave 13 wrote "it stands at **five**,
  and the four that closed are kept as dated settled-notes" inside the paragraph headed
  "**Wave 13 (2 September 2026)**…". Wave 14 preserved "**five**" and appended "— four
  after wave 14 —"; wave 15 preserved "**five**" and appended "and four after wave 15,
  which settled one and opened one". Wave 16 **changed the head number to "three"** and
  appended "and three after wave 16".
- **Expected:** per the convention the sentence's own history establishes — and the
  project's correction standard elsewhere in the same file (wave 12's OM-2 dated
  correction beside the original; "dated correction, not a rewrite") — the as-of-wave-13
  figure stays, and the appended trail carries the present: "…stands at five — four after
  wave 14, four after wave 15, …, and **three after wave 16** —".
- **Observed:** the dated head figure silently retro-edited. The current value (three) is
  true and measured, the trail is intact, and no drawn screen is affected — hence P2, not
  the gate.
- **Recommended correction:** restore "**five**" as wave 13's own figure (the trail
  already ends "three after wave 16", so the present remains stated), or keep the edit
  and mark it as a dated correction in the established shape.

No other findings. Categories measured and clean: mechanics (build determinism, census
on both clocks, verify's nine families, diff scope, `state:` history), the ledger and its
byte-identical survivors, the verbatim settled quotation, the glossary words, all four
closure identities, the duplicate arithmetic and its applicability fix, the counted
absence and its verdict, the library's matrix/since/version columns, the histogram, the
typed-limit sweep, the `#qc`/`#dqa` consequences, and the walk at both widths.

## Verdict

**Zero P0, zero P1 — Wave 16 passes the §11 gate at round 1.** One P2 stands
(W16-A-1, the README head-number rewrite), recommended for the response note rather
than blocking. The wave does what its spec says, and — measured rather than taken on
trust — the absent limit is genuinely counted, not invented: the resolver's `held: false`
sentence, the six-record table, and the verdict all trace to one array, and the arithmetic
they judge is reproducible by hand from the raw literals.

**Count: 0 × P0 · 0 × P1 · 1 × P2 · 0 × P3.**
